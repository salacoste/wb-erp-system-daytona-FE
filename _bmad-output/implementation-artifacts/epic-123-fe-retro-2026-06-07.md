# Epic 123-FE Retrospective: AI Feature Activation + Forecast Accuracy Dashboard

**Date**: 2026-06-07
**Epic**: 123-FE
**Stories**: 6 (123.1--123.6), all done
**Duration**: Single session
**Test delta**: ~7800 --> 9870 (net new from this commit + prior batch test work in commits e7d013cc, 663d7e1f, 1e2b038b)
**Commit**: a3882474

---

## Epic Summary

**Objective**: Activate backend-filtered SKU accuracy (#166), simplify confidence normalizer (#180), update revenue confidence comments (#188), build a new Forecast Accuracy Dashboard at `/analytics/forecast-accuracy`, verify admin MAPE/preferences/anomalies (#184, #185, #167), and wire monitoring recovery panel to backend `displayName` + config fields (#187).

**Delivery breakdown by story**:

- **Story 123.1** (~1 SP): Activate `modelId`+`nmId` filtering in `getSkuAccuracy` -- remove `PENDING BACKEND: #166` guard, send `modelId` as query param. **Request #166 RESOLVED.**
- **Story 123.2** (~0.5 SP): Simplify `scaleConfidence` in `ai-forecast-normalizer.ts` -- remove magnitude-detection heuristic (`raw > 1 ? raw / 100 : raw`), replace with plain `Math.min(1, Math.max(0, raw))`. **Request #180 RESOLVED.**
- **Story 123.3** (~0.5 SP): Update `predictedRevenue` comment in normalizer -- clarify that revenue-target models now emit real `predictedRevenue`; retain `0 -> null` mapping for units-target placeholder. **Request #188 RESOLVED.**
- **Story 123.4** (~3 SP): Build Forecast Accuracy Dashboard -- new types (`forecast-accuracy.ts`), API fetcher + normalizer (`forecast-accuracy.ts`), TanStack Query hook (`useForecastAccuracy.ts`), page with `AccuracyMetricsCards` (MAPE/MAE/Bias), `HorizonBreakdownTable`, `SkuBreakdownTable`. Route `/analytics/forecast-accuracy` registered in `routes.ts` + `isProtectedRoute`.
- **Story 123.5** (~1 SP): Verified admin MAPE metrics, AI preferences, anomaly list already correct against live backend. **Requests #184, #185, #167 RESOLVED** (backend shipped in 2026-06-06 batch).
- **Story 123.6** (~1 SP): Update recovery normalizer to consume backend `displayName`, `maxRetries`, `cooldownMinutes`, `maxWindowDays` per task. `RECOVERY_TASK_LABELS` map retained as fallback. **Request #187 RESOLVED.**

**Delivery**: 6/6 stories closed. 20 files changed (+1013/-224). 7 backend requests resolved (#166, #167, #180, #184, #185, #187, #188). 0 production regressions.

---

## Metrics

| Metric | Value |
|---|---|
| Stories completed | 6/6 (100%) |
| Backend requests resolved | 7 (#166, #167, #180, #184, #185, #187, #188) |
| New files created | 9 (4 components + 1 page + 1 hook + 1 API fetcher + 1 types + 1 index export) |
| Files modified | 11 (normalizers, evaluations, routes, tests) |
| Net lines changed | +1013 / -224 |
| New route registered | `/analytics/forecast-accuracy` |
| `PENDING BACKEND:` comments removed | 2 (evaluations.ts #166, useAiSkuAccuracy.ts #166) |
| Vitest baseline | 9870 passing / 0 failed / 676 skipped |

---

## Wins

| # | Win | Evidence |
|---|-----|----------|
| W-1 | **7 backend requests resolved in one epic** -- largest single-epic resolution count; prior 2026-06-06 backend batch unblocked all AI/ML pending items | Requests #166, #167, #180, #184, #185, #187, #188 all marked RESOLVED |
| W-2 | **New Forecast Accuracy Dashboard shipped in Story 123.4** -- full vertical slice (types -> normalizer -> hook -> page with 3 metric cards + 2 breakdown tables) in single story | 9 new files, 286 lines of component code, route registered |
| W-3 | **Normalizer simplification from heuristic to clamp** -- `scaleConfidence` dropped magnitude-detection logic now that backend guarantees 0-1 scale | `ai-forecast-normalizer.ts`: 6 lines removed, 3 added |
| W-4 | **Recovery panel now consumes backend config** -- `displayName`, `maxRetries`, `cooldownMinutes`, `maxWindowDays` flow through; FE-derived labels kept as fallback | `recovery-normalizer.ts`: `toNullableNumber` helpers + prefer-backend-name pattern |
| W-5 | **MissingCogsAlert test rewrite (682 lines)** -- replaced stub/todo tests with real `renderWithProviders` assertions, tooltip interaction, pluralization coverage | Largest single-file test change in this commit |
| W-6 | **Batch backend-request resolution (commit 4543752d)** -- 14 requests marked RESOLVED in prior commit; 7 of those consumed in Epic 123-FE | AI/ML + Financial batch from 2026-06-06 |

---

## Challenges

| # | Challenge | Impact |
|---|-----------|--------|
| D-1 | **6 stories in single commit loses granular history** -- cannot bisect individual story regressions; `git blame` shows all changes under one message | Acceptable for small stories (0.5-3 SP each) but risky if regression surfaces |
| D-2 | **Mixed work types (activation + simplification + new page + verification)** -- testing strategy differs per type but single commit makes it opaque | Verification stories (123.5) leave no code footprint; only test assertions confirm |
| D-3 | **Forecast accuracy page has no E2E coverage yet** -- new route registered but no Playwright spec exercises it | Standard for initial delivery; E2E should follow in next sprint |
| D-4 | **Confidence normalizer test update was mechanical** -- tests swapped from 0-100/0-1 dual-scale assertions to plain 0-1 clamp; risk of test-mirroring-implementation | Normalizer tests should verify edge cases (NaN, negative, >1) independently of implementation |

---

## Insights

| # | Insight | Action |
|---|---------|--------|
| I-1 | **Backend request resolution enables normalizer simplification** -- #180 removing scale inconsistency let FE drop a heuristic. Every `PENDING BACKEND:` is a future simplification opportunity | Audit remaining `PENDING BACKEND:` markers for resolution; simplify when backend ships |
| I-2 | **Verification stories (123.5) are efficient when backend ships before FE checks** -- no code changes needed, just assertion that existing behavior is correct | Prefer verification stories over re-implementation when backend status is uncertain at story start |
| I-3 | **Single-commit batch execution works for small stories but needs per-story test isolation** -- each story's tests must be independently runnable | Maintain per-story test file boundaries even in batch commits |
| I-4 | **Recovery normalizer's prefer-backend-name + FE-fallback pattern is reusable** -- any field where backend adds a previously-frontend-derived value can use the same `typeof === 'string' && ... : fallback` pattern | Document as canonical in CLAUDE-PATTERNS.md if it recurs |

---

## Discoveries

| # | Discovery | Status |
|---|-----------|--------|
| SD-1 | **2026-06-06 backend batch resolved 14 requests across 2 domains** -- AI/ML (6) + Financial (8); largest single-day resolution in project history | Backend requests in `docs/request-backend/` updated in commit 4543752d |
| SD-2 | **`toCount` / `toNullableNumber` helpers (Story 120.1) used consistently in new normalizer** -- `forecast-accuracy.ts` adopts them for all field coercions | Normalizer-helpers adoption now spans 12+ files |

---

## Action Items for Future Epics

| # | Action | Target |
|---|--------|--------|
| A-1 | **Add E2E spec for `/analytics/forecast-accuracy`** -- page has no Playwright coverage yet | Next sprint E2E batch |
| A-2 | **Audit remaining `PENDING BACKEND:` markers for 2026-06-06 batch resolutions** -- some may be resolvable without code changes | Pre-flight for next AI-domain epic |
| A-3 | **Consider prefer-backend-value + FE-fallback pattern for generalization** -- if recovery-panel pattern recurs in other normalizers, codify in CLAUDE-PATTERNS.md | Monitor for 2nd occurrence |
| A-4 | **Evaluate single-commit vs per-story commits for mixed-work epics** -- 6 small stories benefited from batch speed but lost granular history | Apply judgment: batch for <3 SP stories, separate commits for >3 SP |
| A-5 | **Add edge-case tests for `scaleConfidence`** -- verify NaN, negative, >1, Infinity behavior independently of the clamp implementation | Test suite hardening |

---

## Quality Gate Final State

| Gate | Command | Result | Baseline |
|---|---|---|---|
| ESLint | `npx eslint 'src/**/*.{ts,tsx}'` | **0 errors** | Matches |
| TypeScript | `npm run type-check` | **0 errors** | Matches |
| Vitest | `npm test -- --run` | **9870 passing / 0 failed / 676 skipped** | Above floor (7205) |
| Doc citations | `bash scripts/check-doc-citations.sh` | **22 broken -- baseline match** | Matches |
| Dot-locale percent | `bash scripts/check-locale-percent.sh` | **Baseline match** | Matches |

---

## Closing Reflection

Epic 123-FE was an execution-focused epic: activate pending backend features, simplify normalizers now that backend guarantees exist, and ship a new dashboard page. The 7 backend-request resolutions in a single epic set a project record, enabled by the 2026-06-06 backend deployment batch.

The Forecast Accuracy Dashboard (Story 123.4) is the primary deliverable -- a complete vertical slice consuming `GET /v1/ai/forecast-accuracy` with proper boundary normalization, cabinet-scoped query keys, and three metric visualizations (MAPE, MAE, Bias) plus horizon/SKU breakdown tables. The recovery-normalizer prefer-backend-value pattern (Story 123.6) is worth watching for generalization.

5 action items captured. Most immediately: E2E coverage for the new page (A-1) and auditing remaining `PENDING BACKEND:` markers against the 2026-06-06 batch (A-2).
