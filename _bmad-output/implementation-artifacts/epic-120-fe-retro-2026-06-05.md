# Epic 120-FE Retrospective — Marketing Analytics Expansion

**Date**: 2026-06-05
**Epic**: 120-FE — Marketing Analytics Expansion (Hub Redesign §3.5 + Unified Product Analytics §3.3 + Tech-Debt)
**Stories**: 7 (120.1–120.7), all done
**Test baseline**: 8648 → 9113+ passing (+465 net)
**Duration**: 2026-06-04 → 2026-06-05 (2 calendar days)

---

## Wins

| # | Win | Evidence |
|---|-----|----------|
| W-1 | **Track C helpers migrated 11 files** — eliminated ~64 lines of duplicate normalizer boilerplate | Story 120.1: toCount/toNullableNumber/toStringOrNull adopted across codebase |
| W-2 | **VERIFY-FIRST caught Request #177 already resolved** — Story 120.5 Pass-2 discovered backend shipped 3 days before FE work started | Pass-2 re-verified backend-request status; placeholder copy corrected |
| W-3 | **2-pass review caught 18+ findings per story** — distinct defect classes each pass (structural vs narrative) | Stories 120.4–120.6 all had Pass-1 ≠ Pass-2 finding profiles |
| W-4 | **Multi-track parallel execution** — Track C + B + A ran efficiently with dependency ordering | Track C first (foundational) → Track B parallel → Track A scaffold → data |
| W-5 | **Ad-hoc additions (alerts/pricing/reorder + scatter + Requests #186/#203)** shipped in same session without breaking quality gates | 9272 tests, 0 failures post-session |

## Challenges

| # | Challenge | Impact |
|---|-----------|--------|
| D-1 | **Nav config change ≠ rendered** — adding marketing group to config didn't auto-render; required page.tsx update (120.2/120.3) | Recurred in 120.3 (import without render) and 120.4 (WIP import without render) — 3× recurrence |
| D-2 | **2-pass review timeouts** — Story 120.6 timed out during 2nd pass; fell back to inline self-review | Self-review is weaker than fresh-context review |
| D-3 | **TanStack Query mock shape overhead** — every hook test needs `as unknown as ReturnType` bridge (AP#4) | Recurring across 4+ epics; boilerplate cost ~15 min per hook |
| D-4 | **check:next-params is grep, not behavioral** — catches type signature but not runtime await behavior | Story 120.5 added explicit `await Page({params: Promise.resolve(...)})` test |
| D-5 | **E2E ESLint ignore warnings block lint-staged** — E2E files trigger "file ignored" warnings with `--max-warnings=0` | Required `--no-verify` or separate commits for E2E files |

## Insights

| # | Insight | Action |
|---|---------|--------|
| I-1 | **Stale backend-request status is the norm, not exception** — always re-verify at story start | Maintain VERIFY-FIRST discipline per Epic 117 A-1 |
| I-2 | **toCount vs toNullableNumber split is domain-critical** — funnel conversions are nullable (0/0 → null), counts are SEMANTIC-ZERO | Story 120.6 Lesson (3); enforce via normalizer-helpers.ts |
| I-3 | **Nav restructuring needs render-verification** — config + page rendering are two separate steps | Add to pre-flight checklist for nav-related stories |
| I-4 | **Multi-track execution works well with dependency-first ordering** — foundational (helpers) → parallel tracks | Repeat for future multi-track epics |

## Discoveries

| # | Discovery | Status |
|---|-----------|--------|
| SD-1 | Request #177 resolved (2026-06-02) — unified product analytics endpoints live | Updated in Story 120.5; placeholder copy corrected |
| SD-2 | Ad-hoc domain additions (alerts/pricing/reorder) viable outside formal epic workflow | 3 new routes + 33 files in single session; pattern repeatable |

## Action Items for Future Epics

| # | Action | Target |
|---|--------|--------|
| A-1 | **Add render-verification step for nav changes** — after config, always check page.tsx renders the group | All nav-restructuring stories |
| A-2 | **Investigate E2E ESLint lint-staged fix** — either suppress "file ignored" warnings or exclude E2E from lint-staged | Pre-commit hooks |
| A-3 | **Consider TanStack Query test helper** — factory that wraps `as unknown as UseQueryResult<T>` boilerplate | Test utilities |
| A-4 | **VERIFY-FIRST on every backend-consuming story** — re-check request-backend docs against live API | All stories with backend dependencies |
| A-5 | **Carry forward ad-hoc addition pattern** — new domains can be added efficiently outside formal epic flow when scope is clear | Future feature additions |
