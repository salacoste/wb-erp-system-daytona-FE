# debt-p3-docs-95-zoning — canonical-vs-archival split baseline (V20 §4 п.4)

| Поле | Значение |
|---|---|
| **Status** | review (флип → done пост-merge) |
| **PR** | pending (номер вторым коммитом) |
| **Head-SHA** | closeout-коммит (ветка `debt/docs95-zoning`, база `0b23ae16` = post-#432 main) |
| **Класс** | doc-only (baseline-файл: комментарии + порядок; гейт-семантика нетронута) |
| **Дата** | 2026-09-10 |

## Tasks

- [x] Pre-flight 0.3a: recon explore(sonnet) — механика гейта (комментарии в baseline стрипаются сегодня: `compare_to_baseline` + self-test-фикстуры), контент-анализ 95 записей (каноническая зона ФАКТИЧЕСКИ ПУСТА), история (owner-approved архивная концепция `26a97561` уже существует), опции дизайна
- [x] Манифест-префлайт: baseline не пиннут манифестом 174.3 (0 пересечений)
- [x] Трансформация: детерминированный python-классификатор (host-doc маппинг через `git grep` по цитате, self-assert распределения 1/4/90; первая итерация ПОЙМАЛА собственный дефект — `.bak`-хост не распознан архивным, ассерт отклонил запись, фикс, перепрогон)
- [x] Верификация: bare-гейт exit 0 ДО и ПОСЛЕ · мультисет записей идентичен (95, sort-diff) · 3 зонных маркера · дифф +19/−5
- [x] Реестр §24 APPEND + этот артефакт

## Dev Agent Record

### Имплементация

`scripts/.check-docs-baseline.txt` перезонирован комментариями (гейт стрипает их сегодня — зоны
нарративны, не энфорсмент): **CANONICAL: 1** (`src/hooks-v1/useMarginTrends.ts:70` — единственная
живая дока `docs/VALIDATION-PLAN.md`; цитирует навсегда удалённый `src/hooks-v1/` — чинится только
переписыванием цитаты в доке) · **ARCHIVAL-CANDIDATE: 4** (process/planning/competitor) ·
**ARCHIVAL: 90** (frozen story-артефакты, docs/archive/, docs/stories/epic-60/, `.bak`; owner-approved
архивная трактовка `26a97561` от 2026-08-17). Хедер-дисклоужа: `--update-baseline` регенерирует файл
ПЛОСКО и молча уничтожает зоны — пересплит после каждого регена (канон).

### Ключевой факт (премисса item'а скорректирована рекон-ом)

Каноническая зона пуста в содержательном смысле: все 95 битых цитат живут в замороженных доках.
Нынешний гейт УЖЕ энфорсит содержательный инвариант неявно («ноль битых цитат в живых доках» — любой
новый красит exit). Зонирование добавляет нарратив и осознанность (цель item'а), не семантику;
энфорсмент по зонам (вариант 3 рекон-а) блокирован структурно: baseline не хранит host-doc в ключе —
изменение формата записи = architect-level gate-semantics change, НЕ делается без отдельного решения.

### Верификация (живые прогоны)

| Проверка | Результат |
|---|---|
| `bash scripts/check-doc-citations.sh` (bare, без пайпа) | exit 0 до И после; «broken citations match baseline (95)» |
| Мультисет записей (sort-diff до/после) | идентичен (95) |
| Классификация | self-assert 1/4/90 = recon-распределение (`.bak`-нюанс пойман ассертом) |
| git diff | 1 файл, +19/−5 |

## File List

| Файл | Дельта |
|---|---|
| `scripts/.check-docs-baseline.txt` | +19/−5 (зонные маркеры + хедер-дисклоужа + порядок; записи байт-идентичны) |
| `_bmad-output/planning-artifacts/shadcn-migration-status-and-debt-registry.md` | §24 APPEND |
| `_bmad-output/implementation-artifacts/debt-p3-docs-95-zoning.md` | NEW |

## Change Log

| Дата | Событие |
|---|---|
| 2026-09-10 | Recon (премисса скорректирована: canonical пуст) → doc-only зонирование 1/4/90 → гейт/мультисет-верификация → реестр §24 → 1 ревью-проход (doc-only lane, owner-прецедент #420/#423) |

**Lessons:** (1) Self-assert на распределении классов пойманное расхождение (.bak ≠ архивный префикс) до записи — классификаторы пиши с рекон-эталоном в ассерте. (2) «Зонирование» gate-файла без изменения формата записи — нарратив: `--update-baseline` регенерирует плоско, дисклоужа обязательна. (3) Премисы handoff'ов проверяй рекон-ом: «canonical-vs-archival split» оказался «canonical пуст, split = осознанность».
