# Story 72.6-FE: Buyout Hook Migration

## Status: done

| Field | Value |
|-------|-------|
| **Epic** | 72-FE: Marketing Analytics Data Alignment |
| **Story Points** | 1 |
| **Priority** | P3 |
| **Dependencies** | None |
| **Predecessor** | 72.5-FE (Buyout Table Refactor & Enrichment Fix) |

---

## User Story

**As a** developer,
**I want** the buyout analytics hook verified at its canonical path and covered by unit tests,
**so that** all hooks follow the standard project structure and the buyout domain has test coverage.

---

## Acceptance Criteria

### AC-1: Hook Accessible at `@/hooks/use-buyout-analytics`
- [x] `use-buyout-analytics.ts` is accessible via `@/hooks/use-buyout-analytics` import path
- [x] All consumers import from `@/hooks/use-buyout-analytics` (not `@/hooks-v1/`)
- [x] `npm run type-check` — zero errors
- [x] `npm run lint` — no NEW errors

### AC-2: Zero `@/hooks-v1/` Direct Imports in Codebase
- [x] Grep for `from '@/hooks-v1/'` returns zero results across entire `src/`
- [x] Any discovered `@/hooks-v1/` imports are migrated to `@/hooks/`

### AC-3: Unit Tests for Buyout Analytics Hooks
- [x] New test file `src/hooks-v1/__tests__/use-buyout-analytics.test.ts` created
- [x] `useBuyoutBySku` tested: correct query key, enabled logic, params merging, API function called
- [x] `useBuyoutSummary` tested: correct query key, enabled logic, API function called
- [x] All new tests pass

### AC-4: Validation
- [x] `npm run type-check` — zero errors
- [x] `npm run lint` — no NEW errors
- [x] `npm test` — no regressions beyond pre-existing failures

---

## Technical Details

### Architecture

```
CRITICAL DISCOVERY: src/hooks is a SYMLINK → src/hooks-v1
├── src/hooks → hooks-v1   (symlink, created Feb 15)
├── src/hooks-v1/           (REAL directory, 103+ hook files)
│   ├── use-buyout-analytics.ts    (TARGET — already here, 50 lines)
│   ├── use-all-products-map.ts    (Created in 72.5)
│   ├── useProducts.ts             (Legacy PascalCase)
│   └── __tests__/                 (62 test files, NONE for buyout)
```

**Implication**: The hook is ALREADY at `@/hooks/use-buyout-analytics` because the symlink resolves `src/hooks/` → `src/hooks-v1/`. No physical file move is needed. The original epic tasks assumed separate directories.

### What Actually Needs Doing

1. **Fix the ONE `@/hooks-v1/` import** in `ProductCombobox.tsx:20`:
   ```typescript
   // BEFORE (anomalous direct hooks-v1 import)
   import { useProducts } from '@/hooks-v1/useProducts'
   // AFTER (standard import via symlink)
   import { useProducts } from '@/hooks/useProducts'
   ```

2. **Add unit tests** for `use-buyout-analytics.ts` — no test file exists.

3. **Validate** all imports are correct.

### Current Consumers (already correct)

| File | Import | Status |
|------|--------|--------|
| `BuyoutTable.tsx` | `from '@/hooks/use-buyout-analytics'` | ✅ Correct |
| `BuyoutSummaryWidget.tsx` | `from '@/hooks/use-buyout-analytics'` | ✅ Correct |
| `BuyoutSummaryWidget.test.tsx` | `from '@/hooks/use-buyout-analytics'` | ✅ Correct |

### Known `@/hooks-v1/` Anomaly (1 file)

| File | Import | Fix |
|------|--------|-----|
| `src/app/(dashboard)/analytics/search/components/ProductCombobox.tsx:20` | `from '@/hooks-v1/useProducts'` | → `from '@/hooks/useProducts'` |

### Hook Under Test: `use-buyout-analytics.ts` (50 lines)

```typescript
// 2 exported hooks:
// 1. useBuyoutBySku(from, to, params?) — Per-SKU buyout rates
//    - Default params: source='blended', trend=true, limit=50, offset=0
//    - Query key: buyoutQueryKeys.bySku(fullParams)
//    - enabled: !!from && !!to
//    - staleTime/gcTime from BUYOUT_CACHE
//
// 2. useBuyoutSummary(from, to, source?) — Cabinet-level summary
//    - Query key: buyoutQueryKeys.summary(params)
//    - enabled: !!from && !!to
```

### Test Plan: `use-buyout-analytics.test.ts`

Mock `@/lib/api/buyout-analytics` (getBuyoutBySku, getBuyoutSummary, buyoutQueryKeys, BUYOUT_CACHE).

**useBuyoutBySku tests:**
1. Calls `getBuyoutBySku` with merged default params
2. Uses `buyoutQueryKeys.bySku()` as query key
3. Disabled when `from` is empty
4. Disabled when `to` is empty
5. Overrides defaults with user params (limit, offset, sort)

**useBuyoutSummary tests:**
1. Calls `getBuyoutSummary` with from/to/source
2. Uses `buyoutQueryKeys.summary()` as query key
3. Disabled when `from` is empty
4. Disabled when `to` is empty

**Test pattern reference**: Follow `src/hooks-v1/__tests__/use-search-analytics.test.ts` or `src/hooks-v1/__tests__/use-funnel-analytics.test.ts` for TanStack Query hook testing patterns.

### Existing Patterns to Follow

| Pattern | Source | Usage |
|---------|--------|-------|
| Hook testing | `src/hooks-v1/__tests__/use-search-analytics.test.ts` | TanStack Query hook test pattern |
| API mock | `vi.mock('@/lib/api/buyout-analytics')` | Mock API module |
| renderHook | `@testing-library/react` + `renderWithProviders` | Hook testing utility |

### Important Notes

1. **NO PHYSICAL FILE MOVE NEEDED**: `src/hooks` is a symlink to `src/hooks-v1`. The hook is already accessible at `@/hooks/use-buyout-analytics`. Do NOT move/copy/delete the file.
2. **ProductCombobox fix is small**: One import line change. Verify the component still works after.
3. **No buyout hook tests exist**: This is the primary value of the story — adding test coverage.
4. **BUYOUT_CACHE constants**: Defined in `src/lib/api/buyout-analytics.ts`. Mock them in tests.
5. **buyoutQueryKeys factory**: Also in `src/lib/api/buyout-analytics.ts`. Important for testing query key correctness.
6. **Pre-existing test failures**: 5-6 failed tests are pre-existing and unrelated: MetricsGrid, NetProfitCard, types, MarginByBrandTable, MarginByCategoryTable.
7. **Predecessor learnings (72.5)**: Mock apiClient or API module directly (not hooks). Use `waitFor` for async assertions. Always verify actual import paths vs assumptions.

---

## Tasks

### Task 1: Fix ProductCombobox `@/hooks-v1/` Import
- [x] Open `src/app/(dashboard)/analytics/search/components/ProductCombobox.tsx`
- [x] Change line 20: `from '@/hooks-v1/useProducts'` → `from '@/hooks/useProducts'`
- [x] Verify with `npm run type-check`

### Task 2: Verify Zero `@/hooks-v1/` Imports Remain
- [x] Grep entire `src/` for `from '@/hooks-v1/'`
- [x] Confirm zero results after Task 1 fix

### Task 3: Create Buyout Analytics Hook Tests
- [x] Create `src/hooks-v1/__tests__/use-buyout-analytics.test.ts`
- [x] Mock `@/lib/api/buyout-analytics` module
- [x] Write 5 tests for `useBuyoutBySku` (params merge, query key, enabled logic, API call, overrides)
- [x] Write 4 tests for `useBuyoutSummary` (params, query key, enabled logic, API call)
- [x] Target: under 120 lines (actual: 189 lines including full mock data)

### Task 4: Validation
- [x] `npm run type-check` — zero errors
- [x] `npm run lint` — no NEW errors
- [x] `npm test` — no regressions beyond pre-existing failures
- [x] Grep `from '@/hooks-v1/'` returns zero results

---

## Dev Notes

- **This is a 1 SP story**: 1 import fix + 1 new test file + validation. Low risk, no API changes.
- **The symlink means physical migration is unnecessary**: All effort goes to tests and import cleanup.
- **Test file location**: `src/hooks-v1/__tests__/use-buyout-analytics.test.ts` — colocated with other hook tests.
- **Follow sibling test patterns**: Check `use-search-analytics.test.ts` or `use-funnel-analytics.test.ts` for renderHook + mock patterns.

### Project Structure Notes

- Hook: `src/hooks-v1/use-buyout-analytics.ts` (50 lines, DO NOT MOVE — symlink handles path)
- API: `src/lib/api/buyout-analytics.ts` (getBuyoutBySku, getBuyoutSummary, buyoutQueryKeys, BUYOUT_CACHE)
- Types: `src/types/analytics-buyout.ts` (BuyoutBySkuParams, BuyoutSource)
- Tests: `src/hooks-v1/__tests__/use-buyout-analytics.test.ts` (CREATE)
- Fix: `src/app/(dashboard)/analytics/search/components/ProductCombobox.tsx` (1 import line)

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story 72.6-FE] — Epic definition
- [Source: src/hooks-v1/use-buyout-analytics.ts] — Hook under test (50 lines)
- [Source: src/lib/api/buyout-analytics.ts] — API client with query keys and cache config
- [Source: src/types/analytics-buyout.ts] — Type definitions for buyout domain
- [Source: 72.5-fe story] — Predecessor learnings (symlink discovery, test mock patterns)

---

## Change Log

- 2026-03-08: Story created — Buyout hook migration reframed after symlink discovery. No physical move needed (src/hooks → hooks-v1 symlink). Real tasks: fix 1 @/hooks-v1/ import anomaly in ProductCombobox, add unit tests for use-buyout-analytics hooks (none exist), validate.
- 2026-03-08: Story implemented — Fixed ProductCombobox import, created 9 unit tests for useBuyoutBySku + useBuyoutSummary, all validations pass. Zero @/hooks-v1/ imports remain.
- 2026-03-08: Code review #1 — 3 findings (2 low, 1 info). All fixed: (1) Added 2 error handling tests for API rejection (matching sibling use-search-analytics.test.ts pattern), (2) Story task line count note acknowledged, (3) Fixed mock response `source: 'blended'` → `'weekly'` to match test scenario. Tests: 11/11 passed. Story → done.

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.6

### Completion Notes
- All 4 tasks completed. 1 new file created + 1 file modified.
- `npm run type-check`: zero errors
- `npm run lint`: zero errors (no NEW errors)
- `npm test`: 11/11 new tests passed (9 original + 2 error handling from code review), 49/49 existing buyout tests passed
- Grep `from '@/hooks-v1/'` returns zero results across entire src/
- Pre-existing ESLint config issue: direct `npx eslint` on `hooks-v1/__tests__/*.test.ts` files fails due to symlink path resolution (affects ALL 47 test files in that directory, not just ours). `npm run lint` (via next lint) passes clean.

### File List

#### New Files
- `src/hooks-v1/__tests__/use-buyout-analytics.test.ts` — 11 tests for useBuyoutBySku (6) and useBuyoutSummary (5): params merging, query keys, enabled logic, API calls, param overrides, error handling

#### Modified Files
- `src/app/(dashboard)/analytics/search/components/ProductCombobox.tsx` — Fixed import: `@/hooks-v1/useProducts` → `@/hooks/useProducts` (line 20, 1-line change)
