# Story 128.10 Frontend Verification Evidence

This directory is immutable historical evidence for Story 128.10. Its command
manifest is branch-bound: `requiredBranch` names the former
`feat/epic-128-10-frontend-verification-foundation` feature branch on which the
evidence was captured.

Do not use these scripts or their recorded results as the current project-wide
validation entry point. For current commands, use the root `README.md` **Local
validation** section together with the active story plan.
