# SEC-DOC-1 — Owner Decision Request: ротация тест-креденшела и смежные поверхности

**Дата**: 2026-09-02 · **From**: V14 debt-backlog orchestrator (S-2, §8-P0) · **Статус**: ожидает решения владельца

> Документ не содержит самих секретов (non-echoing): литералы обозначены масками
> `stale-литерал` (заменил live в 2026-01…08; в `.env.e2e` отсутствует) и
> `live-литерал` (действующий пароль seeded-тест-юзера, подтверждён живым `.env.e2e`).

## Что уже сделано (не требует решения)

- **FE-репо, tracked-дерево**: оба парольных литерала изъяты (35 файлов: docs/, test-api/, thoughts/, CLAUDE.md, _bmad-output/) — заменены на env-плейсхолдер `<E2E_TEST_PASSWORD>`; `CLAUDE.md` «Test Credentials» переписан в ссылку на untracked `.env.e2e`.
- **Верификация**: non-echoing `git grep` по обоим литералам = **0** в tracked-дереве FE (проверено оркестратором независимо от исполнителя); гейты docs-95 / lessons-0 / vitest 19415/0 / build — зелёные.
- **Git-история НЕ переписывалась** (канон: no autonomous history rewrite).

## Факты для решения

1. **Live-литерал был в plaintext в 13 tracked-вхождениях FE** (8 файлов, включая действующий процесс-канон V10 и 3 свежих handoff) — т.е. находился в remote-репо GitHub всё это время. Изъят из HEAD; остаётся в git-истории и в уже сделанных клонах/кэшах.
2. **BE-репо**: 135 вхождений stale-литерала (README, docs/, API-TESTING-QUICK-START, CREDENTIAL-SECURITY-REMEDIATION.md и др.; read-only для FE-оркестратора — не трогал). Live-литерал в BE: 0 вхождений.
3. **BE-репо также трекает 9841-файловое зеркало `frontend/`** — зеркальные копии FE-доков содержат оба литерала до следующей mirror-синхронизации (bulk-staging зеркала гейтится `FRONTEND_MIRROR_OK=1`).
4. Креденшел — **локальный seeded-тест-юзер** (`test@test.com`), не прод-секрет; email сознательно не классифицирован как секрет (см. артефакт).
5. `check-privacy`-сканер не поддерживает `.http`-файлы в change-set-режиме (error-строки, не утечка) — мелкий инструментальный долг.

## Решения (за владельцем)

| # | Вопрос | Варианты | Рекомендация |
|---|--------|----------|--------------|
| D-1 | **Ротация live-литерала** (пароль seeded-тест-юзера) | (a) ротировать: обновить seeded-юзера в local/dev БД + `.env.e2e` (значение хранить только там); (b) принять риск (локальный тест-кред, экспозиция в private-репо) | **(a) ротировать** — дёшево (одна local-DB правка + один env-файл), устраняет живую экспозицию в истории/клонах |
| D-2 | **BE-репо cleanup** (135 stale-вхождений + зеркало) | (a) отдельная BE security-lane тем же плейсхолдер-паттерном; (b) оставить (stale-литерал мёртв); зеркало синхронизируется обычным mirror-процессом | **(a)** для консистентности; минимум — mirror-синк (подтянет FE-фиксы в зеркало) |
| D-3 | **Git-history remediation** | (a) ничего (литералы остаются в истории); (b) history rewrite (filter-repo + force) | **(a) ничего** — rewrite разрушителен для 94-сторийной evidence-истории; риск остаточный и закрывается ротацией (D-1a) |
| D-4 | `.http`-поддержка `check-privacy`-сканера | (a) добавить `.http` в isAllowedTextPath; (b) игнорировать | **(a)** — однострочный фикс, закрывает слепую зону сканера |

## DoD-хвост

Item S-2 закрывается полностью при: скан=0 (✅ сделано) + **ротация подтверждена владельцем** (D-1). До решения D-1 статус в реестрах: `redacted; ротация — owner`.
