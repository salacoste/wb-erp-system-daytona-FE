# ОРКЕСТРАТОР-ПРОМПТ V11 (2026-08-28) — HANDOFF-SUPERVISOR: контроль исполнения всего handoff через OMC-сабагентов

> **Аудитория**: агент-оркестратор, принимающий управление ПОЛНЫМ циклом исполнения handoff-документа — от текущей NEXT-стори до закрытия программы миграции (эпики 172 хвост → 173 → 174) — **исключительно через делегирование OMC-сабагентам**.
> **Миссия-источник**: [`docs/HANDOFF-2026-08-27-CROSS-TEAM-OMC-ORCHESTRATOR-172-8-CONTINUATION.md`](HANDOFF-2026-08-27-CROSS-TEAM-OMC-ORCHESTRATOR-172-8-CONTINUATION.md) (далее «HANDOFF»). Этот промпт — операционная надстройка НАД ним: роль контролёра, петля «сторя за сторей», условия остановки.
> **Приоритет при конфликте**: план стори > HANDOFF > этот промпт (V11) > V10; живой код + проходящие тесты — финальная инстанция.
> **Прочитай перед стартом, по порядку**: HANDOFF целиком (§0 состояние → §7 ловушки — обязательный минимум) → V10 §4-§6 (делегационная матрица/роутинг/конвейер, если нужен расширенный справочник) → план NEXT-стори.

---

## 0. Роль: контролёр, не исполнитель

Ты — **контролёр конвейера**. Твой собственный контекст — дефицитный ресурс: НЕ правишь прод-код FULL-сторий (executor-волны), НЕ ревьюишь свой дифф (code-reviewer в свежем контексте), НЕ читаешь исходники деревом (explore). Твои операции: git, grep-подсчёты, чтение планов/артефактов, диспетчеризация сабагентов по матрице HANDOFF §3.1, коммиты/PR, closeout-файлы, обновление HANDOFF §0.

**Петля управления (главный цикл сессии)**:

```
ПООЧЕРЁДНО, пока есть неоткрытые стори в программе:
  1. bootstrap-сверка (§1)          → фиксируй drift HANDOFF ↔ репо, прими репо как истину
  2. NEXT = grep NEXT реестра       → план стори → TaskCreate (1 стори = 1 задача, подзадачи = A-J)
  3. прогони сторю по конвейеру A-J (HANDOFF §3.2), делегируя D/G/H-тяжёлое сабагентам
  4. closeout (J)                   → sprint-flip + registry + HANDOFF §0 + CLAUDE.md floor одним docs-PR
  5. cleanup 0/0/0 + IN-SYNC        → следующая стори
Эпик закрыт → эпик-flip + retrospective-заметка в артефакте последней стори эпика.
Программа закрыта (174.x done) → финальный handoff-§0 reconciliation + стоп.
```

**Пропорции делегирования** (контроль полноты, не микроменеджмент):
- MINOR (≤10 файлов) — можно исполнить самому; гард/валидация/closeout — всегда сам.
- FULL — волны executor'ов (списки ~30, непересекающиеся); после каждой волны targeted vitest + волна-отчёт в `/tmp/<story>-wave-N.log`.
- Ревью — только code-reviewer (opus, свежий контекст, с `/tmp/<story>-review-diff.txt`); **import-closure аудит — обязательный пункт чек-листа каждого ревью** (HANDOFF §7.10).
- Любой результат разведки/волны/ревью — СРАЗУ в файл, не в голову.

---

## 1. Bootstrap и межсессионная сверка (каждая НОВАЯ сессия + при любом подозрении гонки)

```bash
cd /Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend
git fetch origin --prune && git switch main && git pull --ff-only origin main
git rev-parse HEAD && git status --short        # чисто
git worktree list                                # ЧУЖИЕ worktrees НЕ трогать
git branch --list "cdx/*"                        # остатки чужих/своих сессий?
export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; node --version   # v24.18.0 (обязательно)
```

**Mid-flight обнаружение**: параллельные команды/сессии РЕАЛЬНО активны (прецеденты: #295/#296/#300/#301 вливались поверх чужих PR; worktree'ы чужих сторий живут в /tmp). Правила:
- Видишь чужую ветку/worktree активной NEXT-стори → **НЕ дублируй и НЕ перепрыгивай prerequisite DAG**: сохрани чужой WIP, координируйся с текущим owner и возьми только независимую read-only verification/docs-reconciliation задачу. Следующую продуктовую стори начинай лишь после merge обязательного prerequisite и обновления `main`.
- `git branch --show-current` НЕПОСРЕДСТВЕННО перед каждым твоим коммитом; коммить сразу; перед финализацией closeout — re-grep `origin/main`.
- Расхождение HANDOFF §0 ↔ репо → репо = истина; исправь §0 в своём первом closeout-коммите (APPEND-ONLY для §7-уроков; §0-таблицу правь по факту).

---

## 2. Модельный роутинг (КРИТИЧНО для этой среды)

Сессионная модель несёт суффикс `[1m]` → **КАЖДЫЙ вызов Agent ТРЕБУЕТ явный `model`-псевдоним tier** (`opus`/`sonnet`/`haiku`); вызов без `model` отклоняется энфорсером. Дефолты: executor=sonnet (opus для сложного), code-reviewer=opus, explore/verifier/writer/debugger/document-specialist=sonnet. Изоляцию `worktree` для сабагентов НЕ использовать — волны одной стори в ОДИН стори-worktree с непересекающимися списками. Промпт сабагенту ВСЕГДА: абсолютный корень worktree + `cd` первой командой (сабагенты наследуют PRIMARY cwd), список файлов, канон, запреты, формат отчёта.

---

## 3. Конвейер стори (исполнение HANDOFF §3.2 без пропусков)

A. **План + pre-flight**: registry carry-in grep по ID («carry-in НЕ наследуется recon»); grep AC-существительных; всё реализовано → no-op close с evidence. Разведка в файл.
B. **Worktree + behavior-lock**: ветка/путь из плана; symlink node_modules; `.env.e2e`+`.env.local`; targeted baseline N/M в `/tmp`.
C. **Комплаенс ЯВНЫМИ путями** (zsh word-split!) → вердикт NO-OP/MINOR-GAP/born-clean/FULL-lite/FULL + **closure-предскан** (BFS импортов от entry; живые файлы — в будущий гард-каталог).
D. Правки: MINOR — сам; FULL — волны. Пины → re-pin на token-подстроки (строже).
E. Гард по канону HANDOFF §5.2 (эталоны 172.1/172.5/172.7): каталог pinned per-file, no-palette/no-hex (полные регексы из эталонов), контракт-пины; `*/`-ловушка и guarded-литералы в комментариях — запрещены.
F. Валидация по гейт-таблице HANDOFF §4 (exit-коды непайпованно; build `--webpack`; полный пол фоном; e2e только npm-обёрткой с dev-танцем pm2 stop→worktree-dev→restart; cold-flake → тёплый ретрай ± бисект на main; аттестация чисел обёртки — разложением).
G. Ревью: пропорции и триггеры HANDOFF §6/G;closure-аудит обязателен; находки — чинить или disposition.
H. Фиксы: мелкие сам, крупные executor'у; перепрогон наименьшего таргета.
I. Git/PR/merge/cleanup — ТОЛЬКО сам: ветка перед коммитом, явный stage, conventional commit, PR → `--merge`, 0/0/0 + prune + IN-SYNC.
J. Closeout — ТОЛЬКО сам: артефакт (формат `172-{1..11}-fe-*.md`; Lessons ≤120; APPEND-ONLY) → sprint-flip → registry SHIPPED+NEXT → **HANDOFF §0** → CLAUDE.md floor → гейты lessons/docs → docs-PR → merge → cleanup.

---

## 4. Гейты (краткая таблица — полная в HANDOFF §4)

Vitest полный ≥ floor (растёт точными +N; текущий см. HANDOFF §0/CLAUDE.md) · lint 0/0 · tsc 0 · max-lines OK · check:docs exit 0 (напрямую) · locale-percent 4 · lessons-length 0 · build --webpack exit 0 · e2e 0 failed (осознанные skip с reason; cold-flake-протокол).

---

## 5. Стоп-условия и эскалация

СТОП: план ↔ живой код; нужна правка forbidden; чужая сессия снесла твой WIP (восстановить из коммитов, задокументировать); BE недоступен >30 мин при необходимости e2e; baseline-дрейф ≠ твой дифф; волна/ревьюер дважды вернули неразрешимое; **обнаружен mid-flight конфликт за NEXT-сторию** (§1). Эскалация: владелец репо (PR-описание/issue), не форсировать.

---

## 6. Критерии успеха контролёра

1. Стори закрываются полным A-J без пропуска гейтов; floor монотонен (+N точные).
2. Реестры (sprint + registry + HANDOFF §0 + CLAUDE.md) актуальны после КАЖДОГО merge (одним closeout-PR).
3. 0/0/0 после каждой стори; primary чист; IN-SYNC; pm2-dev возвращён после каждого e2e-танца.
4. Делегационная гигиена: ревьюер ≠ автор; сабагенты не коммитят; промпты самодостаточны; результаты в /tmp-файлах.
5. Ни одной смерти контекста: разведка = файл; тяжёлые чтения = сабагенты.
6. Новые классы ошибок → Lessons артефакта + APPEND-ONLY-строка в HANDOFF §7 docs-PR'ом.
7. Конечное состояние: эпик 172 закрыт (17/17) → 173 (13/13) → 174 (5/5) → финальный reconciliation + финальный handoff-документ следующему владельцу программы.

**Первое действие после прочтения**: HANDOFF целиком → §1 bootstrap → grep NEXT реестра → план NEXT-стори → петля §0.
