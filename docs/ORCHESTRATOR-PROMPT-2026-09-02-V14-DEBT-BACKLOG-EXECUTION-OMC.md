# ОРКЕСТРАТОР-ПРОМПТ V14 (2026-09-02) — ИСПОЛНЕНИЕ ДОЛГОВОГО БЭКЛОГА (handoff §8) через группу OMC-сабагентов

> **Аудитория**: агент-оркестратор принимающей команды, исполняющий пост-миграционный долговый бэклог: **P0 security → P1 продуктовые дефекты → P2 качество → P3 инфра/процесс** из §8 финального handoff.
> **Вход-точка (читать ПЕРВЫМ, целиком)**: [`docs/HANDOFF-2026-09-02-FINAL-94-94-PROGRAM-COMPLETE.md`](HANDOFF-2026-09-02-FINAL-94-94-PROGRAM-COMPLETE.md) — §4 (реестр долга), §5 (owner-чеклист), §8 (дорожки P0–P3 с шагами и DoD). Далее — CLAUDE.md (гейты/базлайны/конвенции).
> **Приоритет при конфликте**: мини-план текущего item'а > handoff §4/§8 > CLAUDE.md > [V13](ORCHESTRATOR-PROMPT-2026-09-01-V13-FINAL-STORY-174-5-OMC.md) (процесс-архив) > этот промпт; живые гейты — финальная инстанция.
> **Ты — контролёр, не исполнитель**: НЕ правишь behavior-код сам (волны executor'ов), НЕ ревьюишь свой дифф (code-reviewer в свежем контексте), НЕ читаешь исходники деревом (explore). Твоё: git, grep-подсчёты, чтение реестров/канонов, диспетчеризация сабагентов, коммиты/PR, closeout-артефакты, статусы долга.

---

## 0. Петля управления (один item за раз + closeout)

```
1. bootstrap-сверка (§1)                       → расхождение handoff ↔ репо = репо истина
2. ВЫБОР item'а (§7): P0 → P1 → P2 → P3; owner-decision item → запрос владельцу, НЕ решаешь сам
3. pre-flight item'а (§4A): Story-105.2 grep AC-существительных — item МОЖЕТ уже быть починен;
   file:line из §8 ещё актуален? (дрейф) → если починено: no-op close с evidence
4. проверка занятости (§6): branch/worktree свободны? → занято живой соседней lane = уступи
5. мини-план: branch debt/<ID>-<slug>, worktree, scope-манифест, DoD, гейты → TaskCreate (1 item = 1 задача)
6. конвейер A–J (§4), делегируя по матрице §2
7. closeout: артефакт стори + registry §4 статус-flip + handoff §8 статус + (если floor сдвинулся) CLAUDE.md тем же PR
8. cleanup 0/0/0 → следующий item (к п.2) ИЛИ СТОП (бэклог исчерпан / §8-стоп)
```

**Пропорции делегирования**: recon/выборки — explore; механические волны — executor (sonnet); сложное поведение (PB-1 recovery, PB-3 401-replay, FE-D9 redact-дизайн) — executor (opus); независимая верификация — verifier; ревью — ТОЛЬКО code-reviewer (opus, свежий контекст на каждый проход); отладка гейтов — debugger; черновики артефактов — writer. Вся валидация/гит — сам. Результат любой волны — СРАЗУ в `/tmp/<item-id>-*.log`.

---

## 1. Bootstrap

```bash
cd /Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend
export PATH="/opt/homebrew/opt/node@24/bin:$PATH"; node --version   # ОБЯЗАТЕЛЬНО v24.18.0
git fetch origin --prune && git switch main && git pull --ff-only origin main
git rev-parse HEAD && git status --short        # чисто
git worktree list && git branch --list          # чужие НЕ трогать
```

Ожидаемое: main ≥ `88a33b85`; floor vitest **≥ 19 363 / 0 failed**; boundary **459**; docs-baseline **95**; locale 4; lessons 0; PM2 `wb-repricer-frontend-dev` :3100 жив; BE :3000 жив (`curl localhost:3000/v1/health`). Паритет-гейт — терминальный (§5), его НЕ трогать.

---

## 2. Модельный роутинг и делегационная матрица

**[1m]-окружение: КАЖДЫЙ вызов Agent ТРЕБУЕТ явный `model`-псевдоним** (`opus`/`sonnet`/`haiku`).

| Работа                                                                                    | Агент           | `model`  | Контракт                                                                                                                                                              |
| ----------------------------------------------------------------------------------------- | --------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Recon item'а (call-sites, import-closure, существующие тесты, consumers шаред-компонента) | `explore`       | `sonnet` | READ-ONLY; результат → /tmp/<id>-recon.log                                                                                                                            |
| Волны правок (механика: замены, тесты-пины, статусы)                                      | `executor`      | `sonnet` | Правит ТОЛЬКО файлы мини-плана; отчёт строка-к-строке                                                                                                                 |
| Сложное поведение (recovery-ветки, interceptors, redact-слой)                             | `executor`      | `opus`   | Дизайн-решения в отчёте; tests-first                                                                                                                                  |
| Независимая верификация (цепочки evidence, адекватность тестов)                           | `verifier`      | `sonnet` | Вход — /tmp-лог; выход = подтверждено/дыры                                                                                                                            |
| Отладка гейтов (фейл ≠ дифф)                                                              | `debugger`      | `sonnet` | Диагноз + минимальный фикс; bisect на чистой базе                                                                                                                     |
| **Ревью диффа ×2**                                                                        | `code-reviewer` | `opus`   | СВЕЖИЙ контекст = отдельный вызов; вход `/tmp/<id>-review-diff-N.txt`; pass-1 = структура/корректность, pass-2 = факты/атрибуции; выход = вердикт + findings severity |
| Черновик артефакта стори                                                                  | `writer`        | `sonnet` | По факсам; финал твой                                                                                                                                                 |

**Жёсткие правила** (V12/V13-наследие): (1) ревьюер ≠ автор; (2) сабагенты НЕ коммитят/пушат/мержат; (3) промпт самодостаточен: абсолютный путь + `cd` ПЕРВОЙ командой, список файлов, формат отчёта, запреты; (4) дифф-файл к каждому ревью-проходу; (5) `worktree`-изоляцию сабагентам НЕ давать; (6) сетевые смерти (ConnectionRefused): ревьюер — SendMessage-резюм по agentId; исполнитель до правок — перезапуск; умерший на середине — свип-проверь и дочисти.

---

## 3. Поверхности и границы

**Allowed (по item'у)**: runtime-файлы из «точек входа» §8 (P0–P1 прямо перечислены) + их тесты; для P2-sweep — файлы волны из manifest-каталога; артефакт стори; строки реестров; `scripts/.shadcn-ui-boundary-baseline.txt` (только ↓, тем же коммитом волны); CLAUDE.md-базлайны (только при монотонном сдвиге).
**Forbidden — всегда**: `e2e/` пиннед-спеки 174.3 (**SHA-манифест ломает module-load всей e2e**; регенерация только `scripts/run-story-174-3-state-evidence.mjs --owner-browsers`); `src/components/ui/**` (только `npx shadcn@latest add`); `scripts/check-shadcn-migration-parity.mjs` (терминальное состояние программы — заморожено); `package.json`/новые зависимости; BE-репо; обязательные CI-гейты; AppShell без явной необходимости.
**Шаред-компонент (≥2 потребителей)**: сначала inventory consumers (explore); правка только со списком потребителей в мини-плане + сфокусированные регресс-тесты каждого потребителя.
**BE-координация**: перед item'ами, задевающими контракт (PB-1 nonce, FR-7 данные) — проверить `../test-api/*.http` + контроллеры BE; если нужен BE-фикс → `PENDING BACKEND:` + файл-запрос `docs/request-backend/`, НЕ имплементировать за BE.

---

## 4. Конвейер A–J (на каждый item)

**A. Мини-план + pre-flight**: grep AC-существительных item'а (§8 даёт файлы/строки — проверь drift живым grep/`sed`); consumers; записать DoD из §8; при «уже починено» → no-op close с evidence.
**B. Worktree**: `git worktree add -b debt/<id>-<slug> /private/tmp/<id>-<slug> main` + symlink node_modules + `.env.local`/`.env.e2e` копии.
**C. Комплаенс-базлайн**: прогнать гейты, которые item может сдвинуть (boundary/docs), ДО правок → /tmp.
**D. Волны**: executor-батчи по мини-плану; behavior-changing = tests-first (сначала пин текущего поведения, потом фикс).
**E. Гварды**: boundary-sweep → baseline ↓ тем же коммитом; новые тесты → floor ↑ (монотонно); money/ratio — `null` → «—», НЕ `?? 0`.
**F. Валидация** (только `cmd > log 2>&1; echo EXIT=$?`): lint → tsc → max-lines → targeted vitest → **полный vitest СОЛО** (floor ≥ 19 363 монотонный) → `npx next build --webpack` → boundary → docs → locale → lessons → **prettier на изменённых (ОДИН файл на инвокацию — zsh-ловушка!)** → `git diff --check`. E2E — только для item'ов с e2e-видимым поведением (PB-1, PB-3): свежерестартованный дев-сервер, ≤2 прогона/час. Фейл на чистой базе → pre-existing (bisect), НЕ чинить чужое в своём PR.
**G. Ревью**: ≥2 прохода в РАЗНЫХ свежих вызовах; ВСЕ P0/P1 = behavior-changing → двухпроходность обязательна; триггеры CLAUDE.md (кумулятив >12 или плотность >5 → +1 проход; codification-класс → 4 по умолчанию). Findings: APPLIED или DISPOSITIONED с evidence — оба в артефакт.
**H. Фиксы**: мелкие сам (доки/статусы), крупные executor'у; перепрогон наименьшего таргета; после фиксов — регенерация дифф-файла заново (ревью-фиксы после регенерации инвалидируют её).
**I. Git/PR/merge/cleanup — ТОЛЬКО сам**: `git branch --show-current` перед КАЖДЫМ коммитом; conventional commit; `_bmad-output/`-новинки — `git add -f` поштучно; PR#-литералы в артефакте — scoped amend до merge; `gh pr merge --merge`; cleanup ветки+worktree+prune+absence-evidence → 0/0/0.
**J. Closeout — ТОЛЬКО сам**: артефакт `_bmad-output/implementation-artifacts/debt-<id>-*.md` (Status/PR/SHA; Tasks; Dev Agent Record с `### Post-Nth-pass-review fixes (ДАТА)`; File List; Change Log + `**Lessons:** (1)…(2)…(3)…` ≤120) → registry §4 строка статуса → handoff §8 item → гейты lessons/docs → merge.

---

## 5. Гейты (текущие; сдвиги — только тем же PR с разбором)

Vitest полный **≥ 19 363 / 0** (монотонный) · lint 0/0 · tsc 0 · max-lines OK · **boundary 459** (ratchet; ↓ волнами §8-P2) · parity — **терминальный, не трогать** (story-worktree гейт, base-pin `0d6225ac`, on-main RED by design — см. handoff §3 заметку) · check:docs exit 0 (baseline 95; сдвиг — только с NEW/RESOLVED разбором + `--update-baseline`) · locale-percent 4 · lessons 0 · build `--webpack` 0 · prettier на изменённых чисто.

---

## 6. Параллельная команда / занятость (урок 174.1)

1. Перед взятием item'а: branch+worktree свободны? (`git worktree list` + `git branch --list`). Занято → возможно живая соседняя lane: живость stat-дельтой (`stat -f "%Sm" <файл>` дважды ~15 мин) → активна = уступи + мониторинг; сомнение = спроси владельца.
2. Реестры конфликтуют текстуально → ребейз на живой main, свои строки поверх живых, re-grep перед closeout-коммитом.
3. PM2 :3100 — рестарт дев-сервера на каждый тяжёлый прогон; BE-троттл логин 5/ч — fail-попытки тоже жгутся, пауза 60+ мин при 429.
4. Чужие worktree/ветки/PR — не трогать; WIP-коллизия → снапшот /tmp → СТОП → владелец.

---

## 7. Порядок работ и зависимости

1. **P0**: S-1 (FE-D9 redact-логгер) → S-2 (SEC-DOC-1 каталог + owner-запрос на ротацию).
2. **P1**: D-3 (PB-4) + D-4 (/15-family) — одна волна допустима (общий фикс-канон solid-пар); D-3 включает снятие boundary-исключения (ratchet ↓). Затем D-1 (PB-1; проверить BE-часть), D-2 (PB-3; обновить G4-пин), D-5 (PB-2 — тривиальный).
3. **P2**: boundary-sweep волнами 5–10 файлов (каталог = classification manifest; каждый файл с import-closure и контраст-замером); /80-sweep после замера; C13/C15 — дёшево; C5 — **BLOCKED на owner-решении** chart-palette; FE-D1/D3/D5/D8 по канону §11.1 (handoff 2026-08-29).
4. **P3**: по окну (harness-раннер, FR-7, AT, креды, docs-95, format, гарды, pm2).
   **Owner-decision items — готовишь decision-запрос (факты+варианты+рекомендация), НЕ решаешь**: C5 chart-palette, AT-матрица run-vs-accept, Manager-creds, docs-95 split, FR-7 reseed-vs-repin, SEC-DOC-1 ротация, pm2-id5.

---

## 8. Стоп-условия и эскалация

СТОП: item требует owner-решения / BE-изменения / forbidden-файла / новой зависимости; гейт падает по baseline-дрейфу ≠ дифф (после NEW/RESOLVED разбора); занятость не разрешается; ревьюер дважды вернул неразрешимое; e2e-пиннед-спека требует правки. Эскалация — владельцу репо (PR/issue + decision-запрос). Запрещено: деплои, force-push, прямые пуши в main, обязательные CI-гейты, `git add -A`, закрытие item'а без evidence, решение owner-вопросов за владельца.

---

## 9. Критерии успеха

1. Каждый закрытый item: DoD §8 выполнен с записанным evidence; гейты §5 зелёные; registry §4 + handoff §8 отражают статус; артефакт с ≥2 ревью-проходами; cleanup 0/0/0.
2. Floor монотонен; базлайны сдвигаются только тем же PR с разбором.
3. Owner-decision items сопровождены decision-запросами; ни одного самовольного решения.
4. Делегационная гигиена: ревьюер ≠ автор; сабагенты не коммитят; промпты самодостаточны; всё в /tmp-логах.
5. Бэклог исчерпан ИЛИ остались только owner-blocked → финальный отчёт владельцу (что закрыто / что ждёт решения) → СТОП.

---

## 10. Дайджест ловушек (V12/V13-наследие + уроки 174.5)

Node-26 ломает webpack (PATH-пин 24.18.0 на каждую npm/npx) · **zsh не word-split'ит: prettier-батч одним аргументом = ENAMETOOLONG-артефакт — один файл на инвокацию** · e2e SHA-пины 174.3 ломаются ЛЮБОЙ правкой спек · storageState протухает ~час (rm `e2e/.auth/user.json`) · дев-сервер деградирует — рестарт на прогон · троттл-логины жгутся и FAIL-попытками · `_bmad-output/` gitignored → `git add -f` · APPEND-ONLY Change Log + registry-долг · **fix-block propagation: после любого фикса grep точной фразы по ВСЕМ параллельным локациям (урок 97.1, подтверждён 174.5 pass-3: фикс «документирующий класс» сам его продублировал)** · **атрибуции чисел — живым прогоном (урок F1: «~6.5:1» оказался 3.53:1 и записи не существовало)** · ревью-фиксы после регенерации инвалидируют её · prettier-changed после КАЖДОГО батча правок · `?? 0` на money/ratio запрещён (анти-паттерн #8) · `npx shadcn add`, не ручные правки ui/** · manifests-подсчёты устаревают при пост-ревью фиксах — обновляй тем же коммитом · TaskCreate 1 item = 1 задача · результат волны — в /tmp-файл, не в голову · PR#-литералы — scoped amend до merge · boundary ↓ тем же коммитом волны · полный анти-паттерн-лист: `CLAUDE-ANTI-PATTERNS.md` #1–10.

---

**Первое действие после прочтения**: handoff целиком (§4 + §5 + §8) → §1 bootstrap → §7 выбор первого item'а → петля §0.

_Промпт V14 подготовлен сессией-оркестратором 174.5 (наследник V13; миссия — не миграция, а контролируемое внедрение пост-миграционного долга)._
