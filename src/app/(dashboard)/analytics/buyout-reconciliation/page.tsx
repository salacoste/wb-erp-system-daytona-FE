/**
 * Buyout Reconciliation Page
 * Epic 96-FE Story 96.14: per-SKU table with 3 anomaly columns + Defensive Frontend indicators
 * Route: /analytics/buyout-reconciliation (Option A — distinct from /analytics/buyout Epic 69)
 */

import { BuyoutReconciliationPageContent } from './components/BuyoutReconciliationPageContent'

export default function BuyoutReconciliationPage() {
  return <BuyoutReconciliationPageContent />
}
