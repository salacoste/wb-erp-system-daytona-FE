/**
 * Monitor Dashboard Page
 * Epic 92-FE Story 92.2: KPI Cards + Route Registration
 *
 * Server Component thin wrapper — orchestration logic lives in MonitorPageContent.
 */

import { MonitorPageContent } from './components/MonitorPageContent'

export default function MonitorPage() {
  return <MonitorPageContent />
}
