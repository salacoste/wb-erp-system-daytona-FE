/** Raw API payload diagnostics are intentionally disabled by Story 128.10. */
export function logCogsRawResponse(_endpoint: string, _rawData: unknown): void {
  // Compatibility seam for existing callers; retains and emits nothing.
}

/** Processed API payload diagnostics are intentionally disabled by Story 128.10. */
export function logCogsProcessedResponse(_endpoint: string, _data: unknown): void {
  // Compatibility seam for existing callers; retains and emits nothing.
}
