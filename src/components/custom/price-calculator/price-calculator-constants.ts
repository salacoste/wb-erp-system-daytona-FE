/**
 * Constants for Price Calculator form
 * Story 44.XX: Default acceptance tariff rates, used when API fails
 * Extracted from PriceCalculatorForm.tsx for modularity (Story 74.1)
 */

/** Default acceptance box rate per liter when tariff settings API fails */
export const DEFAULT_ACCEPTANCE_BOX_RATE = 1.7

/** Default acceptance pallet rate when tariff settings API fails */
export const DEFAULT_ACCEPTANCE_PALLET_RATE = 500
