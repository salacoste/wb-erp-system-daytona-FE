---
storyId: '169.15'
storyTitle: 'Align the Shared Frontend Paid-Storage Import Boundary'
epic: '169-FE'
status: backlog
masterPlan: .omx/plans/shadcn-full-ui-migration-master.md
bmadArtifact: _bmad-output/planning-artifacts/epics-166-174-fe-shadcn-migration.md
repository: /Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend
branch: cdx/epic-169-story-15-storage-import-boundary
temporaryWorktree: /private/tmp/wb-repricer-fe-169-15-storage-import-boundary
productionScope: forbidden
---

# OMX Plan — Story 169.15: Align the Shared Frontend Paid-Storage Import Boundary

## Outcome

Align the shared frontend paid-storage import request, lifecycle, result, error, and polling boundary to the real merged Story 169.14 backend contract. The backend-authoritative wire lifecycle remains `pending | processing | completed | failed`; the frontend may retain an internal `unknown` sentinel for malformed or future wire values, but it must stay distinguishable from `failed`, emit defensive diagnostics, and never become false terminal evidence. The route must receive imported-row results and actionable failure detail without route-local contract duplication or a fabricated partial-success state.

This is a non-route shared frontend prerequisite. It changes no storage-route presentation, route-ledger row, generic design-system primitive, product composition, or E2E route spec. Story 169.12 remains the sole owner of `src/app/(dashboard)/analytics/storage/**` migration and browser/visual evidence.

## Authority and exact scope

- **Repository:** `/Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend`.
- **Owned/allowed production files:**
  - `src/lib/api/storage-analytics.ts`;
  - `src/lib/api/storage-import-normalizer.ts`;
  - `src/types/storage-analytics-trends.ts`;
  - `src/types/storage-analytics/index.ts` only if the public export surface must change;
  - `src/hooks/useImportStatus.ts`.
- **Owned/allowed direct tests:**
  - `src/lib/api/__tests__/storage-analytics.test.ts`;
  - `src/lib/api/__tests__/storage-import-normalizer.test.ts`;
  - paid-storage boundary hunks in `src/hooks/__tests__/useStorageAnalytics.test.ts`;
  - Story-specific evidence.
- **Forbidden:** `src/app/(dashboard)/analytics/storage/**`; every route-owned tree; `e2e/storage-analytics.spec.ts`; unrelated API/hook/type/store files; `src/components/ui/**`; product/analytics-shared presentation; tokens/styles/AppShell; package/dependency files; backend repository; deploy/production operations; direct push to `main`; force-push.
- Preserve query keys, mutation invalidation, cabinet/auth headers, date semantics, consumer-visible Russian presentation ownership, and all unrelated storage analytics query behavior.
- A required file outside the exact list is a stop condition and must return to the orchestrator for ownership correction.

**Merged frontend preface baseline:** PR #226 merged as `2c7a3c5931dbc9890ed585eaf71f5717c04453b2`. Inside this Story's allowed surface it already changed `src/types/storage-analytics-trends.ts`, `src/lib/api/storage-import-normalizer.ts`, and `src/lib/api/__tests__/storage-import-normalizer.test.ts` to preserve an internal `unknown` sentinel rather than coerce it to `failed`. It also changed route-owned `src/app/(dashboard)/analytics/storage/components/useStorageImport.ts` and seven other storage-query/fixture/consumer files that remain forbidden here. Story 169.15 locks that preface behavior and owns only the remaining post-169.14 reconciliation; it must not force-edit a path merely to claim work already merged by PR #226.

## Prerequisite DAG and merge evidence

```text
Story 169.14 backend PR merged and cleaned
  -> exact backend merge SHA is an ancestor of backend main
  -> read final request/status/result/error fixtures and API documentation
  -> refresh frontend main
  -> Story 169.15 RED shared-boundary proof
  -> GREEN implementation and local validation
  -> independent review pass 1 -> fixes/reruns
  -> independent review pass 2 -> fixes/reruns
  -> PR merge and mandatory cleanup
  -> Story 169.12 contract closeout may start from refreshed frontend main containing PR #227
```

Before worktree creation, consume the exact Story 169.14 backend merge SHA and Backend Base/foreign-WIP reservation only from the exact committed version-3 30-line final-handoff record and adjacent payload. Independently reconstruct the complete strict nine-line `story-169-14-branch-worktree-cleanup-authority-v1` bytes from the committed backend repository, exact static Story branch/worktree, retained backend reviewed-head/PR hashes, backend-main mode, foreign-WIP hash, version, and `PASS` deletion authorization; require the complete reconstructed-byte SHA-256 to equal the committed cleanup-authority-record hash. Executable synthetics require the valid reconstruction to pass and every changed field or expected hash to fail. Environment values are expected-value assertions only. Require `NONE\n` for clean-local-main, or canonical sorted unique nonempty newline-delimited bytes for recorded-origin-deferral, and reject blank lines, duplicates, unsorted paths, or any self-consistent payload not bound to that independently reconstructed cleanup authority. Do not rediscover authority from commit-message text. Assert backend repository identity before fetch or any other mutation, refresh backend `origin/main`, and prove the recorded merge SHA is its ancestor. A clean local-backend-`main` fast-forward and local ancestry proof remain preferred; local-main equality/ancestry may be deferred only while the same exact recorded foreign-WIP paths still block a safe fast-forward, local `main` remains an ancestor of refreshed `origin/main`, and fresh no-overlap evidence proves that WIP does not touch the Story 169.14 merge or Story 169.15 read-only prerequisite surface. Never stash, commit, reset, clean, restore, or otherwise mutate that WIP. Under recorded-origin deferral, read every prerequisite contract file only from the exact refreshed object database with `git show "$BACKEND_ORIGIN_MAIN_SHA:$PATH"`, record its blob hash, and never use the dirty/behind primary checkout as contract evidence. Prove the exact backend Story branch and worktree were cleaned, and prove all five retired source records plus the retirement transaction are absent. Every expected-absent backend/frontend record, bootstrap, transaction, and exact Story worktree path must also be a non-symlink; a dangling symlink fails closed, with an executable negative synthetic. Refresh frontend `main`, prove PR #226 merge `2c7a3c5931dbc9890ed585eaf71f5717c04453b2` is an ancestor, and inventory any later changes to the allowed surface. Do not simulate a missing or unmerged backend contract and call the frontend boundary GREEN. Scan every linked frontend worktree for staged, unstaged, untracked, and clean committed divergence on the static Story allowlist; also stop for any open PR that changes an allowed path or any all-state PR using the exact Story branch.

## Checkout-independent Git lifecycle

### Create the isolated frontend lane

```bash
set -euo pipefail

FRONTEND_REPOSITORY="/Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend"
BACKEND_REPOSITORY="/Users/r2d2/Documents/Code_Projects/wb-repricer-system-new"
EXPECTED_FRONTEND_GITHUB_REPOSITORY="salacoste/wb-erp-system-daytona-FE"
EXPECTED_BACKEND_GITHUB_REPOSITORY="salacoste/wb-erp-system-daytona"
STORY_BRANCH="cdx/epic-169-story-15-storage-import-boundary"
STORY_WORKTREE="/private/tmp/wb-repricer-fe-169-15-storage-import-boundary"
STORY_EVIDENCE_ARTIFACT="_bmad-output/implementation-artifacts/169-15-fe-align-shared-frontend-paid-storage-import-boundary.md"
STORY_169_14_HANDOFF_ARTIFACT="_bmad-output/implementation-artifacts/169-14-fe-establish-authoritative-paid-storage-import-lifecycle-and-result-contract.md"
STORY_169_14_BRANCH="cdx/epic-169-story-14-paid-storage-import-contract"
STORY_169_14_WORKTREE="/private/tmp/wb-repricer-be-169-14-paid-storage-import-contract"
STORY_169_14_FINAL_HANDOFF_BRANCH="cdx/epic-169-story-14-final-handoff"
STORY_169_14_FINAL_HANDOFF_WORKTREE="/private/tmp/wb-repricer-fe-169-14-final-handoff"
STORY_169_14_HANDOFF_COMMIT_SHA="${STORY_169_14_FINAL_HANDOFF_COMMIT_SHA:?export the exact committed Story 169.14 final-handoff artifact SHA as an expected-value assertion}"
PR_226_PREFACE_MERGE_SHA="2c7a3c5931dbc9890ed585eaf71f5717c04453b2"
STORY_PREFLIGHT_TMP="$(mktemp -d "${TMPDIR:-/tmp}/story-169-15-prerequisite.XXXXXX")"
trap 'rm -rf "$STORY_PREFLIGHT_TMP"' EXIT
assert_failed_git_query_blocks_successor_authorization() {
  local query_kind="$1"
  local authorization_marker="$2"
  local non_repository="$STORY_PREFLIGHT_TMP/non-repository-$query_kind"
  mkdir -p "$non_repository"
  if (
    set -euo pipefail
    case "$query_kind" in
      branch)
        if ! QUERY_OUTPUT="$(git -C "$non_repository" \
          branch --list synthetic 2>/dev/null)"; then
          exit 71
        fi
        ;;
      ls-remote)
        if ! QUERY_OUTPUT="$(git -C "$non_repository" ls-remote --heads \
          "$non_repository/missing-remote.git" \
          refs/heads/synthetic 2>/dev/null)"; then
          exit 71
        fi
        ;;
      status)
        if ! QUERY_OUTPUT="$(git -C "$non_repository" \
          status --porcelain 2>/dev/null)"; then
          exit 71
        fi
        ;;
      diff)
        if ! QUERY_OUTPUT="$(git -C "$non_repository" diff --no-renames \
          --name-only 2>/dev/null)"; then
          exit 71
        fi
        ;;
      ls-files)
        if ! QUERY_OUTPUT="$(git -C "$non_repository" ls-files --others \
          --exclude-standard 2>/dev/null)"; then
          exit 71
        fi
        ;;
      *) exit 64 ;;
    esac
    test -z "$QUERY_OUTPUT"
    : > "$authorization_marker"
  ); then
    exit 73
  fi
  test ! -e "$authorization_marker"
}
assert_failed_git_query_blocks_successor_authorization branch \
  "$STORY_PREFLIGHT_TMP/local-absence-authorized"
assert_failed_git_query_blocks_successor_authorization ls-remote \
  "$STORY_PREFLIGHT_TMP/remote-absence-authorized"
assert_failed_git_query_blocks_successor_authorization status \
  "$STORY_PREFLIGHT_TMP/cleanliness-authorized"
assert_failed_git_query_blocks_successor_authorization diff \
  "$STORY_PREFLIGHT_TMP/scope-authorized"
assert_failed_git_query_blocks_successor_authorization ls-files \
  "$STORY_PREFLIGHT_TMP/successor-authorized"
assert_absent_non_symlink_residue() {
  RESIDUE_PATH="$1"
  test ! -L "$RESIDUE_PATH" && test ! -e "$RESIDUE_PATH"
}
assert_absent_non_symlink_residue \
  "$STORY_PREFLIGHT_TMP/absent-residue-synthetic"
ln -s "$STORY_PREFLIGHT_TMP/missing-residue-synthetic-target" \
  "$STORY_PREFLIGHT_TMP/dangling-residue-synthetic"
if assert_absent_non_symlink_residue \
  "$STORY_PREFLIGHT_TMP/dangling-residue-synthetic"; then
  exit 71
fi
rm -- "$STORY_PREFLIGHT_TMP/dangling-residue-synthetic"
STORY_STATIC_ALLOWED_MANIFEST="$(printf '%s\n' \
  "$STORY_EVIDENCE_ARTIFACT" \
  'src/hooks/__tests__/useStorageAnalytics.test.ts' \
  'src/hooks/useImportStatus.ts' \
  'src/lib/api/__tests__/storage-analytics.test.ts' \
  'src/lib/api/__tests__/storage-import-normalizer.test.ts' \
  'src/lib/api/storage-analytics.ts' \
  'src/lib/api/storage-import-normalizer.ts' \
  'src/types/storage-analytics-trends.ts' \
  'src/types/storage-analytics/index.ts' | sort)"
BACKEND_CONTRACT_PATHS="$(printf '%s\n' \
  'docs/API-PATHS-REFERENCE.md' \
  'src/imports/controllers/__tests__/imports-paid-storage-status.controller.spec.ts' \
  'src/imports/controllers/__tests__/paid-storage-import.controller.spec.ts' \
  'src/imports/controllers/paid-storage-import.controller.ts' \
  'src/imports/dto/import-status.dto.ts' \
  'src/imports/dto/paid-storage-import.dto.ts' \
  'src/imports/imports.controller.ts' \
  'src/imports/services/import-status-builder.service.spec.ts' \
  'src/imports/services/import-status-builder.service.ts' \
  'test/imports/paid-storage.e2e-spec.ts' | sort)"
STORY_169_14_REQUIRED_MANIFEST="$(printf '%s\n' \
  'docs/API-PATHS-REFERENCE.md' \
  'src/imports/controllers/__tests__/imports-paid-storage-status.controller.spec.ts' \
  'src/imports/controllers/__tests__/paid-storage-import.controller.spec.ts' \
  'src/imports/controllers/paid-storage-import.controller.ts' \
  'src/imports/dto/import-status.dto.ts' \
  'src/imports/imports.controller.ts' \
  'src/imports/services/import-status-builder.service.spec.ts' \
  'src/imports/services/import-status-builder.service.ts' \
  'test/imports/paid-storage.e2e-spec.ts' | sort)"

normalize_pr_collision_paths() {
  jq -r '.[] | .[] | .filename, (.previous_filename // empty)' | \
    sed '/^$/d' | sort -u
}
count_pr_file_objects() {
  jq -er '[.[] | .[]] | length'
}
assert_local_rename_away_collision_detection() {
  local synthetic_repository="$STORY_PREFLIGHT_TMP/rename-away-repository"
  local synthetic_allowed_path="$STORY_EVIDENCE_ARTIFACT"
  local synthetic_destination_path='outside/renamed-story-artifact.md'
  local synthetic_base_sha synthetic_head_sha
  local unstaged_paths staged_paths divergent_paths synthetic_overlap
  mkdir -p \
    "$synthetic_repository/$(dirname "$synthetic_allowed_path")" \
    "$synthetic_repository/$(dirname "$synthetic_destination_path")"
  git -C "$synthetic_repository" init -q
  printf 'synthetic\n' > "$synthetic_repository/$synthetic_allowed_path"
  git -C "$synthetic_repository" add -- "$synthetic_allowed_path"
  git -C "$synthetic_repository" \
    -c user.name='Story 169.15 Synthetic' \
    -c user.email='story-169-15-synthetic@example.invalid' \
    commit -qm 'synthetic base'
  synthetic_base_sha="$(git -C "$synthetic_repository" rev-parse HEAD)"
  mv -- \
    "$synthetic_repository/$synthetic_allowed_path" \
    "$synthetic_repository/$synthetic_destination_path"
  if ! synthetic_unstaged_paths="$(git -C "$synthetic_repository" \
    diff --no-renames --name-only)"; then
    exit 71
  fi
  if ! synthetic_untracked_paths="$(git -C "$synthetic_repository" \
    ls-files --others --exclude-standard)"; then
    exit 71
  fi
  unstaged_paths="$(printf '%s\n%s\n' \
    "$synthetic_unstaged_paths" "$synthetic_untracked_paths" | \
    sed '/^$/d' | sort -u)"
  synthetic_overlap="$(comm -12 \
    <(printf '%s\n' "$unstaged_paths") \
    <(printf '%s\n' "$synthetic_allowed_path"))"
  test "$synthetic_overlap" = "$synthetic_allowed_path"
  git -C "$synthetic_repository" add -A
  if ! staged_paths="$(git -C "$synthetic_repository" diff --cached \
    --no-renames --name-only | sort -u)"; then
    exit 71
  fi
  synthetic_overlap="$(comm -12 \
    <(printf '%s\n' "$staged_paths") \
    <(printf '%s\n' "$synthetic_allowed_path"))"
  test "$synthetic_overlap" = "$synthetic_allowed_path"
  git -C "$synthetic_repository" \
    -c user.name='Story 169.15 Synthetic' \
    -c user.email='story-169-15-synthetic@example.invalid' \
    commit -qm 'synthetic rename away'
  synthetic_head_sha="$(git -C "$synthetic_repository" rev-parse HEAD)"
  if ! divergent_paths="$(git -C "$synthetic_repository" diff --no-renames \
    --name-only "$synthetic_base_sha...$synthetic_head_sha" | sort -u)"; then
    exit 71
  fi
  synthetic_overlap="$(comm -12 \
    <(printf '%s\n' "$divergent_paths") \
    <(printf '%s\n' "$synthetic_allowed_path"))"
  test "$synthetic_overlap" = "$synthetic_allowed_path"
}
assert_pr_rename_away_collision_detection() {
  local synthetic_allowed_path="$STORY_EVIDENCE_ARTIFACT"
  local synthetic_pr_json synthetic_count synthetic_paths synthetic_overlap
  synthetic_pr_json="[[{\"filename\":\"outside/renamed-story-artifact.md\",\"previous_filename\":\"$STORY_EVIDENCE_ARTIFACT\"}]]"
  synthetic_count="$(printf '%s\n' "$synthetic_pr_json" | count_pr_file_objects)"
  synthetic_paths="$(printf '%s\n' "$synthetic_pr_json" | \
    normalize_pr_collision_paths)"
  synthetic_overlap="$(comm -12 \
    <(printf '%s\n' "$synthetic_paths") \
    <(printf '%s\n' "$synthetic_allowed_path"))"
  test "$synthetic_count" = "1"
  test "$synthetic_overlap" = "$synthetic_allowed_path"
}
assert_ignored_dangling_allowed_path_collision() {
  local synthetic_repository="$STORY_PREFLIGHT_TMP/ignored-dangling-repository"
  local synthetic_path="$synthetic_repository/$STORY_EVIDENCE_ARTIFACT"
  local tracked_allowed_path ignored_untracked_paths detected_allowed_path=""
  mkdir -p "$synthetic_repository/$(dirname "$STORY_EVIDENCE_ARTIFACT")"
  git -C "$synthetic_repository" init -q
  printf '_bmad-output/\n' > "$synthetic_repository/.gitignore"
  git -C "$synthetic_repository" add -- .gitignore
  git -C "$synthetic_repository" \
    -c user.name='Story 169.15 Synthetic' \
    -c user.email='story-169-15-synthetic@example.invalid' \
    commit -qm 'synthetic ignore rule'
  ln -s "$synthetic_repository/missing-target" "$synthetic_path"
  if ! ignored_untracked_paths="$(git -C "$synthetic_repository" \
    ls-files --others --exclude-standard -- \
    "$STORY_EVIDENCE_ARTIFACT")"; then
    exit 71
  fi
  if ! tracked_allowed_path="$(git -C "$synthetic_repository" \
    ls-files -- "$STORY_EVIDENCE_ARTIFACT")"; then
    exit 71
  fi
  test -z "$ignored_untracked_paths"
  if test -z "$tracked_allowed_path" && \
    { test -e "$synthetic_path" || test -L "$synthetic_path"; }; then
    detected_allowed_path="$STORY_EVIDENCE_ARTIFACT"
  fi
  test "$detected_allowed_path" = "$STORY_EVIDENCE_ARTIFACT"
}
assert_local_rename_away_collision_detection
assert_pr_rename_away_collision_detection
assert_ignored_dangling_allowed_path_collision

if ! FRONTEND_TOPLEVEL="$(git -C "$FRONTEND_REPOSITORY" rev-parse --show-toplevel)"; then
  exit 71
fi
test "$FRONTEND_TOPLEVEL" = "$FRONTEND_REPOSITORY"
if ! FRONTEND_FETCH_URLS="$(git -C "$FRONTEND_REPOSITORY" remote get-url --all origin)"; then
  exit 71
fi
if ! FRONTEND_PUSH_URLS="$(git -C "$FRONTEND_REPOSITORY" remote get-url --push --all origin)"; then
  exit 71
fi
test "$(printf '%s\n' "$FRONTEND_FETCH_URLS" | awk 'END { print NR }')" = "1"
test "$(printf '%s\n' "$FRONTEND_PUSH_URLS" | awk 'END { print NR }')" = "1"
FRONTEND_FETCH_URL="$(printf '%s\n' "$FRONTEND_FETCH_URLS" | sed -n '1p')"
FRONTEND_PUSH_URL="$(printf '%s\n' "$FRONTEND_PUSH_URLS" | sed -n '1p')"
FRONTEND_FETCH_REPOSITORY="$(gh repo view "$FRONTEND_FETCH_URL" --json nameWithOwner --jq '.nameWithOwner')"
FRONTEND_PUSH_REPOSITORY="$(gh repo view "$FRONTEND_PUSH_URL" --json nameWithOwner --jq '.nameWithOwner')"
test "$FRONTEND_FETCH_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
test "$FRONTEND_PUSH_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
FRONTEND_GITHUB_REPOSITORY="$FRONTEND_FETCH_REPOSITORY"
git -C "$FRONTEND_REPOSITORY" fetch "$FRONTEND_FETCH_URL" \
  'refs/heads/main:refs/remotes/origin/main'
if ! FRONTEND_ORIGIN_MAIN_SHA="$(git -C "$FRONTEND_REPOSITORY" rev-parse origin/main)"; then
  exit 71
fi
git -C "$FRONTEND_REPOSITORY" cat-file -e "$STORY_169_14_HANDOFF_COMMIT_SHA^{commit}"
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
  "$STORY_169_14_HANDOFF_COMMIT_SHA" "$FRONTEND_ORIGIN_MAIN_SHA"
if ! STORY_169_14_HANDOFF_COMMIT_AND_PARENTS="$(git -C "$FRONTEND_REPOSITORY" rev-list \
  --parents -n 1 "$STORY_169_14_HANDOFF_COMMIT_SHA")"; then
  exit 71
fi
IFS=' ' read -r VERIFIED_STORY_169_14_HANDOFF_COMMIT_SHA \
  STORY_169_14_HANDOFF_PARENT_SHA STORY_169_14_HANDOFF_EXTRA_PARENT \
  <<< "$STORY_169_14_HANDOFF_COMMIT_AND_PARENTS"
test "$VERIFIED_STORY_169_14_HANDOFF_COMMIT_SHA" = \
  "$STORY_169_14_HANDOFF_COMMIT_SHA"
test -n "$STORY_169_14_HANDOFF_PARENT_SHA"
test -z "$STORY_169_14_HANDOFF_EXTRA_PARENT"
if ! STORY_169_14_HANDOFF_PATHS="$(git -C "$FRONTEND_REPOSITORY" diff-tree \
  --no-commit-id --no-renames --name-only -r \
  "$STORY_169_14_HANDOFF_PARENT_SHA" "$STORY_169_14_HANDOFF_COMMIT_SHA" | \
  LC_ALL=C sort -u)"; then
  exit 71
fi
test "$STORY_169_14_HANDOFF_PATHS" = "$STORY_169_14_HANDOFF_ARTIFACT"
STORY_169_14_HANDOFF_ARTIFACT_FILE="$STORY_PREFLIGHT_TMP/story-169-14-handoff-artifact"
if ! git -C "$FRONTEND_REPOSITORY" show \
  "$STORY_169_14_HANDOFF_COMMIT_SHA:$STORY_169_14_HANDOFF_ARTIFACT" > \
  "$STORY_169_14_HANDOFF_ARTIFACT_FILE"; then
  exit 71
fi
test -s "$STORY_169_14_HANDOFF_ARTIFACT_FILE"
if ! awk '
  BEGIN {
    expected[1] = "<!-- STORY_169_14_FINAL_HANDOFF_RECORD:START -->"
    expected[2] = "<!-- STORY_169_14_FINAL_HANDOFF_RECORD:END -->"
    expected[3] = "<!-- STORY_169_14_FINAL_FOREIGN_WIP_PATHS:START -->"
    expected[4] = "<!-- STORY_169_14_FINAL_FOREIGN_WIP_PATHS:END -->"
    next_expected = 1
    open_block = ""
  }
  /^<!-- STORY_169_14_FINAL_(HANDOFF_RECORD|FOREIGN_WIP_PATHS):(START|END) -->$/ {
    if ($0 != expected[next_expected]) exit 2
    if ($0 ~ /:START -->$/) {
      if (open_block != "") exit 2
      open_block = $0
    } else {
      if (open_block == "") exit 2
      open_block = ""
    }
    next_expected++
  }
  END { if (next_expected != 5 || open_block != "") exit 2 }
' "$STORY_169_14_HANDOFF_ARTIFACT_FILE"; then
  exit 71
fi
extract_story_169_14_handoff_payload() {
  HANDOFF_MARKER_START="$1"
  HANDOFF_MARKER_END="$2"
  HANDOFF_OUTPUT="$3"
  if ! awk -v marker_start="$HANDOFF_MARKER_START" \
    -v marker_end="$HANDOFF_MARKER_END" '
      $0 == marker_start { state = 1; next }
      $0 == marker_end { state = 2; next }
      state == 1 { print }
      END { if (state != 2) exit 2 }
    ' "$STORY_169_14_HANDOFF_ARTIFACT_FILE" > "$HANDOFF_OUTPUT"; then
    exit 71
  fi
  test -s "$HANDOFF_OUTPUT"
}
STORY_169_14_HANDOFF_RECORD_FILE="$STORY_PREFLIGHT_TMP/story-169-14-handoff-record"
STORY_169_14_HANDOFF_WIP_FILE="$STORY_PREFLIGHT_TMP/story-169-14-handoff-wip"
extract_story_169_14_handoff_payload \
  '<!-- STORY_169_14_FINAL_HANDOFF_RECORD:START -->' \
  '<!-- STORY_169_14_FINAL_HANDOFF_RECORD:END -->' \
  "$STORY_169_14_HANDOFF_RECORD_FILE"
extract_story_169_14_handoff_payload \
  '<!-- STORY_169_14_FINAL_FOREIGN_WIP_PATHS:START -->' \
  '<!-- STORY_169_14_FINAL_FOREIGN_WIP_PATHS:END -->' \
  "$STORY_169_14_HANDOFF_WIP_FILE"
if ! STORY_169_14_HANDOFF_VALUES="$(awk -F= '
  BEGIN {
    order[1] = "STORY_169_14_FINAL_HANDOFF_RECORD_VERSION"
    order[2] = "STORY_169_14_BACKEND_REPOSITORY"
    order[3] = "STORY_169_14_BACKEND_BASE_SHA"
    order[4] = "STORY_169_14_EVIDENCE_COMMIT_SHA"
    order[5] = "STORY_169_14_FEATURE_SHA"
    order[6] = "STORY_169_14_FEATURE_TREE_SHA"
    order[7] = "STORY_169_14_FROZEN_MANIFEST_SHA256"
    order[8] = "STORY_169_14_REVIEW_PASS_1_ID"
    order[9] = "STORY_169_14_REVIEW_PASS_1_DISPOSITION"
    order[10] = "STORY_169_14_REVIEW_PASS_2_ID"
    order[11] = "STORY_169_14_REVIEW_PASS_2_DISPOSITION"
    order[12] = "STORY_169_14_PR_NUMBER"
    order[13] = "STORY_169_14_PR_URL"
    order[14] = "STORY_169_14_MERGE_SHA"
    order[15] = "STORY_169_14_BACKEND_MAIN_MODE"
    order[16] = "STORY_169_14_FOREIGN_WIP_PATHS_SHA256"
    order[17] = "STORY_169_14_BRANCH_WORKTREE_CLEANUP_DISPOSITION"
    order[18] = "STORY_169_14_FRONTEND_REPOSITORY"
    order[19] = "STORY_169_14_FINAL_HANDOFF_FRONTEND_BASE_SHA"
    order[20] = "STORY_169_14_FINAL_HANDOFF_ARTIFACT"
    order[21] = "STORY_169_14_FINAL_HANDOFF_PR_BASE_BRANCH"
    order[22] = "STORY_169_14_FINAL_HANDOFF_PR_HEAD_BRANCH"
    order[23] = "STORY_169_14_FINAL_HANDOFF_REVIEW_PASS_1_ID"
    order[24] = "STORY_169_14_FINAL_HANDOFF_REVIEW_PASS_1_DISPOSITION"
    order[25] = "STORY_169_14_FINAL_HANDOFF_REVIEW_PASS_2_ID"
    order[26] = "STORY_169_14_FINAL_HANDOFF_REVIEW_PASS_2_DISPOSITION"
    order[27] = "STORY_169_14_BACKEND_REVIEWED_HEAD_RECORD_SHA256"
    order[28] = "STORY_169_14_BACKEND_PR_RECORD_SHA256"
    order[29] = "STORY_169_14_BACKEND_CLEANUP_AUTHORITY_RECORD_SHA256"
    order[30] = "STORY_169_14_EPHEMERAL_RECORDS_DISPOSITION"
  }
  {
    if (NR > 30 || $1 != order[NR]) exit 2
    value = substr($0, length($1) + 2)
    if (value == "") exit 2
    values[NR] = value
  }
  END {
    if (NR != 30) exit 2
    output = values[1]
    for (line = 2; line <= 30; line++) output = output "|" values[line]
    print output
  }
' "$STORY_169_14_HANDOFF_RECORD_FILE")"; then
  exit 71
fi
IFS='|' read -r STORY_169_14_HANDOFF_VERSION \
  STORY_169_14_HANDOFF_BACKEND_REPOSITORY RECORDED_STORY_169_14_BACKEND_BASE_SHA \
  RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA RECORDED_STORY_169_14_FEATURE_SHA \
  RECORDED_STORY_169_14_FEATURE_TREE_SHA RECORDED_STORY_169_14_MANIFEST_SHA256 \
  RECORDED_STORY_169_14_REVIEW_PASS_1_ID \
  RECORDED_STORY_169_14_REVIEW_PASS_1_DISPOSITION \
  RECORDED_STORY_169_14_REVIEW_PASS_2_ID \
  RECORDED_STORY_169_14_REVIEW_PASS_2_DISPOSITION \
  RECORDED_STORY_169_14_PR_NUMBER RECORDED_STORY_169_14_PR_URL \
  RECORDED_STORY_169_14_MERGE_SHA BACKEND_MAIN_MODE \
  RECORDED_STORY_169_14_WIP_SHA256 STORY_169_14_BRANCH_WORKTREE_DISPOSITION \
  STORY_169_14_HANDOFF_FRONTEND_REPOSITORY \
  RECORDED_STORY_169_14_HANDOFF_FRONTEND_BASE_SHA \
  STORY_169_14_HANDOFF_ARTIFACT_PATH STORY_169_14_HANDOFF_PR_BASE_BRANCH \
  STORY_169_14_HANDOFF_PR_HEAD_BRANCH \
  RECORDED_STORY_169_14_HANDOFF_REVIEW_PASS_1_ID \
  RECORDED_STORY_169_14_HANDOFF_REVIEW_PASS_1_DISPOSITION \
  RECORDED_STORY_169_14_HANDOFF_REVIEW_PASS_2_ID \
  RECORDED_STORY_169_14_HANDOFF_REVIEW_PASS_2_DISPOSITION \
  RECORDED_STORY_169_14_BACKEND_REVIEWED_HEAD_RECORD_SHA256 \
  RECORDED_STORY_169_14_BACKEND_PR_RECORD_SHA256 \
  RECORDED_STORY_169_14_BACKEND_CLEANUP_AUTHORITY_RECORD_SHA256 \
  STORY_169_14_EPHEMERAL_RECORDS_DISPOSITION \
  <<< "$STORY_169_14_HANDOFF_VALUES"
test "$STORY_169_14_HANDOFF_VERSION" = "3"
test "$STORY_169_14_HANDOFF_BACKEND_REPOSITORY" = \
  "$EXPECTED_BACKEND_GITHUB_REPOSITORY"
test "$RECORDED_STORY_169_14_REVIEW_PASS_1_DISPOSITION" = "PASS"
test "$RECORDED_STORY_169_14_REVIEW_PASS_2_DISPOSITION" = "PASS"
test "$RECORDED_STORY_169_14_REVIEW_PASS_1_ID" != \
  "$RECORDED_STORY_169_14_REVIEW_PASS_2_ID"
test "$STORY_169_14_BRANCH_WORKTREE_DISPOSITION" = "PASS"
test "$STORY_169_14_HANDOFF_FRONTEND_REPOSITORY" = \
  "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
test "$RECORDED_STORY_169_14_HANDOFF_FRONTEND_BASE_SHA" = \
  "$STORY_169_14_HANDOFF_PARENT_SHA"
test "$STORY_169_14_HANDOFF_ARTIFACT_PATH" = \
  "$STORY_169_14_HANDOFF_ARTIFACT"
test "$STORY_169_14_HANDOFF_PR_BASE_BRANCH" = "main"
test "$STORY_169_14_HANDOFF_PR_HEAD_BRANCH" = \
  "$STORY_169_14_FINAL_HANDOFF_BRANCH"
test "$RECORDED_STORY_169_14_HANDOFF_REVIEW_PASS_1_DISPOSITION" = "PASS"
test "$RECORDED_STORY_169_14_HANDOFF_REVIEW_PASS_2_DISPOSITION" = "PASS"
test "$RECORDED_STORY_169_14_HANDOFF_REVIEW_PASS_1_ID" != \
  "$RECORDED_STORY_169_14_HANDOFF_REVIEW_PASS_2_ID"
for STORY_169_14_RECORD_SHA256 in \
  "$RECORDED_STORY_169_14_BACKEND_REVIEWED_HEAD_RECORD_SHA256" \
  "$RECORDED_STORY_169_14_BACKEND_PR_RECORD_SHA256" \
  "$RECORDED_STORY_169_14_BACKEND_CLEANUP_AUTHORITY_RECORD_SHA256"; do
  test "${#STORY_169_14_RECORD_SHA256}" -eq 64
  case "$STORY_169_14_RECORD_SHA256" in
    *[!0-9a-f]* | '') exit 64 ;;
  esac
done
write_story_169_14_cleanup_authority_reconstruction() {
  CLEANUP_AUTHORITY_OUTPUT="$1"
  CLEANUP_AUTHORITY_VERSION="$2"
  CLEANUP_AUTHORITY_REPOSITORY="$3"
  CLEANUP_AUTHORITY_BRANCH="$4"
  CLEANUP_AUTHORITY_WORKTREE="$5"
  CLEANUP_AUTHORITY_REVIEWED_HEAD_SHA256="$6"
  CLEANUP_AUTHORITY_PR_SHA256="$7"
  CLEANUP_AUTHORITY_MAIN_MODE="$8"
  CLEANUP_AUTHORITY_WIP_SHA256="$9"
  CLEANUP_AUTHORITY_DELETION_AUTHORIZATION="${10}"
  printf '%s\n' \
    "STORY_169_14_BRANCH_WORKTREE_CLEANUP_AUTHORITY_VERSION=$CLEANUP_AUTHORITY_VERSION" \
    "STORY_169_14_BACKEND_REPOSITORY=$CLEANUP_AUTHORITY_REPOSITORY" \
    "STORY_169_14_BACKEND_BRANCH=$CLEANUP_AUTHORITY_BRANCH" \
    "STORY_169_14_BACKEND_WORKTREE=$CLEANUP_AUTHORITY_WORKTREE" \
    "STORY_169_14_BACKEND_REVIEWED_HEAD_RECORD_SHA256=$CLEANUP_AUTHORITY_REVIEWED_HEAD_SHA256" \
    "STORY_169_14_BACKEND_PR_RECORD_SHA256=$CLEANUP_AUTHORITY_PR_SHA256" \
    "STORY_169_14_BACKEND_MAIN_MODE=$CLEANUP_AUTHORITY_MAIN_MODE" \
    "STORY_169_14_FOREIGN_WIP_PATHS_SHA256=$CLEANUP_AUTHORITY_WIP_SHA256" \
    "STORY_169_14_BRANCH_WORKTREE_DELETION_AUTHORIZATION=$CLEANUP_AUTHORITY_DELETION_AUTHORIZATION" \
    > "$CLEANUP_AUTHORITY_OUTPUT"
  test "$(awk 'END { print NR }' "$CLEANUP_AUTHORITY_OUTPUT")" = "9"
}
assert_story_169_14_cleanup_authority_reconstruction() {
  EXPECTED_CLEANUP_AUTHORITY_SHA256="$1"
  shift
  RECONSTRUCTED_CLEANUP_AUTHORITY_FILE="$1"
  write_story_169_14_cleanup_authority_reconstruction "$@"
  test "$(shasum -a 256 "$RECONSTRUCTED_CLEANUP_AUTHORITY_FILE" | \
    awk '{ print $1 }')" = "$EXPECTED_CLEANUP_AUTHORITY_SHA256"
}
SYNTHETIC_CLEANUP_AUTHORITY_FILE="$STORY_PREFLIGHT_TMP/synthetic-cleanup-authority"
SYNTHETIC_REVIEWED_HEAD_SHA256="$(printf 'a%.0s' {1..64})"
SYNTHETIC_PR_SHA256="$(printf 'b%.0s' {1..64})"
SYNTHETIC_WIP_SHA256="$(printf 'c%.0s' {1..64})"
write_story_169_14_cleanup_authority_reconstruction \
  "$SYNTHETIC_CLEANUP_AUTHORITY_FILE" \
  "1" \
  "$EXPECTED_BACKEND_GITHUB_REPOSITORY" \
  "$STORY_169_14_BRANCH" \
  "$STORY_169_14_WORKTREE" \
  "$SYNTHETIC_REVIEWED_HEAD_SHA256" \
  "$SYNTHETIC_PR_SHA256" \
  "clean-local-main" \
  "$SYNTHETIC_WIP_SHA256" \
  "PASS"
SYNTHETIC_CLEANUP_AUTHORITY_SHA256="$(shasum -a 256 \
  "$SYNTHETIC_CLEANUP_AUTHORITY_FILE" | awk '{ print $1 }')"
assert_story_169_14_cleanup_authority_reconstruction \
  "$SYNTHETIC_CLEANUP_AUTHORITY_SHA256" \
  "$SYNTHETIC_CLEANUP_AUTHORITY_FILE" \
  "1" \
  "$EXPECTED_BACKEND_GITHUB_REPOSITORY" \
  "$STORY_169_14_BRANCH" \
  "$STORY_169_14_WORKTREE" \
  "$SYNTHETIC_REVIEWED_HEAD_SHA256" \
  "$SYNTHETIC_PR_SHA256" \
  "clean-local-main" \
  "$SYNTHETIC_WIP_SHA256" \
  "PASS"
for MUTATED_CLEANUP_AUTHORITY_FIELD in \
  version repository branch worktree reviewed-head pr mode wip authorization; do
  MUTATED_CLEANUP_AUTHORITY_VERSION="1"
  MUTATED_CLEANUP_AUTHORITY_REPOSITORY="$EXPECTED_BACKEND_GITHUB_REPOSITORY"
  MUTATED_CLEANUP_AUTHORITY_BRANCH="$STORY_169_14_BRANCH"
  MUTATED_CLEANUP_AUTHORITY_WORKTREE="$STORY_169_14_WORKTREE"
  MUTATED_CLEANUP_AUTHORITY_REVIEWED_HEAD_SHA256="$SYNTHETIC_REVIEWED_HEAD_SHA256"
  MUTATED_CLEANUP_AUTHORITY_PR_SHA256="$SYNTHETIC_PR_SHA256"
  MUTATED_CLEANUP_AUTHORITY_MAIN_MODE="clean-local-main"
  MUTATED_CLEANUP_AUTHORITY_WIP_SHA256="$SYNTHETIC_WIP_SHA256"
  MUTATED_CLEANUP_AUTHORITY_AUTHORIZATION="PASS"
  case "$MUTATED_CLEANUP_AUTHORITY_FIELD" in
    version) MUTATED_CLEANUP_AUTHORITY_VERSION="2" ;;
    repository) MUTATED_CLEANUP_AUTHORITY_REPOSITORY="example.invalid/foreign" ;;
    branch) MUTATED_CLEANUP_AUTHORITY_BRANCH="foreign-branch" ;;
    worktree) MUTATED_CLEANUP_AUTHORITY_WORKTREE="/tmp/foreign-worktree" ;;
    reviewed-head)
      MUTATED_CLEANUP_AUTHORITY_REVIEWED_HEAD_SHA256="$(printf 'd%.0s' {1..64})"
      ;;
    pr) MUTATED_CLEANUP_AUTHORITY_PR_SHA256="$(printf 'e%.0s' {1..64})" ;;
    mode) MUTATED_CLEANUP_AUTHORITY_MAIN_MODE="recorded-origin-deferral" ;;
    wip) MUTATED_CLEANUP_AUTHORITY_WIP_SHA256="$(printf 'f%.0s' {1..64})" ;;
    authorization) MUTATED_CLEANUP_AUTHORITY_AUTHORIZATION="FAIL" ;;
    *) exit 64 ;;
  esac
  if assert_story_169_14_cleanup_authority_reconstruction \
    "$SYNTHETIC_CLEANUP_AUTHORITY_SHA256" \
    "$SYNTHETIC_CLEANUP_AUTHORITY_FILE" \
    "$MUTATED_CLEANUP_AUTHORITY_VERSION" \
    "$MUTATED_CLEANUP_AUTHORITY_REPOSITORY" \
    "$MUTATED_CLEANUP_AUTHORITY_BRANCH" \
    "$MUTATED_CLEANUP_AUTHORITY_WORKTREE" \
    "$MUTATED_CLEANUP_AUTHORITY_REVIEWED_HEAD_SHA256" \
    "$MUTATED_CLEANUP_AUTHORITY_PR_SHA256" \
    "$MUTATED_CLEANUP_AUTHORITY_MAIN_MODE" \
    "$MUTATED_CLEANUP_AUTHORITY_WIP_SHA256" \
    "$MUTATED_CLEANUP_AUTHORITY_AUTHORIZATION"; then
    exit 71
  fi
done
MUTATED_CLEANUP_AUTHORITY_SHA256="${SYNTHETIC_CLEANUP_AUTHORITY_SHA256%?}0"
if test "$MUTATED_CLEANUP_AUTHORITY_SHA256" = \
  "$SYNTHETIC_CLEANUP_AUTHORITY_SHA256"; then
  MUTATED_CLEANUP_AUTHORITY_SHA256="${SYNTHETIC_CLEANUP_AUTHORITY_SHA256%?}1"
fi
if assert_story_169_14_cleanup_authority_reconstruction \
  "$MUTATED_CLEANUP_AUTHORITY_SHA256" \
  "$SYNTHETIC_CLEANUP_AUTHORITY_FILE" \
  "1" \
  "$EXPECTED_BACKEND_GITHUB_REPOSITORY" \
  "$STORY_169_14_BRANCH" \
  "$STORY_169_14_WORKTREE" \
  "$SYNTHETIC_REVIEWED_HEAD_SHA256" \
  "$SYNTHETIC_PR_SHA256" \
  "clean-local-main" \
  "$SYNTHETIC_WIP_SHA256" \
  "PASS"; then
  exit 71
fi
assert_story_169_14_cleanup_authority_reconstruction \
  "$RECORDED_STORY_169_14_BACKEND_CLEANUP_AUTHORITY_RECORD_SHA256" \
  "$STORY_PREFLIGHT_TMP/reconstructed-story-169-14-cleanup-authority" \
  "1" \
  "$EXPECTED_BACKEND_GITHUB_REPOSITORY" \
  "$STORY_169_14_BRANCH" \
  "$STORY_169_14_WORKTREE" \
  "$RECORDED_STORY_169_14_BACKEND_REVIEWED_HEAD_RECORD_SHA256" \
  "$RECORDED_STORY_169_14_BACKEND_PR_RECORD_SHA256" \
  "$BACKEND_MAIN_MODE" \
  "$RECORDED_STORY_169_14_WIP_SHA256" \
  "PASS"
test "$STORY_169_14_EPHEMERAL_RECORDS_DISPOSITION" = \
  "RETAINED_UNTIL_CROSS_BOUND_VERIFIED"
assert_story_169_14_handoff_wip_semantics() {
  ASSERTED_HANDOFF_MAIN_MODE="$1"
  ASSERTED_HANDOFF_WIP_FILE="$2"
  ASSERTED_HANDOFF_WIP_SHA256="$3"
  ASSERTED_HANDOFF_CANONICAL_OUTPUT="$4"
  test -s "$ASSERTED_HANDOFF_WIP_FILE" || return 64
  if ! COMPUTED_ASSERTED_HANDOFF_WIP_SHA256="$(shasum -a 256 \
    "$ASSERTED_HANDOFF_WIP_FILE" | awk '{ print $1 }')"; then
    return 71
  fi
  test "$COMPUTED_ASSERTED_HANDOFF_WIP_SHA256" = \
    "$ASSERTED_HANDOFF_WIP_SHA256" || return 64
  case "$ASSERTED_HANDOFF_MAIN_MODE" in
    clean-local-main)
      cmp -s "$ASSERTED_HANDOFF_WIP_FILE" <(printf 'NONE\n') || return 64
      ;;
    recorded-origin-deferral)
      if awk 'NF == 0 { blank = 1 } END { exit blank ? 0 : 1 }' \
        "$ASSERTED_HANDOFF_WIP_FILE"; then
        return 64
      fi
      LC_ALL=C sort -u "$ASSERTED_HANDOFF_WIP_FILE" > \
        "$ASSERTED_HANDOFF_CANONICAL_OUTPUT" || return 71
      cmp -s "$ASSERTED_HANDOFF_WIP_FILE" \
        "$ASSERTED_HANDOFF_CANONICAL_OUTPUT" || return 64
      ;;
    *) return 64 ;;
  esac
}

printf 'NONE\n' > "$STORY_PREFLIGHT_TMP/wip-semantic-none"
SYNTHETIC_NONE_SHA256="$(shasum -a 256 \
  "$STORY_PREFLIGHT_TMP/wip-semantic-none" | awk '{ print $1 }')"
assert_story_169_14_handoff_wip_semantics clean-local-main \
  "$STORY_PREFLIGHT_TMP/wip-semantic-none" "$SYNTHETIC_NONE_SHA256" \
  "$STORY_PREFLIGHT_TMP/wip-semantic-none-canonical"
printf 'arbitrary\n' > "$STORY_PREFLIGHT_TMP/wip-semantic-arbitrary"
SYNTHETIC_ARBITRARY_SHA256="$(shasum -a 256 \
  "$STORY_PREFLIGHT_TMP/wip-semantic-arbitrary" | awk '{ print $1 }')"
if assert_story_169_14_handoff_wip_semantics clean-local-main \
  "$STORY_PREFLIGHT_TMP/wip-semantic-arbitrary" \
  "$SYNTHETIC_ARBITRARY_SHA256" \
  "$STORY_PREFLIGHT_TMP/wip-semantic-arbitrary-canonical"; then
  exit 71
fi
: > "$STORY_PREFLIGHT_TMP/wip-semantic-empty"
SYNTHETIC_EMPTY_SHA256="$(shasum -a 256 \
  "$STORY_PREFLIGHT_TMP/wip-semantic-empty" | awk '{ print $1 }')"
if assert_story_169_14_handoff_wip_semantics recorded-origin-deferral \
  "$STORY_PREFLIGHT_TMP/wip-semantic-empty" "$SYNTHETIC_EMPTY_SHA256" \
  "$STORY_PREFLIGHT_TMP/wip-semantic-empty-canonical"; then
  exit 71
fi
printf 'alpha\n \nbeta\n' > "$STORY_PREFLIGHT_TMP/wip-semantic-blank"
SYNTHETIC_BLANK_SHA256="$(shasum -a 256 \
  "$STORY_PREFLIGHT_TMP/wip-semantic-blank" | awk '{ print $1 }')"
if assert_story_169_14_handoff_wip_semantics recorded-origin-deferral \
  "$STORY_PREFLIGHT_TMP/wip-semantic-blank" "$SYNTHETIC_BLANK_SHA256" \
  "$STORY_PREFLIGHT_TMP/wip-semantic-blank-canonical"; then
  exit 71
fi
printf 'beta\nalpha\n' > "$STORY_PREFLIGHT_TMP/wip-semantic-unsorted"
SYNTHETIC_UNSORTED_SHA256="$(shasum -a 256 \
  "$STORY_PREFLIGHT_TMP/wip-semantic-unsorted" | awk '{ print $1 }')"
if assert_story_169_14_handoff_wip_semantics recorded-origin-deferral \
  "$STORY_PREFLIGHT_TMP/wip-semantic-unsorted" "$SYNTHETIC_UNSORTED_SHA256" \
  "$STORY_PREFLIGHT_TMP/wip-semantic-unsorted-canonical"; then
  exit 71
fi
printf 'alpha\nalpha\n' > "$STORY_PREFLIGHT_TMP/wip-semantic-duplicate"
SYNTHETIC_DUPLICATE_SHA256="$(shasum -a 256 \
  "$STORY_PREFLIGHT_TMP/wip-semantic-duplicate" | awk '{ print $1 }')"
if assert_story_169_14_handoff_wip_semantics recorded-origin-deferral \
  "$STORY_PREFLIGHT_TMP/wip-semantic-duplicate" \
  "$SYNTHETIC_DUPLICATE_SHA256" \
  "$STORY_PREFLIGHT_TMP/wip-semantic-duplicate-canonical"; then
  exit 71
fi
printf 'alpha\nbeta\n' > "$STORY_PREFLIGHT_TMP/wip-semantic-canonical"
SYNTHETIC_CANONICAL_SHA256="$(shasum -a 256 \
  "$STORY_PREFLIGHT_TMP/wip-semantic-canonical" | awk '{ print $1 }')"
assert_story_169_14_handoff_wip_semantics recorded-origin-deferral \
  "$STORY_PREFLIGHT_TMP/wip-semantic-canonical" \
  "$SYNTHETIC_CANONICAL_SHA256" \
  "$STORY_PREFLIGHT_TMP/wip-semantic-canonical-output"

assert_story_169_14_handoff_wip_semantics "$BACKEND_MAIN_MODE" \
  "$STORY_169_14_HANDOFF_WIP_FILE" \
  "$RECORDED_STORY_169_14_WIP_SHA256" \
  "$STORY_PREFLIGHT_TMP/canonical-story-169-14-handoff-wip"
case "$BACKEND_MAIN_MODE" in
  clean-local-main) RECORDED_FOREIGN_WIP_PATHS="" ;;
  recorded-origin-deferral)
    RECORDED_FOREIGN_WIP_PATHS="$(cat "$STORY_169_14_HANDOFF_WIP_FILE")"
    test -n "$RECORDED_FOREIGN_WIP_PATHS"
    ;;
  *) exit 64 ;;
esac

if ! STORY_169_14_HANDOFF_PR_CANDIDATES="$(gh pr list \
  --repo "$EXPECTED_FRONTEND_GITHUB_REPOSITORY" \
  --state all \
  --base "$STORY_169_14_HANDOFF_PR_BASE_BRANCH" \
  --head "$STORY_169_14_HANDOFF_PR_HEAD_BRANCH" \
  --limit 100 \
  --json number,state,baseRefName,headRefName,headRefOid,url,mergeCommit)"; then
  exit 71
fi
test "$(printf '%s\n' "$STORY_169_14_HANDOFF_PR_CANDIDATES" | jq 'length')" -lt 100
if ! STORY_169_14_HANDOFF_PR_MATCHES="$(printf '%s\n' \
  "$STORY_169_14_HANDOFF_PR_CANDIDATES" | jq -c \
  --arg base "$STORY_169_14_HANDOFF_PR_BASE_BRANCH" \
  --arg head "$STORY_169_14_HANDOFF_PR_HEAD_BRANCH" \
  --arg sha "$STORY_169_14_HANDOFF_COMMIT_SHA" '
    [.[] | select(
      .baseRefName == $base and
      .headRefName == $head and
      .headRefOid == $sha
    )]
  ')"; then
  exit 71
fi
test "$(printf '%s\n' "$STORY_169_14_HANDOFF_PR_MATCHES" | jq 'length')" = "1"
if ! STORY_169_14_HANDOFF_PR_VALUES="$(printf '%s\n' \
  "$STORY_169_14_HANDOFF_PR_MATCHES" | jq -r '
    .[0] |
    [.number, .state, .baseRefName, .headRefName, .headRefOid, .url,
      (.mergeCommit.oid // "")] | @tsv
  ')"; then
  exit 71
fi
IFS=$'\t' read -r STORY_169_14_HANDOFF_PR_NUMBER \
  STORY_169_14_HANDOFF_PR_STATE VERIFIED_STORY_169_14_HANDOFF_PR_BASE \
  VERIFIED_STORY_169_14_HANDOFF_PR_HEAD STORY_169_14_HANDOFF_PR_HEAD_SHA \
  STORY_169_14_HANDOFF_PR_URL STORY_169_14_HANDOFF_MERGE_SHA \
  <<< "$STORY_169_14_HANDOFF_PR_VALUES"
case "$STORY_169_14_HANDOFF_PR_NUMBER" in
  *[!0-9]* | '') exit 64 ;;
esac
test "$STORY_169_14_HANDOFF_PR_STATE" = "MERGED"
test "$VERIFIED_STORY_169_14_HANDOFF_PR_BASE" = "main"
test "$VERIFIED_STORY_169_14_HANDOFF_PR_HEAD" = \
  "$STORY_169_14_HANDOFF_PR_HEAD_BRANCH"
test "$STORY_169_14_HANDOFF_PR_HEAD_SHA" = "$STORY_169_14_HANDOFF_COMMIT_SHA"
test "$STORY_169_14_HANDOFF_PR_URL" = \
  "https://github.com/$EXPECTED_FRONTEND_GITHUB_REPOSITORY/pull/$STORY_169_14_HANDOFF_PR_NUMBER"
test "${#STORY_169_14_HANDOFF_MERGE_SHA}" -eq 40
case "$STORY_169_14_HANDOFF_MERGE_SHA" in
  *[!0-9a-f]* | '') exit 64 ;;
esac
git -C "$FRONTEND_REPOSITORY" cat-file -e \
  "$STORY_169_14_HANDOFF_MERGE_SHA^{commit}"
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
  "$STORY_169_14_HANDOFF_COMMIT_SHA" "$STORY_169_14_HANDOFF_MERGE_SHA"
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
  "$STORY_169_14_HANDOFF_MERGE_SHA" "$FRONTEND_ORIGIN_MAIN_SHA"
if ! STORY_169_14_HANDOFF_REMOTE_REF="$(git -C "$FRONTEND_REPOSITORY" \
  ls-remote --refs "$FRONTEND_PUSH_URL" \
  "refs/heads/$STORY_169_14_HANDOFF_PR_HEAD_BRANCH")"; then
  exit 71
fi
test -z "$STORY_169_14_HANDOFF_REMOTE_REF"

test "${#RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA}" -eq 40
case "$RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA" in
  *[!0-9a-f]* | '') exit 64 ;;
esac
git -C "$FRONTEND_REPOSITORY" cat-file -e \
  "$RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA^{commit}"
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
  "$RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA" \
  "$STORY_169_14_HANDOFF_PARENT_SHA"
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
  "$RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA" "$FRONTEND_ORIGIN_MAIN_SHA"
if ! STORY_169_14_EVIDENCE_COMMIT_AND_PARENTS="$(git -C "$FRONTEND_REPOSITORY" \
  rev-list --parents -n 1 "$RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA")"; then
  exit 71
fi
IFS=' ' read -r VERIFIED_STORY_169_14_EVIDENCE_COMMIT_SHA \
  STORY_169_14_EVIDENCE_PARENT_SHA STORY_169_14_EVIDENCE_EXTRA_PARENT \
  <<< "$STORY_169_14_EVIDENCE_COMMIT_AND_PARENTS"
test "$VERIFIED_STORY_169_14_EVIDENCE_COMMIT_SHA" = \
  "$RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA"
test -n "$STORY_169_14_EVIDENCE_PARENT_SHA"
test -z "$STORY_169_14_EVIDENCE_EXTRA_PARENT"
if ! STORY_169_14_EVIDENCE_PATHS="$(git -C "$FRONTEND_REPOSITORY" diff-tree \
  --no-commit-id --no-renames --name-only -r \
  "$STORY_169_14_EVIDENCE_PARENT_SHA" \
  "$RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA" | LC_ALL=C sort -u)"; then
  exit 71
fi
test "$STORY_169_14_EVIDENCE_PATHS" = "$STORY_169_14_HANDOFF_ARTIFACT"
STORY_169_14_EVIDENCE_ARTIFACT_FILE="$STORY_PREFLIGHT_TMP/story-169-14-evidence-artifact"
if ! git -C "$FRONTEND_REPOSITORY" show \
  "$RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA:$STORY_169_14_HANDOFF_ARTIFACT" > \
  "$STORY_169_14_EVIDENCE_ARTIFACT_FILE"; then
  exit 71
fi
test -s "$STORY_169_14_EVIDENCE_ARTIFACT_FILE"
if ! awk '
  BEGIN {
    expected[1] = "<!-- STORY_169_14_RETAINED_RED_OUTPUT:START -->"
    expected[2] = "<!-- STORY_169_14_RETAINED_RED_OUTPUT:END -->"
    expected[3] = "<!-- STORY_169_14_RETAINED_REVIEW_EVIDENCE:START -->"
    expected[4] = "<!-- STORY_169_14_RETAINED_REVIEW_EVIDENCE:END -->"
    expected[5] = "<!-- STORY_169_14_FROZEN_REVIEWED_MANIFEST:START -->"
    expected[6] = "<!-- STORY_169_14_FROZEN_REVIEWED_MANIFEST:END -->"
    next_expected = 1
    open_block = ""
  }
  /^<!-- STORY_169_14_(RETAINED_RED_OUTPUT|RETAINED_REVIEW_EVIDENCE|FROZEN_REVIEWED_MANIFEST):(START|END) -->$/ {
    if ($0 != expected[next_expected]) exit 2
    if ($0 ~ /:START -->$/) {
      if (open_block != "") exit 2
      open_block = $0
    } else {
      if (open_block == "") exit 2
      open_block = ""
    }
    next_expected++
  }
  END { if (next_expected != 7 || open_block != "") exit 2 }
' "$STORY_169_14_EVIDENCE_ARTIFACT_FILE"; then
  exit 71
fi
extract_story_169_14_evidence_payload() {
  EVIDENCE_MARKER_START="$1"
  EVIDENCE_MARKER_END="$2"
  EVIDENCE_OUTPUT="$3"
  if ! awk -v marker_start="$EVIDENCE_MARKER_START" \
    -v marker_end="$EVIDENCE_MARKER_END" '
      $0 == marker_start {
        if (state != 0 || start_count != 0) exit 2
        start_count = 1
        state = 1
        next
      }
      $0 == marker_end {
        if (state != 1 || end_count != 0) exit 2
        end_count = 1
        state = 2
        next
      }
      state == 1 { print }
      END {
        if (start_count != 1 || end_count != 1 || state != 2) exit 2
      }
    ' "$STORY_169_14_EVIDENCE_ARTIFACT_FILE" > "$EVIDENCE_OUTPUT"; then
    exit 71
  fi
  test -s "$EVIDENCE_OUTPUT"
}
STORY_169_14_RETAINED_RED_FILE="$STORY_PREFLIGHT_TMP/story-169-14-retained-red"
STORY_169_14_RETAINED_REVIEW_FILE="$STORY_PREFLIGHT_TMP/story-169-14-retained-review"
STORY_169_14_FROZEN_MANIFEST_FILE="$STORY_PREFLIGHT_TMP/story-169-14-frozen-manifest"
extract_story_169_14_evidence_payload \
  '<!-- STORY_169_14_RETAINED_RED_OUTPUT:START -->' \
  '<!-- STORY_169_14_RETAINED_RED_OUTPUT:END -->' \
  "$STORY_169_14_RETAINED_RED_FILE"
extract_story_169_14_evidence_payload \
  '<!-- STORY_169_14_RETAINED_REVIEW_EVIDENCE:START -->' \
  '<!-- STORY_169_14_RETAINED_REVIEW_EVIDENCE:END -->' \
  "$STORY_169_14_RETAINED_REVIEW_FILE"
extract_story_169_14_evidence_payload \
  '<!-- STORY_169_14_FROZEN_REVIEWED_MANIFEST:START -->' \
  '<!-- STORY_169_14_FROZEN_REVIEWED_MANIFEST:END -->' \
  "$STORY_169_14_FROZEN_MANIFEST_FILE"
if ! STORY_169_14_COMMITTED_EVIDENCE_VALUES="$(awk '
  index($0, "STORY_169_14_BACKEND_BASE_SHA: ") == 1 {
    base_count++; base = substr($0, 32)
  }
  index($0, "STORY_169_14_RED_OUTPUT_SHA256: ") == 1 {
    red_count++; red_hash = substr($0, 33)
  }
  index($0, "STORY_169_14_REVIEWED_MANIFEST_SHA256: ") == 1 {
    reviewed_count++; reviewed_hash = substr($0, 40)
  }
  index($0, "STORY_169_14_REVIEW_EVIDENCE_SHA256: ") == 1 {
    review_count++; review_hash = substr($0, 38)
  }
  index($0, "STORY_169_14_FROZEN_MANIFEST_SHA256: ") == 1 {
    frozen_count++; frozen_hash = substr($0, 38)
  }
  END {
    if (base_count != 1 || red_count != 1 || reviewed_count != 1 ||
        review_count != 1 || frozen_count != 1) exit 2
    print base "|" red_hash "|" reviewed_hash "|" review_hash "|" frozen_hash
  }
' "$STORY_169_14_EVIDENCE_ARTIFACT_FILE")"; then
  exit 71
fi
IFS='|' read -r STORY_169_14_EVIDENCE_BACKEND_BASE_SHA \
  STORY_169_14_EVIDENCE_RED_SHA256 STORY_169_14_EVIDENCE_REVIEWED_SHA256 \
  STORY_169_14_EVIDENCE_REVIEW_SHA256 STORY_169_14_EVIDENCE_FROZEN_SHA256 \
  <<< "$STORY_169_14_COMMITTED_EVIDENCE_VALUES"
test "$STORY_169_14_EVIDENCE_BACKEND_BASE_SHA" = \
  "$RECORDED_STORY_169_14_BACKEND_BASE_SHA"
test "$STORY_169_14_EVIDENCE_REVIEWED_SHA256" = \
  "$RECORDED_STORY_169_14_MANIFEST_SHA256"
test "$STORY_169_14_EVIDENCE_FROZEN_SHA256" = \
  "$RECORDED_STORY_169_14_MANIFEST_SHA256"
for STORY_169_14_EVIDENCE_SHA256 in \
  "$STORY_169_14_EVIDENCE_RED_SHA256" \
  "$STORY_169_14_EVIDENCE_REVIEWED_SHA256" \
  "$STORY_169_14_EVIDENCE_REVIEW_SHA256" \
  "$STORY_169_14_EVIDENCE_FROZEN_SHA256"; do
  test "${#STORY_169_14_EVIDENCE_SHA256}" -eq 64
  case "$STORY_169_14_EVIDENCE_SHA256" in
    *[!0-9a-f]* | '') exit 64 ;;
  esac
done
test "$(shasum -a 256 "$STORY_169_14_RETAINED_RED_FILE" | awk '{ print $1 }')" = \
  "$STORY_169_14_EVIDENCE_RED_SHA256"
test "$(shasum -a 256 "$STORY_169_14_RETAINED_REVIEW_FILE" | awk '{ print $1 }')" = \
  "$STORY_169_14_EVIDENCE_REVIEW_SHA256"
test "$(shasum -a 256 "$STORY_169_14_FROZEN_MANIFEST_FILE" | awk '{ print $1 }')" = \
  "$RECORDED_STORY_169_14_MANIFEST_SHA256"
STORY_169_14_SENSITIVE_EVIDENCE_PATTERN='(^|[^[:alnum:]_-])(authorization|proxy-authorization|cookie|set-cookie|x-api-key)[[:space:]]*:[[:space:]]*[^[:space:]]|(^|[^[:alnum:]_-])[[:alnum:]_-]*(authorization|proxy-authorization|cookie|set-cookie|x-api-key|password|passwd|secret|token|auth[_-]?token|access[_-]?token|refresh[_-]?token|client[_-]?secret|api[_-]?key|credentials?|aws[_-]?secret[_-]?access[_-]?key|aws[_-]?access[_-]?key[_-]?id)[[:space:]]*(=|:=|\+=|-=|\?=|&&=|\|\|=)[[:space:]]*[^[:space:]]|(^|[[:space:]])(bearer|basic)[[:space:]]+[[:graph:]]+|-----BEGIN [A-Z ]*PRIVATE KEY-----|[[:alpha:]][[:alnum:]+.-]*://[^[:space:]/:@]+:[^[:space:]@]+@'
if LC_ALL=C rg -q -i "$STORY_169_14_SENSITIVE_EVIDENCE_PATTERN" \
  "$STORY_169_14_RETAINED_RED_FILE" "$STORY_169_14_RETAINED_REVIEW_FILE"; then
  exit 72
else
  STORY_169_14_PRIVACY_RG_STATUS="$?"
fi
test "$STORY_169_14_PRIVACY_RG_STATUS" = "1"
if ! STORY_169_14_PRIVACY_ATTESTATION_COUNTS="$(awk '
  index($0, "STORY_169_14_PRIVACY_REVIEW_ATTESTATION:") == 1 {
    total++
    if ($0 == "STORY_169_14_PRIVACY_REVIEW_ATTESTATION: PASS_NO_SECRET_OR_CUSTOMER_PII") {
      exact++
    }
  }
  END { print (total + 0) "|" (exact + 0) }
' "$STORY_169_14_RETAINED_REVIEW_FILE")"; then
  exit 71
fi
test "$STORY_169_14_PRIVACY_ATTESTATION_COUNTS" = "1|1"
printf '%s\n' "$BACKEND_CONTRACT_PATHS" > \
  "$STORY_PREFLIGHT_TMP/story-169-14-allowed-manifest"
printf '%s\n' "$STORY_169_14_REQUIRED_MANIFEST" > \
  "$STORY_PREFLIGHT_TMP/story-169-14-required-manifest"
LC_ALL=C sort -u "$STORY_169_14_FROZEN_MANIFEST_FILE" > \
  "$STORY_PREFLIGHT_TMP/story-169-14-canonical-manifest"
cmp -s "$STORY_169_14_FROZEN_MANIFEST_FILE" \
  "$STORY_PREFLIGHT_TMP/story-169-14-canonical-manifest"
comm -23 "$STORY_169_14_FROZEN_MANIFEST_FILE" \
  "$STORY_PREFLIGHT_TMP/story-169-14-allowed-manifest" > \
  "$STORY_PREFLIGHT_TMP/story-169-14-outside-allowed"
test ! -s "$STORY_PREFLIGHT_TMP/story-169-14-outside-allowed"
comm -23 "$STORY_PREFLIGHT_TMP/story-169-14-required-manifest" \
  "$STORY_169_14_FROZEN_MANIFEST_FILE" > \
  "$STORY_PREFLIGHT_TMP/story-169-14-missing-required"
test ! -s "$STORY_PREFLIGHT_TMP/story-169-14-missing-required"

if ! BACKEND_TOPLEVEL="$(git -C "$BACKEND_REPOSITORY" rev-parse --show-toplevel)"; then
  exit 71
fi
test "$BACKEND_TOPLEVEL" = "$BACKEND_REPOSITORY"
if ! BACKEND_FETCH_URLS="$(git -C "$BACKEND_REPOSITORY" remote get-url --all origin)"; then
  exit 71
fi
if ! BACKEND_PUSH_URLS="$(git -C "$BACKEND_REPOSITORY" remote get-url --push --all origin)"; then
  exit 71
fi
test "$(printf '%s\n' "$BACKEND_FETCH_URLS" | awk 'END { print NR }')" = "1"
test "$(printf '%s\n' "$BACKEND_PUSH_URLS" | awk 'END { print NR }')" = "1"
BACKEND_FETCH_URL="$(printf '%s\n' "$BACKEND_FETCH_URLS" | sed -n '1p')"
BACKEND_PUSH_URL="$(printf '%s\n' "$BACKEND_PUSH_URLS" | sed -n '1p')"
BACKEND_FETCH_REPOSITORY="$(gh repo view "$BACKEND_FETCH_URL" --json nameWithOwner --jq '.nameWithOwner')"
BACKEND_PUSH_REPOSITORY="$(gh repo view "$BACKEND_PUSH_URL" --json nameWithOwner --jq '.nameWithOwner')"
test "$BACKEND_FETCH_REPOSITORY" = "$EXPECTED_BACKEND_GITHUB_REPOSITORY"
test "$BACKEND_PUSH_REPOSITORY" = "$EXPECTED_BACKEND_GITHUB_REPOSITORY"
git -C "$BACKEND_REPOSITORY" fetch "$BACKEND_FETCH_URL" \
  'refs/heads/main:refs/remotes/origin/main'
if ! BACKEND_LOCAL_MAIN_SHA="$(git -C "$BACKEND_REPOSITORY" rev-parse main)"; then
  exit 71
fi
if ! BACKEND_ORIGIN_MAIN_SHA="$(git -C "$BACKEND_REPOSITORY" rev-parse origin/main)"; then
  exit 71
fi
git -C "$BACKEND_REPOSITORY" merge-base --is-ancestor "$BACKEND_LOCAL_MAIN_SHA" "$BACKEND_ORIGIN_MAIN_SHA"
for RECORDED_STORY_169_14_SHA in \
  "$RECORDED_STORY_169_14_BACKEND_BASE_SHA" \
  "$RECORDED_STORY_169_14_EVIDENCE_COMMIT_SHA" \
  "$RECORDED_STORY_169_14_FEATURE_SHA" \
  "$RECORDED_STORY_169_14_FEATURE_TREE_SHA" \
  "$RECORDED_STORY_169_14_MERGE_SHA"; do
  test "${#RECORDED_STORY_169_14_SHA}" -eq 40
  case "$RECORDED_STORY_169_14_SHA" in
    *[!0-9a-f]* | '') exit 64 ;;
  esac
done
test "${#RECORDED_STORY_169_14_MANIFEST_SHA256}" -eq 64
case "$RECORDED_STORY_169_14_MANIFEST_SHA256" in
  *[!0-9a-f]* | '') exit 64 ;;
esac
git -C "$BACKEND_REPOSITORY" cat-file -e \
  "$RECORDED_STORY_169_14_BACKEND_BASE_SHA^{commit}"
git -C "$BACKEND_REPOSITORY" cat-file -e \
  "$RECORDED_STORY_169_14_FEATURE_SHA^{commit}"
if ! RECORDED_STORY_169_14_ACTUAL_FEATURE_TREE_SHA="$(git -C \
  "$BACKEND_REPOSITORY" rev-parse \
  "$RECORDED_STORY_169_14_FEATURE_SHA^{tree}")"; then
  exit 71
fi
test "$RECORDED_STORY_169_14_ACTUAL_FEATURE_TREE_SHA" = \
  "$RECORDED_STORY_169_14_FEATURE_TREE_SHA"
if ! STORY_169_14_FEATURE_AND_PARENTS="$(git -C "$BACKEND_REPOSITORY" rev-list \
  --parents -n 1 "$RECORDED_STORY_169_14_FEATURE_SHA")"; then
  exit 71
fi
IFS=' ' read -r VERIFIED_STORY_169_14_FEATURE_SHA \
  VERIFIED_STORY_169_14_FEATURE_PARENT STORY_169_14_FEATURE_EXTRA_PARENT \
  <<< "$STORY_169_14_FEATURE_AND_PARENTS"
test "$VERIFIED_STORY_169_14_FEATURE_SHA" = "$RECORDED_STORY_169_14_FEATURE_SHA"
test "$VERIFIED_STORY_169_14_FEATURE_PARENT" = \
  "$RECORDED_STORY_169_14_BACKEND_BASE_SHA"
test -z "$STORY_169_14_FEATURE_EXTRA_PARENT"
STORY_169_14_ACTUAL_FEATURE_MANIFEST_FILE="$STORY_PREFLIGHT_TMP/story-169-14-actual-feature-manifest"
if ! git -C "$BACKEND_REPOSITORY" diff --no-renames --name-only \
  "$RECORDED_STORY_169_14_BACKEND_BASE_SHA" \
  "$RECORDED_STORY_169_14_FEATURE_SHA" | LC_ALL=C sort -u > \
  "$STORY_169_14_ACTUAL_FEATURE_MANIFEST_FILE"; then
  exit 71
fi
test -s "$STORY_169_14_ACTUAL_FEATURE_MANIFEST_FILE"
cmp -s "$STORY_169_14_ACTUAL_FEATURE_MANIFEST_FILE" \
  "$STORY_169_14_FROZEN_MANIFEST_FILE"
test "$(shasum -a 256 "$STORY_169_14_ACTUAL_FEATURE_MANIFEST_FILE" | \
  awk '{ print $1 }')" = "$RECORDED_STORY_169_14_MANIFEST_SHA256"
case "$RECORDED_STORY_169_14_PR_NUMBER" in
  *[!0-9]* | '') exit 64 ;;
esac
test "$RECORDED_STORY_169_14_PR_URL" = \
  "https://github.com/$EXPECTED_BACKEND_GITHUB_REPOSITORY/pull/$RECORDED_STORY_169_14_PR_NUMBER"
if ! STORY_169_14_PR_JSON="$(gh pr view "$RECORDED_STORY_169_14_PR_NUMBER" \
  --repo "$EXPECTED_BACKEND_GITHUB_REPOSITORY" \
  --json number,state,baseRefName,headRefName,headRefOid,url,mergeCommit)"; then
  exit 71
fi
test "$(printf '%s\n' "$STORY_169_14_PR_JSON" | jq -r '.number')" = \
  "$RECORDED_STORY_169_14_PR_NUMBER"
test "$(printf '%s\n' "$STORY_169_14_PR_JSON" | jq -r '.state')" = "MERGED"
test "$(printf '%s\n' "$STORY_169_14_PR_JSON" | jq -r '.baseRefName')" = "main"
test "$(printf '%s\n' "$STORY_169_14_PR_JSON" | jq -r '.headRefName')" = \
  "$STORY_169_14_BRANCH"
test "$(printf '%s\n' "$STORY_169_14_PR_JSON" | jq -r '.headRefOid')" = \
  "$RECORDED_STORY_169_14_FEATURE_SHA"
test "$(printf '%s\n' "$STORY_169_14_PR_JSON" | jq -r '.url')" = \
  "$RECORDED_STORY_169_14_PR_URL"
test "$(printf '%s\n' "$STORY_169_14_PR_JSON" | jq -r '.mergeCommit.oid')" = \
  "$RECORDED_STORY_169_14_MERGE_SHA"
git -C "$BACKEND_REPOSITORY" merge-base --is-ancestor \
  "$RECORDED_STORY_169_14_FEATURE_SHA" "$RECORDED_STORY_169_14_MERGE_SHA"
git -C "$BACKEND_REPOSITORY" merge-base --is-ancestor "$RECORDED_STORY_169_14_MERGE_SHA" "$BACKEND_ORIGIN_MAIN_SHA"
if ! BACKEND_GIT_COMMON_DIR="$(git -C "$BACKEND_REPOSITORY" rev-parse \
  --path-format=absolute --git-common-dir)"; then
  exit 71
fi
assert_absent_non_symlink_residue \
  "$BACKEND_GIT_COMMON_DIR/story-169-14-reviewed-head-v1"
assert_absent_non_symlink_residue \
  "$BACKEND_GIT_COMMON_DIR/story-169-14-pr-record-v1"
assert_absent_non_symlink_residue \
  "$BACKEND_GIT_COMMON_DIR/story-169-14-branch-worktree-cleanup-authority-v1"
assert_absent_non_symlink_residue \
  "$BACKEND_GIT_COMMON_DIR/story-169-14-review-bootstrap-v1"
assert_absent_non_symlink_residue \
  "$BACKEND_GIT_COMMON_DIR/story-169-14-record-retirement-transaction-v1"
if ! BACKEND_UNSTAGED_PATHS="$(git -C "$BACKEND_REPOSITORY" diff --no-renames --name-only)"; then
  exit 71
fi
if ! BACKEND_STAGED_PATHS="$(git -C "$BACKEND_REPOSITORY" diff --cached --no-renames --name-only)"; then
  exit 71
fi
if ! BACKEND_UNTRACKED_PATHS="$(git -C "$BACKEND_REPOSITORY" ls-files --others --exclude-standard)"; then
  exit 71
fi
BACKEND_PRIMARY_WIP_PATHS="$(printf '%s\n%s\n%s\n' \
  "$BACKEND_UNSTAGED_PATHS" \
  "$BACKEND_STAGED_PATHS" \
  "$BACKEND_UNTRACKED_PATHS" | sed '/^$/d' | sort -u)"
if ! STORY_169_14_MERGED_PATHS="$(git -C "$BACKEND_REPOSITORY" diff --no-renames --name-only \
  "$RECORDED_STORY_169_14_MERGE_SHA^1" "$RECORDED_STORY_169_14_MERGE_SHA" | sort -u)"; then
  exit 71
fi
if ! BACKEND_WIP_MERGE_OVERLAP="$(comm -12 \
  <(printf '%s\n' "$BACKEND_PRIMARY_WIP_PATHS") \
  <(printf '%s\n' "$STORY_169_14_MERGED_PATHS"))"; then
  exit 71
fi
test -z "$BACKEND_WIP_MERGE_OVERLAP"
if ! BACKEND_WIP_CONTRACT_OVERLAP="$(comm -12 \
  <(printf '%s\n' "$BACKEND_PRIMARY_WIP_PATHS") \
  <(printf '%s\n' "$BACKEND_CONTRACT_PATHS"))"; then
  exit 71
fi
test -z "$BACKEND_WIP_CONTRACT_OVERLAP"

case "$BACKEND_MAIN_MODE" in
  clean-local-main)
    test -z "$BACKEND_PRIMARY_WIP_PATHS"
    git -C "$BACKEND_REPOSITORY" switch main
    git -C "$BACKEND_REPOSITORY" merge --ff-only origin/main
    if ! UPDATED_BACKEND_LOCAL_MAIN_SHA="$(git -C "$BACKEND_REPOSITORY" rev-parse main)"; then
      exit 71
    fi
    test "$UPDATED_BACKEND_LOCAL_MAIN_SHA" = "$BACKEND_ORIGIN_MAIN_SHA"
    git -C "$BACKEND_REPOSITORY" merge-base --is-ancestor "$RECORDED_STORY_169_14_MERGE_SHA" main
    ;;
  recorded-origin-deferral)
    if ! INCOMING_PATHS="$(git -C "$BACKEND_REPOSITORY" diff --no-renames --name-only "$BACKEND_LOCAL_MAIN_SHA..$BACKEND_ORIGIN_MAIN_SHA" | sort -u)"; then
      exit 71
    fi
    if ! CURRENT_FOREIGN_WIP_PATHS="$(comm -12 \
      <(printf '%s\n' "$BACKEND_PRIMARY_WIP_PATHS") \
      <(printf '%s\n' "$INCOMING_PATHS"))"; then
      exit 71
    fi
    test -n "$CURRENT_FOREIGN_WIP_PATHS"
    test "$CURRENT_FOREIGN_WIP_PATHS" = "$RECORDED_FOREIGN_WIP_PATHS"
    ;;
  *)
    exit 64
    ;;
esac

if ! BACKEND_STORY_LOCAL_BRANCH="$(git -C "$BACKEND_REPOSITORY" branch --list "$STORY_169_14_BRANCH")"; then
  exit 71
fi
if ! BACKEND_STORY_REMOTE_BRANCH="$(git -C "$BACKEND_REPOSITORY" ls-remote --heads "$BACKEND_PUSH_URL" "$STORY_169_14_BRANCH")"; then
  exit 71
fi
if ! BACKEND_WORKTREE_LIST="$(git -C "$BACKEND_REPOSITORY" worktree list --porcelain)"; then
  exit 71
fi
test -z "$BACKEND_STORY_LOCAL_BRANCH"
test -z "$BACKEND_STORY_REMOTE_BRANCH"
assert_absent_non_symlink_residue "$STORY_169_14_WORKTREE"
case "$BACKEND_WORKTREE_LIST" in
  *"$STORY_169_14_WORKTREE"*) exit 66 ;;
esac

BACKEND_CONTRACT_BLOB_EVIDENCE=""
while IFS= read -r BACKEND_CONTRACT_PATH; do
  if ! BACKEND_CONTRACT_CONTENT="$(git -C "$BACKEND_REPOSITORY" show "$BACKEND_ORIGIN_MAIN_SHA:$BACKEND_CONTRACT_PATH")"; then
    exit 71
  fi
  if ! BACKEND_CONTRACT_BLOB_SHA="$(git -C "$BACKEND_REPOSITORY" rev-parse "$BACKEND_ORIGIN_MAIN_SHA:$BACKEND_CONTRACT_PATH")"; then
    exit 71
  fi
  test -n "$BACKEND_CONTRACT_CONTENT"
  test -n "$BACKEND_CONTRACT_BLOB_SHA"
  BACKEND_CONTRACT_BLOB_EVIDENCE="${BACKEND_CONTRACT_BLOB_EVIDENCE}${BACKEND_CONTRACT_PATH} ${BACKEND_CONTRACT_BLOB_SHA}\n"
done <<< "$BACKEND_CONTRACT_PATHS"
printf '%b' "$BACKEND_CONTRACT_BLOB_EVIDENCE"

if ! FRONTEND_TOPLEVEL="$(git -C "$FRONTEND_REPOSITORY" rev-parse --show-toplevel)"; then
  exit 71
fi
test "$FRONTEND_TOPLEVEL" = "$FRONTEND_REPOSITORY"
if ! FRONTEND_FETCH_URLS="$(git -C "$FRONTEND_REPOSITORY" remote get-url --all origin)"; then
  exit 71
fi
if ! FRONTEND_PUSH_URLS="$(git -C "$FRONTEND_REPOSITORY" remote get-url --push --all origin)"; then
  exit 71
fi
test "$(printf '%s\n' "$FRONTEND_FETCH_URLS" | awk 'END { print NR }')" = "1"
test "$(printf '%s\n' "$FRONTEND_PUSH_URLS" | awk 'END { print NR }')" = "1"
FRONTEND_FETCH_URL="$(printf '%s\n' "$FRONTEND_FETCH_URLS" | sed -n '1p')"
FRONTEND_PUSH_URL="$(printf '%s\n' "$FRONTEND_PUSH_URLS" | sed -n '1p')"
FRONTEND_FETCH_REPOSITORY="$(gh repo view "$FRONTEND_FETCH_URL" --json nameWithOwner --jq '.nameWithOwner')"
FRONTEND_PUSH_REPOSITORY="$(gh repo view "$FRONTEND_PUSH_URL" --json nameWithOwner --jq '.nameWithOwner')"
test "$FRONTEND_FETCH_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
test "$FRONTEND_PUSH_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
FRONTEND_GITHUB_REPOSITORY="$FRONTEND_FETCH_REPOSITORY"
git -C "$FRONTEND_REPOSITORY" fetch "$FRONTEND_FETCH_URL" \
  'refs/heads/main:refs/remotes/origin/main'
if ! FRONTEND_ORIGIN_MAIN_SHA="$(git -C "$FRONTEND_REPOSITORY" rev-parse origin/main)"; then
  exit 71
fi
if ! FRONTEND_PRIMARY_STATUS="$(git -C "$FRONTEND_REPOSITORY" status --porcelain)"; then
  exit 71
fi
if ! FRONTEND_STORY_LOCAL_BRANCH="$(git -C "$FRONTEND_REPOSITORY" branch --list "$STORY_BRANCH")"; then
  exit 71
fi
if ! FRONTEND_STORY_REMOTE_BRANCH="$(git -C "$FRONTEND_REPOSITORY" ls-remote --heads "$FRONTEND_PUSH_URL" "$STORY_BRANCH")"; then
  exit 71
fi
if ! FRONTEND_WORKTREE_LIST="$(git -C "$FRONTEND_REPOSITORY" worktree list --porcelain)"; then
  exit 71
fi
if ! STORY_169_14_HANDOFF_LOCAL_BRANCH="$(git -C "$FRONTEND_REPOSITORY" \
  branch --list "$STORY_169_14_FINAL_HANDOFF_BRANCH")"; then
  exit 71
fi
if ! STORY_169_14_HANDOFF_REMOTE_BRANCH="$(git -C "$FRONTEND_REPOSITORY" \
  ls-remote --heads "$FRONTEND_PUSH_URL" \
  "$STORY_169_14_FINAL_HANDOFF_BRANCH")"; then
  exit 71
fi
test -z "$STORY_169_14_HANDOFF_LOCAL_BRANCH"
test -z "$STORY_169_14_HANDOFF_REMOTE_BRANCH"
assert_absent_non_symlink_residue "$STORY_169_14_FINAL_HANDOFF_WORKTREE"
case "$FRONTEND_WORKTREE_LIST" in
  *"$STORY_169_14_FINAL_HANDOFF_WORKTREE"*) exit 66 ;;
esac
if ! FRONTEND_GIT_COMMON_DIR="$(git -C "$FRONTEND_REPOSITORY" \
  rev-parse --path-format=absolute --git-common-dir)"; then
  exit 71
fi
assert_absent_non_symlink_residue \
  "$FRONTEND_GIT_COMMON_DIR/story-169-14-final-handoff-reviewed-head-v1"
assert_absent_non_symlink_residue \
  "$FRONTEND_GIT_COMMON_DIR/story-169-14-final-handoff-pr-record-v1"
assert_absent_non_symlink_residue \
  "$FRONTEND_GIT_COMMON_DIR/story-169-14-final-handoff-review-bootstrap-v1"
assert_absent_non_symlink_residue \
  "$FRONTEND_GIT_COMMON_DIR/story-169-15-review-bootstrap-v1"
assert_absent_non_symlink_residue \
  "$FRONTEND_GIT_COMMON_DIR/story-169-15-reviewed-head-v1"
assert_absent_non_symlink_residue \
  "$FRONTEND_GIT_COMMON_DIR/story-169-15-lifecycle-record-v1"
assert_absent_non_symlink_residue \
  "$FRONTEND_GIT_COMMON_DIR/story-169-15-cleanup-transaction-v1"
PR_SCAN_LIMIT=1000
if ! FRONTEND_STORY_PR_JSON="$(gh pr list \
  --repo "$FRONTEND_GITHUB_REPOSITORY" \
  --state all \
  --head "$STORY_BRANCH" \
  --limit "$PR_SCAN_LIMIT" \
  --json number)"; then
  exit 71
fi
if ! FRONTEND_STORY_PR_COUNT="$(printf '%s\n' \
  "$FRONTEND_STORY_PR_JSON" | jq -er 'length')"; then
  exit 71
fi
case "$FRONTEND_STORY_PR_COUNT" in
  *[!0-9]* | '') exit 64 ;;
esac
test "$FRONTEND_STORY_PR_COUNT" -lt "$PR_SCAN_LIMIT"
test -z "$FRONTEND_PRIMARY_STATUS"
test -z "$FRONTEND_STORY_LOCAL_BRANCH"
test -z "$FRONTEND_STORY_REMOTE_BRANCH"
test "$FRONTEND_STORY_PR_COUNT" = "0"
assert_absent_non_symlink_residue "$STORY_WORKTREE"
case "$FRONTEND_WORKTREE_LIST" in
  *"$STORY_WORKTREE"*) exit 68 ;;
esac

LINKED_FRONTEND_WORKTREES="$(printf '%s\n' "$FRONTEND_WORKTREE_LIST" | awk '$1 == "worktree" {print substr($0, 10)}')"
while IFS= read -r LINKED_FRONTEND_WORKTREE; do
  test -n "$LINKED_FRONTEND_WORKTREE"
  if ! LINKED_FRONTEND_TOPLEVEL="$(git -C "$LINKED_FRONTEND_WORKTREE" rev-parse --show-toplevel)"; then
    exit 71
  fi
  test "$LINKED_FRONTEND_TOPLEVEL" = "$LINKED_FRONTEND_WORKTREE"
  if ! LINKED_UNSTAGED_PATHS="$(git -C "$LINKED_FRONTEND_WORKTREE" diff --no-renames --name-only)"; then
    exit 71
  fi
  if ! LINKED_STAGED_PATHS="$(git -C "$LINKED_FRONTEND_WORKTREE" diff --cached --no-renames --name-only)"; then
    exit 71
  fi
  if ! LINKED_UNTRACKED_PATHS="$(git -C "$LINKED_FRONTEND_WORKTREE" ls-files --others --exclude-standard)"; then
    exit 71
  fi
  LINKED_IGNORED_UNTRACKED_PATHS=""
  while IFS= read -r STORY_ALLOWED_PATH; do
    if ! TRACKED_ALLOWED_PATH="$(git -C "$LINKED_FRONTEND_WORKTREE" ls-files -- "$STORY_ALLOWED_PATH")"; then
      exit 71
    fi
    IGNORED_ALLOWED_PATH="$LINKED_FRONTEND_WORKTREE/$STORY_ALLOWED_PATH"
    if test -z "$TRACKED_ALLOWED_PATH" && \
      { test -e "$IGNORED_ALLOWED_PATH" || test -L "$IGNORED_ALLOWED_PATH"; }; then
      LINKED_IGNORED_UNTRACKED_PATHS="${LINKED_IGNORED_UNTRACKED_PATHS}${STORY_ALLOWED_PATH}\n"
    fi
  done <<< "$STORY_STATIC_ALLOWED_MANIFEST"
  LINKED_WIP_PATHS="$(printf '%s\n%s\n%s\n%b' \
    "$LINKED_UNSTAGED_PATHS" \
    "$LINKED_STAGED_PATHS" \
    "$LINKED_UNTRACKED_PATHS" \
    "$LINKED_IGNORED_UNTRACKED_PATHS" | sed '/^$/d' | sort -u)"
  if ! LINKED_WIP_OVERLAP="$(comm -12 \
    <(printf '%s\n' "$LINKED_WIP_PATHS") \
    <(printf '%s\n' "$STORY_STATIC_ALLOWED_MANIFEST"))"; then
    exit 71
  fi
  test -z "$LINKED_WIP_OVERLAP"
  if ! LINKED_HEAD_SHA="$(git -C "$LINKED_FRONTEND_WORKTREE" rev-parse HEAD)"; then
    exit 71
  fi
  if ! LINKED_DIVERGENT_PATHS="$(git -C "$LINKED_FRONTEND_WORKTREE" diff --no-renames --name-only \
    "$FRONTEND_ORIGIN_MAIN_SHA...$LINKED_HEAD_SHA" | sort -u)"; then
    exit 71
  fi
  if ! LINKED_DIVERGENT_OVERLAP="$(comm -12 \
    <(printf '%s\n' "$LINKED_DIVERGENT_PATHS") \
    <(printf '%s\n' "$STORY_STATIC_ALLOWED_MANIFEST"))"; then
    exit 71
  fi
  test -z "$LINKED_DIVERGENT_OVERLAP"
done <<< "$LINKED_FRONTEND_WORKTREES"

if ! FRONTEND_LOCAL_BRANCHES="$(git -C "$FRONTEND_REPOSITORY" for-each-ref \
  --format='%(refname:short)' refs/heads | sort -u)"; then
  exit 71
fi
if ! ATTACHED_FRONTEND_BRANCHES="$(printf '%s\n' \
  "$FRONTEND_WORKTREE_LIST" | awk '
    $1 == "branch" {
      sub("^refs/heads/", "", $2)
      print $2
    }
  ' | sort -u)"; then
  exit 71
fi
if ! UNATTACHED_FRONTEND_BRANCHES="$(comm -23 \
  <(printf '%s\n' "$FRONTEND_LOCAL_BRANCHES") \
  <(printf '%s\n' "$ATTACHED_FRONTEND_BRANCHES"))"; then
  exit 71
fi
if test -n "$UNATTACHED_FRONTEND_BRANCHES"; then
  while IFS= read -r UNATTACHED_FRONTEND_BRANCH; do
    test -n "$UNATTACHED_FRONTEND_BRANCH"
    if ! UNATTACHED_FRONTEND_HEAD_SHA="$(git -C "$FRONTEND_REPOSITORY" \
      rev-parse --verify "refs/heads/$UNATTACHED_FRONTEND_BRANCH^{commit}")"; then
      exit 71
    fi
    if ! UNATTACHED_FRONTEND_DIVERGENT_PATHS="$(git -C "$FRONTEND_REPOSITORY" \
      diff --no-renames --name-only \
      "$FRONTEND_ORIGIN_MAIN_SHA...$UNATTACHED_FRONTEND_HEAD_SHA" | sort -u)"; then
      exit 71
    fi
    if ! UNATTACHED_FRONTEND_OVERLAP="$(comm -12 \
      <(printf '%s\n' "$UNATTACHED_FRONTEND_DIVERGENT_PATHS") \
      <(printf '%s\n' "$STORY_STATIC_ALLOWED_MANIFEST"))"; then
      exit 71
    fi
    test -z "$UNATTACHED_FRONTEND_OVERLAP"
  done <<< "$UNATTACHED_FRONTEND_BRANCHES"
fi

if ! OPEN_FRONTEND_PR_JSON="$(gh pr list \
  --repo "$FRONTEND_GITHUB_REPOSITORY" \
  --state open \
  --limit "$PR_SCAN_LIMIT" \
  --json number)"; then
  exit 71
fi
if ! OPEN_FRONTEND_PR_COUNT="$(printf '%s\n' \
  "$OPEN_FRONTEND_PR_JSON" | jq -er 'length')"; then
  exit 71
fi
case "$OPEN_FRONTEND_PR_COUNT" in
  *[!0-9]* | '') exit 64 ;;
esac
test "$OPEN_FRONTEND_PR_COUNT" -lt "$PR_SCAN_LIMIT"
if ! OPEN_FRONTEND_PR_NUMBERS="$(printf '%s\n' \
  "$OPEN_FRONTEND_PR_JSON" | jq -r '.[].number')"; then
  exit 71
fi
if test -n "$OPEN_FRONTEND_PR_NUMBERS"; then
  while IFS= read -r OPEN_FRONTEND_PR_NUMBER; do
    if ! OPEN_FRONTEND_PR_CHANGED_FILE_COUNT="$(gh pr view \
      "$OPEN_FRONTEND_PR_NUMBER" \
      --repo "$FRONTEND_GITHUB_REPOSITORY" \
      --json changedFiles \
      --jq '.changedFiles')"; then
      exit 71
    fi
    if ! OPEN_FRONTEND_PR_FILES_JSON="$(gh api --paginate --slurp \
      -H 'Accept: application/vnd.github+json' \
      "/repos/$FRONTEND_GITHUB_REPOSITORY/pulls/$OPEN_FRONTEND_PR_NUMBER/files?per_page=100")"; then
      exit 71
    fi
    if ! OPEN_FRONTEND_PR_ENUMERATED_FILE_COUNT="$(printf '%s\n' \
      "$OPEN_FRONTEND_PR_FILES_JSON" | count_pr_file_objects)"; then
      exit 71
    fi
    test "$OPEN_FRONTEND_PR_ENUMERATED_FILE_COUNT" = \
      "$OPEN_FRONTEND_PR_CHANGED_FILE_COUNT"
    if ! OPEN_FRONTEND_PR_PATHS="$(printf '%s\n' \
      "$OPEN_FRONTEND_PR_FILES_JSON" | normalize_pr_collision_paths)"; then
      exit 71
    fi
    if ! OPEN_FRONTEND_PR_OVERLAP="$(comm -12 \
      <(printf '%s\n' "$OPEN_FRONTEND_PR_PATHS") \
      <(printf '%s\n' "$STORY_STATIC_ALLOWED_MANIFEST"))"; then
      exit 71
    fi
    test -z "$OPEN_FRONTEND_PR_OVERLAP"
  done <<< "$OPEN_FRONTEND_PR_NUMBERS"
fi

git -C "$FRONTEND_REPOSITORY" switch main
git -C "$FRONTEND_REPOSITORY" merge --ff-only origin/main
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor "$PR_226_PREFACE_MERGE_SHA" "$FRONTEND_ORIGIN_MAIN_SHA"
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor "$PR_226_PREFACE_MERGE_SHA" main
if ! STORY_BASE_SHA="$(git -C "$FRONTEND_REPOSITORY" rev-parse HEAD)"; then
  exit 71
fi
test "$STORY_BASE_SHA" = "$FRONTEND_ORIGIN_MAIN_SHA"
git -C "$FRONTEND_REPOSITORY" worktree add -b "$STORY_BRANCH" "$STORY_WORKTREE" "$STORY_BASE_SHA"
if ! CREATED_STORY_TOPLEVEL="$(git -C "$STORY_WORKTREE" rev-parse --show-toplevel)"; then
  exit 71
fi
if ! CREATED_STORY_BRANCH="$(git -C "$STORY_WORKTREE" branch --show-current)"; then
  exit 71
fi
if ! CREATED_STORY_HEAD="$(git -C "$STORY_WORKTREE" rev-parse HEAD)"; then
  exit 71
fi
test "$CREATED_STORY_TOPLEVEL" = "$STORY_WORKTREE"
test "$CREATED_STORY_BRANCH" = "$STORY_BRANCH"
test "$CREATED_STORY_HEAD" = "$STORY_BASE_SHA"
printf 'export STORY_169_15_BASE_SHA=%q\n' "$STORY_BASE_SHA"
git -C "$STORY_WORKTREE" status --short --branch
```

The exact recorded merge SHA and, when used, the exact Story 169.14 foreign-WIP reservation are mandatory inputs; merge-message grep or another fuzzy lookup is forbidden. Record backend local/origin SHAs and whether local-main equality was proven or correctly deferred. Never reuse a stale branch/worktree or clean/revert another owner's work.

## RED-first implementation sequence

1. Read the merged Story 169.14 DTO/controller/status fixtures and authoritative API documentation; record exact request keys and response fields.
2. Record PR #226 as the frontend preface baseline, inventory every later consumer/change to the allowed API/type/normalizer/hook paths, and lock its `unknown`-sentinel regression tests before writing new RED cases. Do not relabel already-green PR #226 behavior as Story 169.15 RED.
3. Add honest RED tests only for gaps that remain against the merged Story 169.14 contract: exact request serialization, start/status field aliases, imported-row result propagation, actionable failure detail, shared polling termination/recovery, and defensive diagnostics.
4. Cover pending, processing, completed with imported rows, failed with actionable detail, snake/camel aliases only where the merged contract explicitly requires them, and unknown/malformed values. Assert that `unknown` remains distinguishable from `failed` and cannot create false terminal evidence.
5. After behavioral RED identifies the acceptance-necessary changes and before the first implementation edit, freeze the exact sorted newline-delimited reviewed implementation manifest in `_bmad-output/implementation-artifacts/169-15-fe-align-shared-frontend-paid-storage-import-boundary.md` between `<!-- STORY_169_15_FROZEN_REVIEWED_MANIFEST:START -->` and `<!-- STORY_169_15_FROZEN_REVIEWED_MANIFEST:END -->`. The evidence artifact must contain exactly one each of `STORY_169_15_BASE_SHA`, `STORY_169_15_RED_CLASSIFICATION: BEHAVIORAL_CONTRACT_FAILURE`, `STORY_169_15_RED_COMMAND`, `STORY_169_15_RED_EXIT_CODE`, `STORY_169_15_RED_OUTPUT_SHA256`, `STORY_169_15_PRE_IMPLEMENTATION_REVIEWER_ID`, `STORY_169_15_PRE_IMPLEMENTATION_REVIEW_DISPOSITION: PASS`, `STORY_169_15_REVIEWED_MANIFEST_SHA256`, `STORY_169_15_REVIEW_EVIDENCE_SHA256`, and `STORY_169_15_FROZEN_MANIFEST_SHA256`, plus exact nonempty blocks delimited by `STORY_169_15_RETAINED_RED_OUTPUT`, `STORY_169_15_RETAINED_REVIEW_EVIDENCE`, and `STORY_169_15_FROZEN_REVIEWED_MANIFEST` START/END markers. The retained review-evidence payload must contain exactly one complete line `STORY_169_15_PRIVACY_REVIEW_DISPOSITION: PASS`; because that complete payload is committed and hashed, the privacy attestation is bound to the same immutable review evidence. Read the committed artifact into a file; one global exact-order state machine must reject duplicate markers, same-type or cross-type nesting, mismatched/early END, reordered blocks, and unclosed blocks. Extract all three payloads to files with delimiters excluded and one LF per complete payload line, including trailing blank lines; hash those files directly and compare the RED/reviewer/manifest hashes to their committed markers. Hash-only or caller-only evidence is forbidden. The independent reviewer must review the exact artifact bytes and manifest hash before the evidence-only commit. Force-stage that ignored artifact by its exact path, commit it alone, retain its exact commit SHA, and export the identical expected values for executable validation. Do not derive evidence from the later Git diff or include an unchanged preface path merely to satisfy a static list.
6. Prove RED is behavioral, not broken imports/fixtures/types, and retain exact failing output plus the exact independent review payload with synthetic fixture identities and sanitized logging. Apply the same non-echoing sensitive-evidence scan and exact privacy reviewer confirmation as Story 169.14 before committing the artifact. Assignment detection is suffix-aware for conventional prefixed credential names and rejects `=`, `:=`, `+=`, `-=`, `?=`, `&&=`, and `||=`; common password/secret/token/auth-token/access-token/refresh-token/client-secret/API-key/credential/AWS secret-access-key/access-key-ID families are covered. Evidence preflight must fail closed until the blank-line-stripped manifest is nonempty, byte-canonical sorted-unique, a subset of the static allowlist, and excludes the evidence artifact. Its executable self-test covers an allowed path, a forbidden path, an empty manifest, the evidence-artifact path, every protected colon header, plain and compound prefixed credential assignments, private-key material, credential-bearing URI userinfo, benign diagnostics, and missing/duplicate/exact privacy attestations.
7. Implement the smallest type/API/normalizer/polling alignment. Keep the authoritative mapping in the shared boundary; do not duplicate it in Story 169.12's route.
8. Preserve an explicit defensive `unknown` sentinel and diagnostics for unrecognized lifecycle values; do not coerce it to `failed`, treat it as success, or synthesize `partial`, counts, or retry subsets.
9. Run targeted and universal frontend validation, exact scope audit, review pass 1, fixes/reruns, review pass 2, and final readiness verification.

## Validation

Run in this order and record commands, exit codes, counts, and complete failures:

```bash
set -euo pipefail

npx vitest run src/lib/api/__tests__/storage-analytics.test.ts src/lib/api/__tests__/storage-import-normalizer.test.ts src/hooks/__tests__/useStorageAnalytics.test.ts
npm run format:check
npm run lint
npm run type-check
npm run check:max-lines
npm run build
git diff --check
```

No browser/E2E or route-presentation proof is claimed here. `e2e/storage-analytics.spec.ts` is read-only and belongs to Story 169.12's exact-path validation. An unavailable backend integration check is a named gap, never a pass.

Record Node/npm versions, frontend base SHA, Story 169.14 merge SHA/refreshed-origin ancestry, backend local/origin SHAs, any exact foreign-WIP reservation and local-main deferral, request/response fixtures, exact changed-file manifest, targeted/full gate output, defensive-unknown diagnostics evidence, and reviewer dispositions.

## Independent review

1. **Pass 1 — adversarial code review:** a fresh reviewer who did not author the change checks merged-contract fidelity, request serialization, type/normalizer strictness, polling termination, rows/error preservation, query/invalidation stability, tests, diagnostics, and exact scope.
2. Resolve every accepted finding and rerun the smallest affected checks plus all universal frontend gates.
3. **Pass 2 — fresh verifier review:** a different fresh context verifies final diff, recorded backend merge proof, no route/E2E/forbidden files, no fabricated partial state, test evidence, and PR readiness.
4. Do not commit with unresolved accepted findings or self-approval as the only evidence.

## Commit, push, PR, and merge

Run this fence first with `STORY_169_15_EXECUTION_PHASE=evidence-preflight` immediately after behavioral RED and before the first implementation edit; it must stop successfully after exact committed base/RED/reviewer/manifest validation and reject stale bootstrap residue. Rerun it with `STORY_169_15_EXECUTION_PHASE=delivery` only after the backend prerequisite, RED→GREEN proof, all local gates, both reviews, and exact scope audit. Exactly one serialized Story leader may execute delivery, publish recovery, or cleanup; concurrent lifecycle-fence invocations are forbidden. Before the final commit, delivery atomically publishes mode-600 `story-169-15-review-bootstrap-v1`, binding the exact evidence parent, reviewed tree, cumulative-manifest and evidence-artifact hashes, and both reviewer identities/PASS dispositions. That record is the only authority for post-commit/pre-reviewed-head recovery. After publishing and cross-checking the reviewed-head record, delivery consumes the bootstrap before the first push. REST all-state enumeration normalizes `open → OPEN`, `closed + merged_at → MERGED`, and `closed + null → CLOSED_UNMERGED`; recovery re-reads the normalized row, never re-merges an already merged PR, and rejects closed-unmerged/invalid state before lifecycle-record publication. A finalized lifecycle record routes directly to cleanup and is never overwritten by publish recovery:

```bash
set -euo pipefail

FRONTEND_REPOSITORY="/Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend"
EXPECTED_FRONTEND_GITHUB_REPOSITORY="salacoste/wb-erp-system-daytona-FE"
STORY_BRANCH="cdx/epic-169-story-15-storage-import-boundary"
STORY_WORKTREE="/private/tmp/wb-repricer-fe-169-15-storage-import-boundary"
STORY_EVIDENCE_ARTIFACT="_bmad-output/implementation-artifacts/169-15-fe-align-shared-frontend-paid-storage-import-boundary.md"
STORY_BASE_SHA="${STORY_169_15_BASE_SHA:?export the exact Story base SHA recorded at worktree creation}"
EVIDENCE_COMMIT_SHA="${STORY_169_15_EVIDENCE_COMMIT_SHA:?export the exact commit containing the frozen-manifest evidence artifact}"
EXECUTION_PHASE="${STORY_169_15_EXECUTION_PHASE:?export evidence-preflight, delivery, or publish-recovery}"
case "$EXECUTION_PHASE" in
  evidence-preflight | delivery | publish-recovery) ;;
  *) exit 64 ;;
esac
RAW_REVIEWED_MANIFEST="${STORY_169_15_FROZEN_REVIEWED_MANIFEST:?export the exact manifest frozen after RED and before implementation edits}"
RECORDED_MANIFEST_SHA256="${STORY_169_15_FROZEN_MANIFEST_SHA256:?export the SHA-256 recorded with the frozen manifest}"
RECORDED_RED_OUTPUT_SHA256="${STORY_169_15_RED_OUTPUT_SHA256:?export the SHA-256 of retained complete behavioral RED output}"
RECORDED_PRE_IMPLEMENTATION_REVIEWER_ID="${STORY_169_15_PRE_IMPLEMENTATION_REVIEWER_ID:?export the stable independent evidence reviewer identifier}"
RECORDED_REVIEW_EVIDENCE_SHA256="${STORY_169_15_REVIEW_EVIDENCE_SHA256:?export the SHA-256 of retained pre-implementation review evidence}"
STORY_STATIC_ALLOWED_MANIFEST="$(printf '%s\n' \
  "$STORY_EVIDENCE_ARTIFACT" \
  'src/hooks/__tests__/useStorageAnalytics.test.ts' \
  'src/hooks/useImportStatus.ts' \
  'src/lib/api/__tests__/storage-analytics.test.ts' \
  'src/lib/api/__tests__/storage-import-normalizer.test.ts' \
  'src/lib/api/storage-analytics.ts' \
  'src/lib/api/storage-import-normalizer.ts' \
  'src/types/storage-analytics-trends.ts' \
  'src/types/storage-analytics/index.ts' | sort)"

assert_story_worktree_identity() {
  if ! ASSERTED_TOPLEVEL="$(git -C "$STORY_WORKTREE" rev-parse --show-toplevel)"; then
    exit 71
  fi
  if ! ASSERTED_BRANCH="$(git -C "$STORY_WORKTREE" branch --show-current)"; then
    exit 71
  fi
  test "$ASSERTED_TOPLEVEL" = "$STORY_WORKTREE"
  test "$ASSERTED_BRANCH" = "$STORY_BRANCH"
}

if ! FRONTEND_TOPLEVEL="$(git -C "$FRONTEND_REPOSITORY" rev-parse --show-toplevel)"; then
  exit 71
fi
test "$FRONTEND_TOPLEVEL" = "$FRONTEND_REPOSITORY"
if ! FRONTEND_GIT_COMMON_DIR="$(git -C "$FRONTEND_REPOSITORY" rev-parse --path-format=absolute --git-common-dir)"; then
  exit 71
fi
test -d "$FRONTEND_GIT_COMMON_DIR"
STORY_REVIEW_BOOTSTRAP_RECORD="$FRONTEND_GIT_COMMON_DIR/story-169-15-review-bootstrap-v1"
STORY_TMP="$(mktemp -d "${TMPDIR:-/tmp}/story-169-15-delivery.XXXXXX")"
trap 'rm -rf "$STORY_TMP"' EXIT
assert_regular_mode_600_record() {
  local record_path="$1"
  test ! -L "$record_path" || return 74
  test -f "$record_path" || return 74
  test "$(stat -f '%Lp' "$record_path")" = "600" || return 74
}
assert_absent_non_symlink_residue() {
  local residue_path="$1"
  test ! -L "$residue_path" || return 74
  test ! -e "$residue_path" || return 74
}
if test "$EXECUTION_PHASE" = "evidence-preflight"; then
  assert_absent_non_symlink_residue "$STORY_REVIEW_BOOTSTRAP_RECORD"
fi
SYNTHETIC_REGULAR_RECORD="$STORY_TMP/regular-mode-600-record"
SYNTHETIC_LIVE_RECORD_SYMLINK="$STORY_TMP/live-record-symlink"
printf '%s\n' 'synthetic-authority' > "$SYNTHETIC_REGULAR_RECORD"
chmod 600 "$SYNTHETIC_REGULAR_RECORD"
assert_regular_mode_600_record "$SYNTHETIC_REGULAR_RECORD"
ln -s "$SYNTHETIC_REGULAR_RECORD" "$SYNTHETIC_LIVE_RECORD_SYMLINK"
if assert_regular_mode_600_record "$SYNTHETIC_LIVE_RECORD_SYMLINK"; then
  exit 73
fi
rm -- "$SYNTHETIC_LIVE_RECORD_SYMLINK" "$SYNTHETIC_REGULAR_RECORD"
SYNTHETIC_ABSENT_RESIDUE="$STORY_TMP/absent-residue"
assert_absent_non_symlink_residue "$SYNTHETIC_ABSENT_RESIDUE"
ln -s "$STORY_TMP/missing-residue-target" "$SYNTHETIC_ABSENT_RESIDUE"
if assert_absent_non_symlink_residue "$SYNTHETIC_ABSENT_RESIDUE"; then
  exit 73
fi
rm -- "$SYNTHETIC_ABSENT_RESIDUE"
if ! ORIGIN_FETCH_URLS="$(git -C "$FRONTEND_REPOSITORY" remote get-url --all origin)"; then
  exit 71
fi
if ! ORIGIN_PUSH_URLS="$(git -C "$FRONTEND_REPOSITORY" remote get-url --push --all origin)"; then
  exit 71
fi
test "$(printf '%s\n' "$ORIGIN_FETCH_URLS" | awk 'END { print NR }')" = "1"
test "$(printf '%s\n' "$ORIGIN_PUSH_URLS" | awk 'END { print NR }')" = "1"
ORIGIN_FETCH_URL="$(printf '%s\n' "$ORIGIN_FETCH_URLS" | sed -n '1p')"
ORIGIN_PUSH_URL="$(printf '%s\n' "$ORIGIN_PUSH_URLS" | sed -n '1p')"
FETCH_REPOSITORY="$(gh repo view "$ORIGIN_FETCH_URL" --json nameWithOwner --jq '.nameWithOwner')"
PUSH_REPOSITORY="$(gh repo view "$ORIGIN_PUSH_URL" --json nameWithOwner --jq '.nameWithOwner')"
test "$FETCH_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
test "$PUSH_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
REPOSITORY="$FETCH_REPOSITORY"
assert_story_worktree_identity
git -C "$STORY_WORKTREE" diff --check
if ! CACHED_BEFORE_STAGING="$(git -C "$STORY_WORKTREE" diff --cached --no-renames --name-only)"; then
  exit 71
fi
if test "$EXECUTION_PHASE" != "delivery"; then
  test -z "$CACHED_BEFORE_STAGING"
fi
if ! CURRENT_HEAD_SHA="$(git -C "$STORY_WORKTREE" rev-parse HEAD)"; then
  exit 71
fi
case "$EXECUTION_PHASE" in
  evidence-preflight)
    test "$CURRENT_HEAD_SHA" = "$EVIDENCE_COMMIT_SHA"
    ;;
  delivery)
    if test "$CURRENT_HEAD_SHA" = "$EVIDENCE_COMMIT_SHA"; then
      DELIVERY_BOOTSTRAP_STATE="PRE_COMMIT_NO_RECORD"
    else
      DELIVERY_BOOTSTRAP_STATE="POST_COMMIT_NO_RECORD"
      if ! DELIVERY_BOOTSTRAP_STATUS="$(git -C "$STORY_WORKTREE" status --porcelain)"; then
        exit 71
      fi
      test -z "$DELIVERY_BOOTSTRAP_STATUS"
    fi
    ;;
  publish-recovery)
    test "$CURRENT_HEAD_SHA" != "$EVIDENCE_COMMIT_SHA"
    if ! RECOVERY_STATUS="$(git -C "$STORY_WORKTREE" status --porcelain)"; then
      exit 71
    fi
    test -z "$RECOVERY_STATUS"
    git -C "$STORY_WORKTREE" merge-base --is-ancestor \
      "$EVIDENCE_COMMIT_SHA" "$CURRENT_HEAD_SHA"
    ;;
esac
git -C "$STORY_WORKTREE" cat-file -e "$EVIDENCE_COMMIT_SHA^{commit}"
if ! EVIDENCE_COMMIT_AND_PARENTS="$(git -C "$STORY_WORKTREE" rev-list \
  --parents -n 1 "$EVIDENCE_COMMIT_SHA")"; then
  exit 71
fi
IFS=' ' read -r EVIDENCE_COMMIT_OID EVIDENCE_PARENT_SHA EVIDENCE_EXTRA_PARENT \
  <<< "$EVIDENCE_COMMIT_AND_PARENTS"
test "$EVIDENCE_COMMIT_OID" = "$EVIDENCE_COMMIT_SHA"
test "$EVIDENCE_PARENT_SHA" = "$STORY_BASE_SHA"
test -z "$EVIDENCE_EXTRA_PARENT"
if ! EVIDENCE_COMMIT_PATHS="$(git -C "$STORY_WORKTREE" diff-tree \
  --no-commit-id \
  --no-renames \
  --name-only \
  -r "$STORY_BASE_SHA" "$EVIDENCE_COMMIT_SHA" | sort -u)"; then
  exit 71
fi
test "$EVIDENCE_COMMIT_PATHS" = "$STORY_EVIDENCE_ARTIFACT"
EVIDENCE_ARTIFACT_FILE="$STORY_TMP/evidence-artifact"
if ! git -C "$STORY_WORKTREE" show \
  "$EVIDENCE_COMMIT_SHA:$STORY_EVIDENCE_ARTIFACT" > \
  "$EVIDENCE_ARTIFACT_FILE"; then
  exit 71
fi
test -s "$EVIDENCE_ARTIFACT_FILE"
if ! EVIDENCE_ARTIFACT_SHA256="$(shasum -a 256 \
  "$EVIDENCE_ARTIFACT_FILE" | awk '{ print $1 }')"; then
  exit 71
fi
if ! COMMITTED_EVIDENCE_VALUES="$(awk '
  index($0, "STORY_169_15_BASE_SHA: ") == 1 {
    base_count++; base = substr($0, 24)
  }
  $0 == "STORY_169_15_RED_CLASSIFICATION: BEHAVIORAL_CONTRACT_FAILURE" {
    classification_count++
  }
  index($0, "STORY_169_15_RED_COMMAND: ") == 1 {
    command_count++; command = substr($0, 27)
  }
  index($0, "STORY_169_15_RED_EXIT_CODE: ") == 1 {
    exit_count++; red_exit = substr($0, 29)
  }
  index($0, "STORY_169_15_RED_OUTPUT_SHA256: ") == 1 {
    red_hash_count++; red_hash = substr($0, 33)
  }
  index($0, "STORY_169_15_PRE_IMPLEMENTATION_REVIEWER_ID: ") == 1 {
    reviewer_count++; reviewer = substr($0, 46)
  }
  $0 == "STORY_169_15_PRE_IMPLEMENTATION_REVIEW_DISPOSITION: PASS" {
    disposition_count++; disposition = "PASS"
  }
  index($0, "STORY_169_15_REVIEWED_MANIFEST_SHA256: ") == 1 {
    reviewed_hash_count++; reviewed_hash = substr($0, 40)
  }
  index($0, "STORY_169_15_REVIEW_EVIDENCE_SHA256: ") == 1 {
    review_evidence_count++; review_evidence = substr($0, 38)
  }
  index($0, "STORY_169_15_FROZEN_MANIFEST_SHA256: ") == 1 {
    manifest_hash_count++; manifest_hash = substr($0, 38)
  }
  END {
    if (base_count != 1 || classification_count != 1 || command_count != 1 ||
        exit_count != 1 || red_hash_count != 1 || reviewer_count != 1 ||
        disposition_count != 1 || reviewed_hash_count != 1 ||
        review_evidence_count != 1 || manifest_hash_count != 1 || command == "") {
      exit 2
    }
    print base "|" red_exit "|" red_hash "|" reviewer "|" disposition "|" \
      reviewed_hash "|" review_evidence "|" manifest_hash
  }
' "$EVIDENCE_ARTIFACT_FILE")"; then
  exit 71
fi
IFS='|' read -r COMMITTED_BASE_SHA COMMITTED_RED_EXIT_CODE \
  COMMITTED_RED_OUTPUT_SHA256 COMMITTED_PRE_IMPLEMENTATION_REVIEWER_ID \
  COMMITTED_REVIEW_DISPOSITION COMMITTED_REVIEWED_MANIFEST_SHA256 \
  COMMITTED_REVIEW_EVIDENCE_SHA256 COMMITTED_MANIFEST_SHA256 \
  <<< "$COMMITTED_EVIDENCE_VALUES"
test "$COMMITTED_BASE_SHA" = "$STORY_BASE_SHA"
case "$COMMITTED_RED_EXIT_CODE" in
  '' | *[!0-9]*) exit 64 ;;
esac
test "$COMMITTED_RED_EXIT_CODE" != "0"
test "$COMMITTED_RED_OUTPUT_SHA256" = "$RECORDED_RED_OUTPUT_SHA256"
test "$COMMITTED_PRE_IMPLEMENTATION_REVIEWER_ID" = \
  "$RECORDED_PRE_IMPLEMENTATION_REVIEWER_ID"
test "$COMMITTED_REVIEW_DISPOSITION" = "PASS"
test "$COMMITTED_REVIEWED_MANIFEST_SHA256" = "$RECORDED_MANIFEST_SHA256"
test "$COMMITTED_REVIEW_EVIDENCE_SHA256" = \
  "$RECORDED_REVIEW_EVIDENCE_SHA256"
test "$COMMITTED_MANIFEST_SHA256" = "$RECORDED_MANIFEST_SHA256"
for SHA256_VALUE in \
  "$COMMITTED_RED_OUTPUT_SHA256" \
  "$COMMITTED_REVIEWED_MANIFEST_SHA256" \
  "$COMMITTED_REVIEW_EVIDENCE_SHA256" \
  "$COMMITTED_MANIFEST_SHA256"; do
  test "${#SHA256_VALUE}" -eq 64
  case "$SHA256_VALUE" in
    *[!0-9a-f]* | '') exit 64 ;;
  esac
done
case "$COMMITTED_PRE_IMPLEMENTATION_REVIEWER_ID" in
  '' | *[!A-Za-z0-9._/@:-]*) exit 64 ;;
esac
if ! awk '
  BEGIN {
    expected[1] = "<!-- STORY_169_15_RETAINED_RED_OUTPUT:START -->"
    expected[2] = "<!-- STORY_169_15_RETAINED_RED_OUTPUT:END -->"
    expected[3] = "<!-- STORY_169_15_RETAINED_REVIEW_EVIDENCE:START -->"
    expected[4] = "<!-- STORY_169_15_RETAINED_REVIEW_EVIDENCE:END -->"
    expected[5] = "<!-- STORY_169_15_FROZEN_REVIEWED_MANIFEST:START -->"
    expected[6] = "<!-- STORY_169_15_FROZEN_REVIEWED_MANIFEST:END -->"
    next_expected = 1
    open_block = ""
  }
  /^<!-- STORY_169_15_(RETAINED_RED_OUTPUT|RETAINED_REVIEW_EVIDENCE|FROZEN_REVIEWED_MANIFEST):(START|END) -->$/ {
    if ($0 != expected[next_expected]) exit 2
    if ($0 ~ /:START -->$/) {
      if (open_block != "") exit 2
      open_block = $0
    } else {
      if (open_block == "") exit 2
      open_block = ""
    }
    next_expected++
  }
  END {
    if (next_expected != 7 || open_block != "") exit 2
  }
' "$EVIDENCE_ARTIFACT_FILE"; then
  exit 71
fi
extract_story_169_15_payload() {
  MARKER_START="$1"
  MARKER_END="$2"
  MARKER_OUTPUT="$3"
  if ! awk -v marker_start="$MARKER_START" -v marker_end="$MARKER_END" '
    $0 == marker_start { state = 1; next }
    $0 == marker_end { state = 2; next }
    state == 1 { print }
    END { if (state != 2) exit 2 }
  ' "$EVIDENCE_ARTIFACT_FILE" > "$MARKER_OUTPUT"; then
    exit 71
  fi
  test -s "$MARKER_OUTPUT"
}
COMMITTED_RETAINED_RED_OUTPUT_FILE="$STORY_TMP/committed-retained-red-output"
COMMITTED_RETAINED_REVIEW_EVIDENCE_FILE="$STORY_TMP/committed-retained-review-evidence"
COMMITTED_REVIEWED_MANIFEST_FILE="$STORY_TMP/committed-reviewed-manifest"
extract_story_169_15_payload \
  '<!-- STORY_169_15_RETAINED_RED_OUTPUT:START -->' \
  '<!-- STORY_169_15_RETAINED_RED_OUTPUT:END -->' \
  "$COMMITTED_RETAINED_RED_OUTPUT_FILE"
extract_story_169_15_payload \
  '<!-- STORY_169_15_RETAINED_REVIEW_EVIDENCE:START -->' \
  '<!-- STORY_169_15_RETAINED_REVIEW_EVIDENCE:END -->' \
  "$COMMITTED_RETAINED_REVIEW_EVIDENCE_FILE"
extract_story_169_15_payload \
  '<!-- STORY_169_15_FROZEN_REVIEWED_MANIFEST:START -->' \
  '<!-- STORY_169_15_FROZEN_REVIEWED_MANIFEST:END -->' \
  "$COMMITTED_REVIEWED_MANIFEST_FILE"
test -s "$COMMITTED_REVIEWED_MANIFEST_FILE"
if ! CALCULATED_RED_OUTPUT_SHA256="$(shasum -a 256 \
  "$COMMITTED_RETAINED_RED_OUTPUT_FILE" | awk '{ print $1 }')"; then
  exit 71
fi
if ! CALCULATED_REVIEW_EVIDENCE_SHA256="$(shasum -a 256 \
  "$COMMITTED_RETAINED_REVIEW_EVIDENCE_FILE" | awk '{ print $1 }')"; then
  exit 71
fi
test "$CALCULATED_RED_OUTPUT_SHA256" = "$COMMITTED_RED_OUTPUT_SHA256"
test "$CALCULATED_REVIEW_EVIDENCE_SHA256" = \
  "$COMMITTED_REVIEW_EVIDENCE_SHA256"
SENSITIVE_EVIDENCE_PATTERN='(^|[^[:alnum:]_-])(authorization|proxy-authorization|cookie|set-cookie|x-api-key)[[:space:]]*:[[:space:]]*[^[:space:]]|(^|[^[:alnum:]_-])([[:alnum:]][[:alnum:]_-]*[_-])?(authorization|proxy-authorization|cookie|set-cookie|x-api-key|password|passwd|secret|token|auth[_-]?token|access[_-]?token|refresh[_-]?token|client[_-]?secret|api[_-]?key|credentials?|aws[_-]?secret[_-]?access[_-]?key|aws[_-]?access[_-]?key[_-]?id)[[:space:]]*(=|:=|\+=|-=|\?=|&&=|\|\|=)[[:space:]]*[^[:space:]]|(^|[[:space:]])(bearer|basic)[[:space:]]+[[:graph:]]+|-----BEGIN [A-Z ]*PRIVATE KEY-----|[[:alpha:]][[:alnum:]+.-]*://[^[:space:]/:@]+:[^[:space:]@]+@'
PRIVACY_REVIEW_ATTESTATION='STORY_169_15_PRIVACY_REVIEW_DISPOSITION: PASS'
assert_story_169_15_privacy_evidence() {
  PRIVACY_RED_FILE="$1"
  PRIVACY_REVIEW_FILE="$2"
  if LC_ALL=C rg -q -i "$SENSITIVE_EVIDENCE_PATTERN" \
    "$PRIVACY_RED_FILE" "$PRIVACY_REVIEW_FILE"; then
    return 72
  else
    PRIVACY_RG_STATUS="$?"
  fi
  test "$PRIVACY_RG_STATUS" = "1" || return 71
  if ! PRIVACY_ATTESTATION_COUNTS="$(awk -v expected="$PRIVACY_REVIEW_ATTESTATION" '
    index($0, "STORY_169_15_PRIVACY_REVIEW_DISPOSITION:") == 1 {
      total++
      if ($0 == expected) exact++
    }
    END { print (total + 0) "|" (exact + 0) }
  ' "$PRIVACY_REVIEW_FILE")"; then
    return 71
  fi
  test "$PRIVACY_ATTESTATION_COUNTS" = "1|1"
}
assert_story_169_15_reviewed_manifest() {
  ASSERTED_MANIFEST_FILE="$1"
  ASSERTED_NORMALIZED_MANIFEST_FILE="$2"
  ASSERTED_CANONICAL_MANIFEST_FILE="$3"
  if ! sed '/^$/d' "$ASSERTED_MANIFEST_FILE" > \
    "$ASSERTED_NORMALIZED_MANIFEST_FILE"; then
    return 71
  fi
  test -s "$ASSERTED_NORMALIZED_MANIFEST_FILE" || return 71
  if ! LC_ALL=C sort -u "$ASSERTED_NORMALIZED_MANIFEST_FILE" > \
    "$ASSERTED_CANONICAL_MANIFEST_FILE"; then
    return 71
  fi
  cmp -s "$ASSERTED_NORMALIZED_MANIFEST_FILE" \
    "$ASSERTED_CANONICAL_MANIFEST_FILE" || return 71
  if ! ASSERTED_OUTSIDE_ALLOWLIST="$(comm -23 \
    "$ASSERTED_NORMALIZED_MANIFEST_FILE" \
    <(printf '%s\n' "$STORY_STATIC_ALLOWED_MANIFEST"))"; then
    return 71
  fi
  test -z "$ASSERTED_OUTSIDE_ALLOWLIST" || return 71
  if ! ASSERTED_EVIDENCE_PATH="$(comm -12 \
    "$ASSERTED_NORMALIZED_MANIFEST_FILE" \
    <(printf '%s\n' "$STORY_EVIDENCE_ARTIFACT"))"; then
    return 71
  fi
  test -z "$ASSERTED_EVIDENCE_PATH"
}
run_story_169_15_evidence_preflight_synthetics() {
  SYNTHETIC_MANIFEST_FILE="$STORY_TMP/synthetic-manifest"
  SYNTHETIC_NORMALIZED_MANIFEST_FILE="$STORY_TMP/synthetic-normalized-manifest"
  SYNTHETIC_CANONICAL_MANIFEST_FILE="$STORY_TMP/synthetic-canonical-manifest"
  SYNTHETIC_RED_FILE="$STORY_TMP/synthetic-red-evidence"
  SYNTHETIC_REVIEW_FILE="$STORY_TMP/synthetic-review-evidence"

  printf '%s\n' 'src/hooks/useImportStatus.ts' > "$SYNTHETIC_MANIFEST_FILE"
  assert_story_169_15_reviewed_manifest \
    "$SYNTHETIC_MANIFEST_FILE" "$SYNTHETIC_NORMALIZED_MANIFEST_FILE" \
    "$SYNTHETIC_CANONICAL_MANIFEST_FILE" || return 71
  printf '%s\n' 'src/forbidden-story-path.ts' > "$SYNTHETIC_MANIFEST_FILE"
  if assert_story_169_15_reviewed_manifest \
    "$SYNTHETIC_MANIFEST_FILE" "$SYNTHETIC_NORMALIZED_MANIFEST_FILE" \
    "$SYNTHETIC_CANONICAL_MANIFEST_FILE"; then
    return 71
  fi
  printf '\n' > "$SYNTHETIC_MANIFEST_FILE"
  if assert_story_169_15_reviewed_manifest \
    "$SYNTHETIC_MANIFEST_FILE" "$SYNTHETIC_NORMALIZED_MANIFEST_FILE" \
    "$SYNTHETIC_CANONICAL_MANIFEST_FILE"; then
    return 71
  fi
  printf '%s\n' "$STORY_EVIDENCE_ARTIFACT" > "$SYNTHETIC_MANIFEST_FILE"
  if assert_story_169_15_reviewed_manifest \
    "$SYNTHETIC_MANIFEST_FILE" "$SYNTHETIC_NORMALIZED_MANIFEST_FILE" \
    "$SYNTHETIC_CANONICAL_MANIFEST_FILE"; then
    return 71
  fi

  SYNTHETIC_RENAME_REPOSITORY="$STORY_TMP/rename-into-allowed-repository"
  SYNTHETIC_FORBIDDEN_PATH='outside/forbidden-storage-analytics.ts'
  SYNTHETIC_ALLOWED_PATH='src/lib/api/storage-analytics.ts'
  mkdir -p \
    "$SYNTHETIC_RENAME_REPOSITORY/$(dirname "$SYNTHETIC_FORBIDDEN_PATH")" \
    "$SYNTHETIC_RENAME_REPOSITORY/$(dirname "$SYNTHETIC_ALLOWED_PATH")" \
    "$SYNTHETIC_RENAME_REPOSITORY/$(dirname "$STORY_EVIDENCE_ARTIFACT")"
  git -C "$SYNTHETIC_RENAME_REPOSITORY" init -q
  printf '%s\n' 'synthetic identical bytes' > \
    "$SYNTHETIC_RENAME_REPOSITORY/$SYNTHETIC_FORBIDDEN_PATH"
  git -C "$SYNTHETIC_RENAME_REPOSITORY" add -- "$SYNTHETIC_FORBIDDEN_PATH"
  git -C "$SYNTHETIC_RENAME_REPOSITORY" \
    -c user.name='Story 169.15 Synthetic' \
    -c user.email='story-169-15-synthetic@example.invalid' \
    commit -qm 'synthetic forbidden-source base'
  SYNTHETIC_BASE_SHA="$(git -C "$SYNTHETIC_RENAME_REPOSITORY" rev-parse HEAD)"
  printf '%s\n' 'synthetic evidence bytes' > \
    "$SYNTHETIC_RENAME_REPOSITORY/$STORY_EVIDENCE_ARTIFACT"
  git -C "$SYNTHETIC_RENAME_REPOSITORY" add -- "$STORY_EVIDENCE_ARTIFACT"
  git -C "$SYNTHETIC_RENAME_REPOSITORY" \
    -c user.name='Story 169.15 Synthetic' \
    -c user.email='story-169-15-synthetic@example.invalid' \
    commit -qm 'synthetic evidence commit'
  git -C "$SYNTHETIC_RENAME_REPOSITORY" mv -- \
    "$SYNTHETIC_FORBIDDEN_PATH" "$SYNTHETIC_ALLOWED_PATH"

  git -C "$SYNTHETIC_RENAME_REPOSITORY" -c diff.renames=true diff \
    --cached --find-renames=100% --name-only | LC_ALL=C sort -u > \
    "$STORY_TMP/rename-synthetic-vulnerable-staged"
  git -C "$SYNTHETIC_RENAME_REPOSITORY" diff \
    --cached --no-renames --name-only | LC_ALL=C sort -u > \
    "$STORY_TMP/rename-synthetic-hardened-staged"
  test "$(sed -n '1,$p' "$STORY_TMP/rename-synthetic-vulnerable-staged")" = \
    "$SYNTHETIC_ALLOWED_PATH"
  test "$(sed -n '1,$p' "$STORY_TMP/rename-synthetic-hardened-staged")" = \
    "$(printf '%s\n' "$SYNTHETIC_FORBIDDEN_PATH" \
      "$SYNTHETIC_ALLOWED_PATH" | LC_ALL=C sort -u)"
  assert_story_169_15_reviewed_manifest \
    "$STORY_TMP/rename-synthetic-vulnerable-staged" \
    "$STORY_TMP/rename-synthetic-normalized-staged" \
    "$STORY_TMP/rename-synthetic-canonical-staged" || return 71
  if assert_story_169_15_reviewed_manifest \
    "$STORY_TMP/rename-synthetic-hardened-staged" \
    "$STORY_TMP/rename-synthetic-hardened-normalized-staged" \
    "$STORY_TMP/rename-synthetic-hardened-canonical-staged"; then
    return 73
  fi

  git -C "$SYNTHETIC_RENAME_REPOSITORY" \
    -c user.name='Story 169.15 Synthetic' \
    -c user.email='story-169-15-synthetic@example.invalid' \
    commit -qm 'synthetic forbidden rename into allowed path'
  SYNTHETIC_FEATURE_SHA="$(git -C "$SYNTHETIC_RENAME_REPOSITORY" rev-parse HEAD)"
  printf '%s\n' "$STORY_EVIDENCE_ARTIFACT" "$SYNTHETIC_ALLOWED_PATH" | \
    LC_ALL=C sort -u > "$STORY_TMP/rename-synthetic-expected-cumulative"
  printf '%s\n' \
    "$SYNTHETIC_FORBIDDEN_PATH" \
    "$STORY_EVIDENCE_ARTIFACT" \
    "$SYNTHETIC_ALLOWED_PATH" | LC_ALL=C sort -u > \
    "$STORY_TMP/rename-synthetic-expected-hardened-cumulative"
  git -C "$SYNTHETIC_RENAME_REPOSITORY" -c diff.renames=true diff \
    --find-renames=100% --name-only \
    "$SYNTHETIC_BASE_SHA...$SYNTHETIC_FEATURE_SHA" | LC_ALL=C sort -u > \
    "$STORY_TMP/rename-synthetic-vulnerable-committed"
  git -C "$SYNTHETIC_RENAME_REPOSITORY" diff \
    --no-renames --name-only \
    "$SYNTHETIC_BASE_SHA...$SYNTHETIC_FEATURE_SHA" | LC_ALL=C sort -u > \
    "$STORY_TMP/rename-synthetic-hardened-committed"
  cmp -s "$STORY_TMP/rename-synthetic-vulnerable-committed" \
    "$STORY_TMP/rename-synthetic-expected-cumulative"
  cmp -s "$STORY_TMP/rename-synthetic-hardened-committed" \
    "$STORY_TMP/rename-synthetic-expected-hardened-cumulative"
  if cmp -s "$STORY_TMP/rename-synthetic-hardened-committed" \
    "$STORY_TMP/rename-synthetic-expected-cumulative"; then
    return 73
  fi
  git -C "$SYNTHETIC_RENAME_REPOSITORY" -c diff.renames=true diff \
    --find-renames=100% --name-only \
    "$SYNTHETIC_BASE_SHA" "$SYNTHETIC_FEATURE_SHA" | LC_ALL=C sort -u > \
    "$STORY_TMP/rename-synthetic-vulnerable-final"
  git -C "$SYNTHETIC_RENAME_REPOSITORY" diff \
    --no-renames --name-only \
    "$SYNTHETIC_BASE_SHA" "$SYNTHETIC_FEATURE_SHA" | LC_ALL=C sort -u > \
    "$STORY_TMP/rename-synthetic-hardened-final"
  cmp -s "$STORY_TMP/rename-synthetic-vulnerable-final" \
    "$STORY_TMP/rename-synthetic-expected-cumulative"
  cmp -s "$STORY_TMP/rename-synthetic-hardened-final" \
    "$STORY_TMP/rename-synthetic-expected-hardened-cumulative"
  if cmp -s "$STORY_TMP/rename-synthetic-hardened-final" \
    "$STORY_TMP/rename-synthetic-expected-cumulative"; then
    return 73
  fi

  printf '%s\n' \
    'authorization header omitted; api_key field omitted' \
    'synthetic fixture diagnostics contain no retained credentials' > \
    "$SYNTHETIC_RED_FILE"
  printf '%s\n' \
    "$PRIVACY_REVIEW_ATTESTATION" \
    'reviewed sanitized synthetic fixture output' > "$SYNTHETIC_REVIEW_FILE"
  assert_story_169_15_privacy_evidence \
    "$SYNTHETIC_RED_FILE" "$SYNTHETIC_REVIEW_FILE" || return 71
  while IFS= read -r SYNTHETIC_SENSITIVE_LINE; do
    printf '%s\n' "$SYNTHETIC_SENSITIVE_LINE" > "$SYNTHETIC_RED_FILE"
    if assert_story_169_15_privacy_evidence \
      "$SYNTHETIC_RED_FILE" "$SYNTHETIC_REVIEW_FILE"; then
      return 71
    fi
  done <<'SYNTHETIC_SENSITIVE_EVIDENCE'
Authorization: Digest synthetic-value
Proxy-Authorization: Negotiate synthetic-value
Cookie: synthetic-session=value
Set-Cookie: synthetic-session=value
X-API-Key: synthetic-value
Bearer synthetic-value
Basic synthetic-value
  api_key+=synthetic-value
  credentials:=synthetic-value
  aws-secret-access-key?=synthetic-value
  authorization&&=synthetic-value
  cookie||=synthetic-value
  client_secret?=synthetic-value
  password=synthetic-value
  DATABASE_PASSWORD=synthetic-value
  OPENAI_API_KEY:=synthetic-value
  GH_TOKEN+=synthetic-value
  JWT_SECRET-=synthetic-value
  AWS_ACCESS_KEY_ID?=synthetic-value
  X_AUTH_TOKEN&&=synthetic-value
  service-refresh-token||=synthetic-value
  -----BEGIN OPENSSH PRIVATE KEY-----
https://synthetic-user:synthetic-password@example.invalid/path
SYNTHETIC_SENSITIVE_EVIDENCE
  printf '%s\n' 'benign sanitized diagnostics' > "$SYNTHETIC_RED_FILE"
  : > "$SYNTHETIC_REVIEW_FILE"
  if assert_story_169_15_privacy_evidence \
    "$SYNTHETIC_RED_FILE" "$SYNTHETIC_REVIEW_FILE"; then
    return 71
  fi
  printf '%s\n%s\n' \
    "$PRIVACY_REVIEW_ATTESTATION" "$PRIVACY_REVIEW_ATTESTATION" > \
    "$SYNTHETIC_REVIEW_FILE"
  if assert_story_169_15_privacy_evidence \
    "$SYNTHETIC_RED_FILE" "$SYNTHETIC_REVIEW_FILE"; then
    return 71
  fi
  printf '%s\n%s\n' \
    "$PRIVACY_REVIEW_ATTESTATION" \
    'STORY_169_15_PRIVACY_REVIEW_DISPOSITION: FAIL' > \
    "$SYNTHETIC_REVIEW_FILE"
  if assert_story_169_15_privacy_evidence \
    "$SYNTHETIC_RED_FILE" "$SYNTHETIC_REVIEW_FILE"; then
    return 71
  fi
}
run_story_169_15_evidence_preflight_synthetics
if ! assert_story_169_15_privacy_evidence \
  "$COMMITTED_RETAINED_RED_OUTPUT_FILE" \
  "$COMMITTED_RETAINED_REVIEW_EVIDENCE_FILE"; then
  exit 72
fi
printf '%s\n' "$RAW_REVIEWED_MANIFEST" > "$STORY_TMP/exported-reviewed-manifest"
cmp -s "$STORY_TMP/exported-reviewed-manifest" \
  "$COMMITTED_REVIEWED_MANIFEST_FILE"
if ! CALCULATED_MANIFEST_SHA256="$(shasum -a 256 \
  "$COMMITTED_REVIEWED_MANIFEST_FILE" | awk '{print $1}')"; then
  exit 71
fi
test "$CALCULATED_MANIFEST_SHA256" = "$RECORDED_MANIFEST_SHA256"
if ! assert_story_169_15_reviewed_manifest \
  "$COMMITTED_REVIEWED_MANIFEST_FILE" \
  "$STORY_TMP/reviewed-manifest" \
  "$STORY_TMP/canonical-reviewed-manifest"; then
  exit 71
fi
REVIEWED_MANIFEST="$(sed -n '1,$p' "$STORY_TMP/reviewed-manifest")"
test -n "$REVIEWED_MANIFEST"
if test "$EXECUTION_PHASE" = "evidence-preflight"; then
  exit 0
fi
EXPECTED_STORY_MANIFEST="$(printf '%s\n%s\n' \
  "$STORY_EVIDENCE_ARTIFACT" \
  "$REVIEWED_MANIFEST" | sort -u)"
printf '%s\n' "$EXPECTED_STORY_MANIFEST" > \
  "$STORY_TMP/expected-cumulative-story-manifest"
EXPECTED_CUMULATIVE_MANIFEST_SHA256="$(shasum -a 256 \
  "$STORY_TMP/expected-cumulative-story-manifest" | awk '{ print $1 }')"
REVIEW_PASS_1_ID="${STORY_169_15_REVIEW_PASS_1_ID:?export the stable pass-1 reviewer identifier}"
REVIEW_PASS_2_ID="${STORY_169_15_REVIEW_PASS_2_ID:?export the stable pass-2 reviewer identifier}"
EXPECTED_REVIEWED_TREE_SHA="${STORY_169_15_REVIEWED_TREE_SHA:?export the exact tree SHA authenticated by both final reviews}"
STORY_LIFECYCLE_RECORD="$FRONTEND_GIT_COMMON_DIR/story-169-15-lifecycle-record-v1"
STORY_REVIEWED_HEAD_RECORD="$FRONTEND_GIT_COMMON_DIR/story-169-15-reviewed-head-v1"
case "$REVIEW_PASS_1_ID" in
  '' | *[!A-Za-z0-9._/@:-]*) exit 64 ;;
esac
case "$REVIEW_PASS_2_ID" in
  '' | *[!A-Za-z0-9._/@:-]*) exit 64 ;;
esac
test "$REVIEW_PASS_1_ID" != "$REVIEW_PASS_2_ID"
test "${#EXPECTED_REVIEWED_TREE_SHA}" -eq 40
case "$EXPECTED_REVIEWED_TREE_SHA" in
  *[!0-9a-f]* | '') exit 64 ;;
esac
classify_story_169_15_bootstrap_state() {
  BOOTSTRAP_HEAD_STATE="$1"
  BOOTSTRAP_RECORD_STATE="$2"
  BOOTSTRAP_TREE_STATE="$3"
  BOOTSTRAP_MANIFEST_STATE="$4"
  BOOTSTRAP_REVIEWER_STATE="$5"
  BOOTSTRAP_REMOTE_STATE="$6"
  case "$BOOTSTRAP_HEAD_STATE|$BOOTSTRAP_RECORD_STATE|$BOOTSTRAP_TREE_STATE|$BOOTSTRAP_MANIFEST_STATE|$BOOTSTRAP_REVIEWER_STATE|$BOOTSTRAP_REMOTE_STATE" in
    PRE_COMMIT\|ABSENT\|EXACT\|EXACT\|EXACT\|ABSENT)
      printf '%s\n' 'PRE_COMMIT_NO_RECORD'
      ;;
    PRE_COMMIT\|EXACT\|EXACT\|EXACT\|EXACT\|ABSENT)
      printf '%s\n' 'PRE_COMMIT_EXACT_BOOTSTRAP'
      ;;
    POST_COMMIT\|EXACT\|EXACT\|EXACT\|EXACT\|ABSENT)
      printf '%s\n' 'POST_COMMIT_NO_REVIEWED_HEAD_EXACT_BOOTSTRAP'
      ;;
    *) return 1 ;;
  esac
}
test "$(classify_story_169_15_bootstrap_state \
  PRE_COMMIT ABSENT EXACT EXACT EXACT ABSENT)" = "PRE_COMMIT_NO_RECORD"
test "$(classify_story_169_15_bootstrap_state \
  PRE_COMMIT EXACT EXACT EXACT EXACT ABSENT)" = \
  "PRE_COMMIT_EXACT_BOOTSTRAP"
test "$(classify_story_169_15_bootstrap_state \
  POST_COMMIT EXACT EXACT EXACT EXACT ABSENT)" = \
  "POST_COMMIT_NO_REVIEWED_HEAD_EXACT_BOOTSTRAP"
while IFS=' ' read -r REJECTED_HEAD REJECTED_RECORD REJECTED_TREE \
  REJECTED_MANIFEST REJECTED_REVIEWERS REJECTED_REMOTE; do
  if classify_story_169_15_bootstrap_state \
    "$REJECTED_HEAD" "$REJECTED_RECORD" "$REJECTED_TREE" \
    "$REJECTED_MANIFEST" "$REJECTED_REVIEWERS" "$REJECTED_REMOTE" \
    > "$STORY_TMP/rejected-bootstrap-state"; then
    exit 73
  fi
done <<'STORY_169_15_REJECTED_BOOTSTRAP_STATES'
POST_COMMIT FOREIGN EXACT EXACT EXACT ABSENT
POST_COMMIT ABSENT EXACT EXACT EXACT ABSENT
POST_COMMIT ABSENT FOREIGN EXACT EXACT ABSENT
POST_COMMIT ABSENT EXACT FOREIGN EXACT ABSENT
POST_COMMIT ABSENT EXACT EXACT FOREIGN ABSENT
POST_COMMIT ABSENT EXACT EXACT EXACT FOREIGN
STORY_169_15_REJECTED_BOOTSTRAP_STATES
write_story_169_15_review_bootstrap_candidate() {
  REVIEW_BOOTSTRAP_OUTPUT="$1"
  printf '%s\n' \
    'STORY_169_15_REVIEW_BOOTSTRAP_VERSION=1' \
    "STORY_169_15_REPOSITORY=$EXPECTED_FRONTEND_GITHUB_REPOSITORY" \
    "STORY_169_15_EXPECTED_PARENT_SHA=$EVIDENCE_COMMIT_SHA" \
    "STORY_169_15_EXPECTED_TREE_SHA=$EXPECTED_REVIEWED_TREE_SHA" \
    "STORY_169_15_CUMULATIVE_MANIFEST_SHA256=$EXPECTED_CUMULATIVE_MANIFEST_SHA256" \
    "STORY_169_15_EVIDENCE_ARTIFACT_SHA256=$EVIDENCE_ARTIFACT_SHA256" \
    "STORY_169_15_REVIEW_PASS_1_ID=$REVIEW_PASS_1_ID" \
    'STORY_169_15_REVIEW_PASS_1_DISPOSITION=PASS' \
    "STORY_169_15_REVIEW_PASS_2_ID=$REVIEW_PASS_2_ID" \
    'STORY_169_15_REVIEW_PASS_2_DISPOSITION=PASS' \
    > "$REVIEW_BOOTSTRAP_OUTPUT"
  chmod 600 "$REVIEW_BOOTSTRAP_OUTPUT"
  test "$(awk 'END { print NR }' "$REVIEW_BOOTSTRAP_OUTPUT")" = "10"
}
read_story_169_15_review_bootstrap_record() {
  assert_regular_mode_600_record "$STORY_REVIEW_BOOTSTRAP_RECORD"
  if ! REVIEW_BOOTSTRAP_VALUES="$(awk -F= '
    BEGIN {
      order[1] = "STORY_169_15_REVIEW_BOOTSTRAP_VERSION"
      order[2] = "STORY_169_15_REPOSITORY"
      order[3] = "STORY_169_15_EXPECTED_PARENT_SHA"
      order[4] = "STORY_169_15_EXPECTED_TREE_SHA"
      order[5] = "STORY_169_15_CUMULATIVE_MANIFEST_SHA256"
      order[6] = "STORY_169_15_EVIDENCE_ARTIFACT_SHA256"
      order[7] = "STORY_169_15_REVIEW_PASS_1_ID"
      order[8] = "STORY_169_15_REVIEW_PASS_1_DISPOSITION"
      order[9] = "STORY_169_15_REVIEW_PASS_2_ID"
      order[10] = "STORY_169_15_REVIEW_PASS_2_DISPOSITION"
    }
    {
      if (NR > 10 || $1 != order[NR]) exit 2
      value = substr($0, length($1) + 2)
      if (value == "") exit 2
      values[NR] = value
    }
    END {
      if (NR != 10) exit 2
      output = values[1]
      for (line = 2; line <= 10; line++) output = output "|" values[line]
      print output
    }
  ' "$STORY_REVIEW_BOOTSTRAP_RECORD")"; then
    exit 71
  fi
  IFS='|' read -r REVIEW_BOOTSTRAP_VERSION REVIEW_BOOTSTRAP_REPOSITORY \
    REVIEW_BOOTSTRAP_PARENT_SHA REVIEW_BOOTSTRAP_TREE_SHA \
    REVIEW_BOOTSTRAP_MANIFEST_SHA256 REVIEW_BOOTSTRAP_ARTIFACT_SHA256 \
    REVIEW_BOOTSTRAP_REVIEW_1_ID REVIEW_BOOTSTRAP_REVIEW_1_DISPOSITION \
    REVIEW_BOOTSTRAP_REVIEW_2_ID REVIEW_BOOTSTRAP_REVIEW_2_DISPOSITION \
    <<< "$REVIEW_BOOTSTRAP_VALUES"
  test "$REVIEW_BOOTSTRAP_VERSION" = "1"
  test "$REVIEW_BOOTSTRAP_REPOSITORY" = \
    "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
  test "$REVIEW_BOOTSTRAP_PARENT_SHA" = "$EVIDENCE_COMMIT_SHA"
  test "$REVIEW_BOOTSTRAP_TREE_SHA" = "$EXPECTED_REVIEWED_TREE_SHA"
  test "$REVIEW_BOOTSTRAP_MANIFEST_SHA256" = \
    "$EXPECTED_CUMULATIVE_MANIFEST_SHA256"
  test "$REVIEW_BOOTSTRAP_ARTIFACT_SHA256" = "$EVIDENCE_ARTIFACT_SHA256"
  test "$REVIEW_BOOTSTRAP_REVIEW_1_ID" = "$REVIEW_PASS_1_ID"
  test "$REVIEW_BOOTSTRAP_REVIEW_1_DISPOSITION" = "PASS"
  test "$REVIEW_BOOTSTRAP_REVIEW_2_ID" = "$REVIEW_PASS_2_ID"
  test "$REVIEW_BOOTSTRAP_REVIEW_2_DISPOSITION" = "PASS"
  REVIEW_BOOTSTRAP_SHA256="$(shasum -a 256 \
    "$STORY_REVIEW_BOOTSTRAP_RECORD" | awk '{ print $1 }')"
}
publish_or_validate_story_169_15_review_bootstrap_record() {
  test ! -L "$STORY_REVIEW_BOOTSTRAP_RECORD"
  umask 077
  REVIEW_BOOTSTRAP_TMP="$(mktemp \
    "$FRONTEND_GIT_COMMON_DIR/story-169-15-review-bootstrap-v1.tmp.XXXXXX")"
  write_story_169_15_review_bootstrap_candidate "$REVIEW_BOOTSTRAP_TMP"
  if test -e "$STORY_REVIEW_BOOTSTRAP_RECORD"; then
    cmp -s "$STORY_REVIEW_BOOTSTRAP_RECORD" "$REVIEW_BOOTSTRAP_TMP"
    rm -- "$REVIEW_BOOTSTRAP_TMP"
  else
    mv -- "$REVIEW_BOOTSTRAP_TMP" "$STORY_REVIEW_BOOTSTRAP_RECORD"
  fi
  read_story_169_15_review_bootstrap_record
}
read_reviewed_head_record() {
  assert_regular_mode_600_record "$STORY_REVIEWED_HEAD_RECORD"
  if ! REVIEWED_HEAD_VALUES="$(awk -F= '
    BEGIN {
      required["STORY_169_15_REVIEWED_HEAD_RECORD_VERSION"] = 1
      required["STORY_169_15_REPOSITORY"] = 1
      required["STORY_169_15_BASE_SHA"] = 1
      required["STORY_169_15_EVIDENCE_COMMIT_SHA"] = 1
      required["STORY_169_15_FEATURE_SHA"] = 1
      required["STORY_169_15_FEATURE_TREE_SHA"] = 1
      required["STORY_169_15_CUMULATIVE_MANIFEST_SHA256"] = 1
      required["STORY_169_15_REVIEW_PASS_1_ID"] = 1
      required["STORY_169_15_REVIEW_PASS_1_DISPOSITION"] = 1
      required["STORY_169_15_REVIEW_PASS_2_ID"] = 1
      required["STORY_169_15_REVIEW_PASS_2_DISPOSITION"] = 1
    }
    {
      if (!($1 in required) || seen[$1]++) exit 2
      value = substr($0, length($1) + 2)
      if (value == "") exit 2
      values[$1] = value
      lines++
    }
    END {
      if (lines != 11) exit 2
      for (key in required) if (seen[key] != 1) exit 2
      print values["STORY_169_15_REVIEWED_HEAD_RECORD_VERSION"] "|" \
        values["STORY_169_15_REPOSITORY"] "|" \
        values["STORY_169_15_BASE_SHA"] "|" \
        values["STORY_169_15_EVIDENCE_COMMIT_SHA"] "|" \
        values["STORY_169_15_FEATURE_SHA"] "|" \
        values["STORY_169_15_FEATURE_TREE_SHA"] "|" \
        values["STORY_169_15_CUMULATIVE_MANIFEST_SHA256"] "|" \
        values["STORY_169_15_REVIEW_PASS_1_ID"] "|" \
        values["STORY_169_15_REVIEW_PASS_1_DISPOSITION"] "|" \
        values["STORY_169_15_REVIEW_PASS_2_ID"] "|" \
        values["STORY_169_15_REVIEW_PASS_2_DISPOSITION"]
    }
  ' "$STORY_REVIEWED_HEAD_RECORD")"; then
    exit 71
  fi
  IFS='|' read -r REVIEWED_HEAD_VERSION REVIEWED_HEAD_REPOSITORY \
    REVIEWED_HEAD_BASE_SHA REVIEWED_HEAD_EVIDENCE_COMMIT_SHA \
    REVIEWED_HEAD_FEATURE_SHA REVIEWED_HEAD_FEATURE_TREE_SHA \
    REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256 REVIEWED_HEAD_REVIEW_PASS_1_ID \
    REVIEWED_HEAD_REVIEW_PASS_1_DISPOSITION REVIEWED_HEAD_REVIEW_PASS_2_ID \
    REVIEWED_HEAD_REVIEW_PASS_2_DISPOSITION <<< "$REVIEWED_HEAD_VALUES"
  test "$REVIEWED_HEAD_VERSION" = "1"
  test "$REVIEWED_HEAD_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
  test "$REVIEWED_HEAD_BASE_SHA" = "$STORY_BASE_SHA"
  test "$REVIEWED_HEAD_EVIDENCE_COMMIT_SHA" = "$EVIDENCE_COMMIT_SHA"
  test "$REVIEWED_HEAD_FEATURE_TREE_SHA" = "$EXPECTED_REVIEWED_TREE_SHA"
  test "$REVIEWED_HEAD_REVIEW_PASS_1_ID" = "$REVIEW_PASS_1_ID"
  test "$REVIEWED_HEAD_REVIEW_PASS_1_DISPOSITION" = "PASS"
  test "$REVIEWED_HEAD_REVIEW_PASS_2_ID" = "$REVIEW_PASS_2_ID"
  test "$REVIEWED_HEAD_REVIEW_PASS_2_DISPOSITION" = "PASS"
  for SHA_VALUE in \
    "$REVIEWED_HEAD_BASE_SHA" \
    "$REVIEWED_HEAD_EVIDENCE_COMMIT_SHA" \
    "$REVIEWED_HEAD_FEATURE_SHA" \
    "$REVIEWED_HEAD_FEATURE_TREE_SHA"; do
    test "${#SHA_VALUE}" -eq 40
    case "$SHA_VALUE" in
      *[!0-9a-f]* | '') exit 64 ;;
    esac
  done
  test "${#REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256}" -eq 64
  case "$REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256" in
    *[!0-9a-f]* | '') exit 64 ;;
  esac
  test ! -L "$STORY_REVIEW_BOOTSTRAP_RECORD"
  if test -e "$STORY_REVIEW_BOOTSTRAP_RECORD"; then
    read_story_169_15_review_bootstrap_record
    test "$REVIEWED_HEAD_EVIDENCE_COMMIT_SHA" = \
      "$REVIEW_BOOTSTRAP_PARENT_SHA"
    test "$REVIEWED_HEAD_FEATURE_TREE_SHA" = "$REVIEW_BOOTSTRAP_TREE_SHA"
    test "$REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256" = \
      "$REVIEW_BOOTSTRAP_MANIFEST_SHA256"
    test "$REVIEWED_HEAD_REVIEW_PASS_1_ID" = \
      "$REVIEW_BOOTSTRAP_REVIEW_1_ID"
    test "$REVIEWED_HEAD_REVIEW_PASS_2_ID" = \
      "$REVIEW_BOOTSTRAP_REVIEW_2_ID"
  fi
}
publish_reviewed_head_record() {
  umask 077
  if ! STORY_REVIEWED_HEAD_RECORD_TMP="$(mktemp \
    "$FRONTEND_GIT_COMMON_DIR/story-169-15-reviewed-head-v1.tmp.XXXXXX")"; then
    exit 71
  fi
  printf '%s\n' \
    'STORY_169_15_REVIEWED_HEAD_RECORD_VERSION=1' \
    "STORY_169_15_REPOSITORY=$EXPECTED_FRONTEND_GITHUB_REPOSITORY" \
    "STORY_169_15_BASE_SHA=$STORY_BASE_SHA" \
    "STORY_169_15_EVIDENCE_COMMIT_SHA=$EVIDENCE_COMMIT_SHA" \
    "STORY_169_15_FEATURE_SHA=$STORY_FEATURE_SHA" \
    "STORY_169_15_FEATURE_TREE_SHA=$STORY_FEATURE_TREE_SHA" \
    "STORY_169_15_CUMULATIVE_MANIFEST_SHA256=$CUMULATIVE_MANIFEST_SHA256" \
    "STORY_169_15_REVIEW_PASS_1_ID=$REVIEW_PASS_1_ID" \
    'STORY_169_15_REVIEW_PASS_1_DISPOSITION=PASS' \
    "STORY_169_15_REVIEW_PASS_2_ID=$REVIEW_PASS_2_ID" \
    'STORY_169_15_REVIEW_PASS_2_DISPOSITION=PASS' \
    > "$STORY_REVIEWED_HEAD_RECORD_TMP"
  chmod 600 "$STORY_REVIEWED_HEAD_RECORD_TMP"
  test "$(awk 'END { print NR }' "$STORY_REVIEWED_HEAD_RECORD_TMP")" = "11"
  assert_absent_non_symlink_residue "$STORY_REVIEWED_HEAD_RECORD"
  mv -- "$STORY_REVIEWED_HEAD_RECORD_TMP" "$STORY_REVIEWED_HEAD_RECORD"
  read_reviewed_head_record
  test "$REVIEWED_HEAD_FEATURE_SHA" = "$STORY_FEATURE_SHA"
  test "$REVIEWED_HEAD_FEATURE_TREE_SHA" = "$STORY_FEATURE_TREE_SHA"
  test "$REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256" = \
    "$CUMULATIVE_MANIFEST_SHA256"
}
case "$EXECUTION_PHASE" in
  delivery)
    assert_absent_non_symlink_residue "$STORY_REVIEWED_HEAD_RECORD"
    assert_absent_non_symlink_residue "$STORY_LIFECYCLE_RECORD"
    ;;
  publish-recovery)
    read_reviewed_head_record
    STORY_FEATURE_SHA="$REVIEWED_HEAD_FEATURE_SHA"
    STORY_FEATURE_TREE_SHA="$REVIEWED_HEAD_FEATURE_TREE_SHA"
    test ! -L "$STORY_LIFECYCLE_RECORD"
    if test -e "$STORY_LIFECYCLE_RECORD"; then
      assert_regular_mode_600_record "$STORY_LIFECYCLE_RECORD"
      if ! RECOVERY_LIFECYCLE_VALUES="$(awk -F= '
        BEGIN {
          required["STORY_169_15_LIFECYCLE_RECORD_VERSION"] = 1
          required["STORY_169_15_REPOSITORY"] = 1
          required["STORY_169_15_PR_NUMBER"] = 1
          required["STORY_169_15_PR_URL"] = 1
          required["STORY_169_15_FEATURE_SHA"] = 1
          required["STORY_169_15_MERGE_SHA"] = 1
          required["STORY_169_15_REVIEW_PASS_1_ID"] = 1
          required["STORY_169_15_REVIEW_PASS_1_DISPOSITION"] = 1
          required["STORY_169_15_REVIEW_PASS_2_ID"] = 1
          required["STORY_169_15_REVIEW_PASS_2_DISPOSITION"] = 1
        }
        {
          if (!($1 in required) || seen[$1]++) exit 2
          value = substr($0, length($1) + 2)
          if (value == "") exit 2
          values[$1] = value
          lines++
        }
        END {
          if (lines != 10) exit 2
          for (key in required) if (seen[key] != 1) exit 2
          print values["STORY_169_15_LIFECYCLE_RECORD_VERSION"] "|" \
            values["STORY_169_15_REPOSITORY"] "|" \
            values["STORY_169_15_PR_NUMBER"] "|" \
            values["STORY_169_15_PR_URL"] "|" \
            values["STORY_169_15_FEATURE_SHA"] "|" \
            values["STORY_169_15_MERGE_SHA"] "|" \
            values["STORY_169_15_REVIEW_PASS_1_ID"] "|" \
            values["STORY_169_15_REVIEW_PASS_1_DISPOSITION"] "|" \
            values["STORY_169_15_REVIEW_PASS_2_ID"] "|" \
            values["STORY_169_15_REVIEW_PASS_2_DISPOSITION"]
        }
      ' "$STORY_LIFECYCLE_RECORD")"; then
        exit 71
      fi
      IFS='|' read -r RECOVERY_RECORD_VERSION RECOVERY_RECORD_REPOSITORY \
        RECOVERY_RECORD_PR_NUMBER RECOVERY_RECORD_PR_URL \
        RECOVERY_RECORD_FEATURE_SHA RECOVERY_RECORD_MERGE_SHA \
        RECOVERY_RECORD_REVIEW_PASS_1_ID \
        RECOVERY_RECORD_REVIEW_PASS_1_DISPOSITION \
        RECOVERY_RECORD_REVIEW_PASS_2_ID \
        RECOVERY_RECORD_REVIEW_PASS_2_DISPOSITION \
        <<< "$RECOVERY_LIFECYCLE_VALUES"
      test "$RECOVERY_RECORD_VERSION" = "1"
      test "$RECOVERY_RECORD_REPOSITORY" = \
        "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
      case "$RECOVERY_RECORD_PR_NUMBER" in
        *[!0-9]* | '') exit 64 ;;
      esac
      test "$RECOVERY_RECORD_PR_URL" = \
        "https://github.com/$EXPECTED_FRONTEND_GITHUB_REPOSITORY/pull/$RECOVERY_RECORD_PR_NUMBER"
      test "$RECOVERY_RECORD_FEATURE_SHA" = "$STORY_FEATURE_SHA"
      test "$RECOVERY_RECORD_REVIEW_PASS_1_ID" = "$REVIEW_PASS_1_ID"
      test "$RECOVERY_RECORD_REVIEW_PASS_1_DISPOSITION" = "PASS"
      test "$RECOVERY_RECORD_REVIEW_PASS_2_ID" = "$REVIEW_PASS_2_ID"
      test "$RECOVERY_RECORD_REVIEW_PASS_2_DISPOSITION" = "PASS"
      if test "$RECOVERY_RECORD_MERGE_SHA" != "PENDING"; then
        test "${#RECOVERY_RECORD_MERGE_SHA}" -eq 40
        case "$RECOVERY_RECORD_MERGE_SHA" in
          *[!0-9a-f]* | '') exit 64 ;;
        esac
        printf '%s\n' 'finalized Story 169.15 lifecycle record: run mandatory cleanup' >&2
        exit 75
      fi
      RECOVERY_PENDING_RECORD_CANDIDATE="$STORY_TMP/recovery-pending-lifecycle-record"
      printf '%s\n' \
        'STORY_169_15_LIFECYCLE_RECORD_VERSION=1' \
        "STORY_169_15_REPOSITORY=$EXPECTED_FRONTEND_GITHUB_REPOSITORY" \
        "STORY_169_15_PR_NUMBER=$RECOVERY_RECORD_PR_NUMBER" \
        "STORY_169_15_PR_URL=$RECOVERY_RECORD_PR_URL" \
        "STORY_169_15_FEATURE_SHA=$STORY_FEATURE_SHA" \
        'STORY_169_15_MERGE_SHA=PENDING' \
        "STORY_169_15_REVIEW_PASS_1_ID=$REVIEW_PASS_1_ID" \
        'STORY_169_15_REVIEW_PASS_1_DISPOSITION=PASS' \
        "STORY_169_15_REVIEW_PASS_2_ID=$REVIEW_PASS_2_ID" \
        'STORY_169_15_REVIEW_PASS_2_DISPOSITION=PASS' \
        > "$RECOVERY_PENDING_RECORD_CANDIDATE"
      cmp -s "$STORY_LIFECYCLE_RECORD" \
        "$RECOVERY_PENDING_RECORD_CANDIDATE"
      if ! RECOVERY_PENDING_PR_JSON="$(gh pr view \
        "$RECOVERY_RECORD_PR_NUMBER" --repo "$REPOSITORY" \
        --json number,state,baseRefName,headRefName,headRefOid,url,mergeCommit)"; then
        exit 71
      fi
      test "$(printf '%s\n' "$RECOVERY_PENDING_PR_JSON" | jq -r '.number')" = \
        "$RECOVERY_RECORD_PR_NUMBER"
      RECOVERY_PENDING_PR_STATE="$(printf '%s\n' \
        "$RECOVERY_PENDING_PR_JSON" | jq -r '.state')"
      case "$RECOVERY_PENDING_PR_STATE" in
        OPEN) ;;
        MERGED)
          test "$(printf '%s\n' "$RECOVERY_PENDING_PR_JSON" | \
            jq -r '.mergeCommit.oid')" != "null"
          ;;
        *) exit 73 ;;
      esac
      test "$(printf '%s\n' "$RECOVERY_PENDING_PR_JSON" | jq -r '.baseRefName')" = \
        "main"
      test "$(printf '%s\n' "$RECOVERY_PENDING_PR_JSON" | jq -r '.headRefName')" = \
        "$STORY_BRANCH"
      test "$(printf '%s\n' "$RECOVERY_PENDING_PR_JSON" | jq -r '.headRefOid')" = \
        "$STORY_FEATURE_SHA"
      test "$(printf '%s\n' "$RECOVERY_PENDING_PR_JSON" | jq -r '.url')" = \
        "$RECOVERY_RECORD_PR_URL"
    fi
    ;;
esac
test -n "$REVIEWED_MANIFEST"
case "$EXECUTION_PHASE" in
  delivery)
    if test "$DELIVERY_BOOTSTRAP_STATE" = "PRE_COMMIT_NO_RECORD"; then
      if ! UNSTAGED_CHANGED_PATHS="$(git -C "$STORY_WORKTREE" diff --no-renames --name-only)"; then
        exit 71
      fi
      if ! UNTRACKED_CHANGED_PATHS="$(git -C "$STORY_WORKTREE" ls-files --others --exclude-standard)"; then
        exit 71
      fi
      ACTUAL_CHANGED_MANIFEST="$(printf '%s\n%s\n%s\n' \
        "$CACHED_BEFORE_STAGING" \
        "$UNSTAGED_CHANGED_PATHS" \
        "$UNTRACKED_CHANGED_PATHS" | sed '/^$/d' | sort -u)"
      test -n "$ACTUAL_CHANGED_MANIFEST"
      test "$ACTUAL_CHANGED_MANIFEST" = "$REVIEWED_MANIFEST"
    else
      test "$DELIVERY_BOOTSTRAP_STATE" = "POST_COMMIT_NO_RECORD"
      UNSTAGED_CHANGED_PATHS=""
      UNTRACKED_CHANGED_PATHS=""
      git -C "$STORY_WORKTREE" diff --check \
        "$EVIDENCE_COMMIT_SHA" "$CURRENT_HEAD_SHA"
    fi
    ;;
  publish-recovery)
    UNSTAGED_CHANGED_PATHS=""
    UNTRACKED_CHANGED_PATHS=""
    git -C "$STORY_WORKTREE" diff --check "$STORY_BASE_SHA" "$CURRENT_HEAD_SHA"
    ;;
esac
EXPECTED_STORY_MANIFEST="$(printf '%s\n%s\n' \
  "$STORY_EVIDENCE_ARTIFACT" \
  "$REVIEWED_MANIFEST" | sort -u)"
printf '%s\n' "$EXPECTED_STORY_MANIFEST" > \
  "$STORY_TMP/expected-cumulative-story-manifest"
if ! COMMITTED_STORY_PATHS="$(git -C "$STORY_WORKTREE" diff --no-renames --name-only \
  "$STORY_BASE_SHA...$CURRENT_HEAD_SHA" | sort -u)"; then
  exit 71
fi
CUMULATIVE_STORY_MANIFEST="$(printf '%s\n%s\n%s\n' \
  "$COMMITTED_STORY_PATHS" \
  "$UNSTAGED_CHANGED_PATHS" \
  "$UNTRACKED_CHANGED_PATHS" | sed '/^$/d' | sort -u)"
test "$CUMULATIVE_STORY_MANIFEST" = "$EXPECTED_STORY_MANIFEST"
case "$EXECUTION_PHASE" in
  delivery)
    if test "$DELIVERY_BOOTSTRAP_STATE" = "PRE_COMMIT_NO_RECORD"; then
      printf '%s\n' "$REVIEWED_MANIFEST"
      while IFS= read -r STORY_PATH; do
        git -C "$STORY_WORKTREE" add -- "$STORY_PATH"
      done <<< "$REVIEWED_MANIFEST"
      git -C "$STORY_WORKTREE" diff --cached --check
      if ! CACHED_MANIFEST="$(git -C "$STORY_WORKTREE" diff --cached --no-renames --name-only | sort)"; then
        exit 71
      fi
      test "$CACHED_MANIFEST" = "$REVIEWED_MANIFEST"
      if ! ACTUAL_REVIEWED_TREE_SHA="$(git -C "$STORY_WORKTREE" \
        write-tree)"; then
        exit 71
      fi
      test "$ACTUAL_REVIEWED_TREE_SHA" = "$EXPECTED_REVIEWED_TREE_SHA"
      publish_or_validate_story_169_15_review_bootstrap_record
      git -C "$STORY_WORKTREE" commit -m "fix(analytics): align paid-storage import boundary" -m "Implement Story 169.15 against the merged Story 169.14 request/status/result contract with polling, normalization, failure-detail, contract-test, and cleanup evidence."
    else
      test "$DELIVERY_BOOTSTRAP_STATE" = "POST_COMMIT_NO_RECORD"
      assert_absent_non_symlink_residue "$STORY_REVIEWED_HEAD_RECORD"
      assert_absent_non_symlink_residue "$STORY_LIFECYCLE_RECORD"
      read_story_169_15_review_bootstrap_record
      if ! REMOTE_BOOTSTRAP_BRANCH="$(git -C "$STORY_WORKTREE" ls-remote \
        --heads "$ORIGIN_PUSH_URL" "refs/heads/$STORY_BRANCH")"; then
        exit 71
      fi
      test -z "$REMOTE_BOOTSTRAP_BRANCH"
    fi
    if ! STORY_FEATURE_SHA="$(git -C "$STORY_WORKTREE" rev-parse HEAD)"; then
      exit 71
    fi
    if ! STORY_FEATURE_TREE_SHA="$(git -C "$STORY_WORKTREE" rev-parse \
      "$STORY_FEATURE_SHA^{tree}")"; then
      exit 71
    fi
    if ! STORY_FEATURE_AND_PARENTS="$(git -C "$STORY_WORKTREE" rev-list \
      --parents -n 1 "$STORY_FEATURE_SHA")"; then
      exit 71
    fi
    IFS=' ' read -r STORY_FEATURE_OID STORY_FEATURE_PARENT_SHA \
      STORY_FEATURE_EXTRA_PARENT <<< "$STORY_FEATURE_AND_PARENTS"
    test "$STORY_FEATURE_OID" = "$STORY_FEATURE_SHA"
    test "$STORY_FEATURE_PARENT_SHA" = "$EVIDENCE_COMMIT_SHA"
    test -z "$STORY_FEATURE_EXTRA_PARENT"
    test "$STORY_FEATURE_TREE_SHA" = "$EXPECTED_REVIEWED_TREE_SHA"
    if ! (
      set -o pipefail
      git -C "$STORY_WORKTREE" diff --no-renames --name-only \
        "$STORY_BASE_SHA" "$STORY_FEATURE_SHA" | LC_ALL=C sort -u > \
        "$STORY_TMP/final-cumulative-story-manifest"
    ); then
      exit 71
    fi
    cmp -s "$STORY_TMP/final-cumulative-story-manifest" \
      "$STORY_TMP/expected-cumulative-story-manifest"
    if ! CUMULATIVE_MANIFEST_SHA256="$(shasum -a 256 \
      "$STORY_TMP/final-cumulative-story-manifest" | awk '{ print $1 }')"; then
      exit 71
    fi
    test "$CUMULATIVE_MANIFEST_SHA256" = \
      "$EXPECTED_CUMULATIVE_MANIFEST_SHA256"
    publish_reviewed_head_record
    ;;
  publish-recovery)
    test "$CURRENT_HEAD_SHA" = "$STORY_FEATURE_SHA"
    if ! CURRENT_FEATURE_TREE_SHA="$(git -C "$STORY_WORKTREE" rev-parse \
      "$CURRENT_HEAD_SHA^{tree}")"; then
      exit 71
    fi
    test "$CURRENT_FEATURE_TREE_SHA" = "$STORY_FEATURE_TREE_SHA"
    if ! STORY_FEATURE_AND_PARENTS="$(git -C "$STORY_WORKTREE" rev-list \
      --parents -n 1 "$STORY_FEATURE_SHA")"; then
      exit 71
    fi
    IFS=' ' read -r STORY_FEATURE_OID STORY_FEATURE_PARENT_SHA \
      STORY_FEATURE_EXTRA_PARENT <<< "$STORY_FEATURE_AND_PARENTS"
    test "$STORY_FEATURE_OID" = "$STORY_FEATURE_SHA"
    test "$STORY_FEATURE_PARENT_SHA" = "$EVIDENCE_COMMIT_SHA"
    test -z "$STORY_FEATURE_EXTRA_PARENT"
    git -C "$STORY_WORKTREE" merge-base --is-ancestor \
      "$STORY_BASE_SHA" "$STORY_FEATURE_SHA"
    if ! (
      set -o pipefail
      git -C "$STORY_WORKTREE" diff --no-renames --name-only \
        "$STORY_BASE_SHA" "$STORY_FEATURE_SHA" | LC_ALL=C sort -u > \
        "$STORY_TMP/final-cumulative-story-manifest"
    ); then
      exit 71
    fi
    cmp -s "$STORY_TMP/final-cumulative-story-manifest" \
      "$STORY_TMP/expected-cumulative-story-manifest"
    if ! CUMULATIVE_MANIFEST_SHA256="$(shasum -a 256 \
      "$STORY_TMP/final-cumulative-story-manifest" | awk '{ print $1 }')"; then
      exit 71
    fi
    test "$CUMULATIVE_MANIFEST_SHA256" = \
      "$REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256"
    ;;
esac
read_reviewed_head_record
test ! -L "$STORY_REVIEW_BOOTSTRAP_RECORD"
if test -e "$STORY_REVIEW_BOOTSTRAP_RECORD"; then
  read_story_169_15_review_bootstrap_record
  VERIFIED_REVIEW_BOOTSTRAP_SHA256="$REVIEW_BOOTSTRAP_SHA256"
  test "$(shasum -a 256 "$STORY_REVIEW_BOOTSTRAP_RECORD" | \
    awk '{ print $1 }')" = "$VERIFIED_REVIEW_BOOTSTRAP_SHA256"
  rm -- "$STORY_REVIEW_BOOTSTRAP_RECORD"
fi
assert_absent_non_symlink_residue "$STORY_REVIEW_BOOTSTRAP_RECORD"
if ! REMOTE_STORY_BRANCH_BEFORE_PUSH="$(git -C "$STORY_WORKTREE" ls-remote \
  --heads "$ORIGIN_PUSH_URL" "refs/heads/$STORY_BRANCH")"; then
  exit 71
fi
EXPECTED_REMOTE_STORY_BRANCH="$(printf '%s\trefs/heads/%s' \
  "$STORY_FEATURE_SHA" "$STORY_BRANCH")"
# The empty expected value is an atomic must-not-exist precondition for an
# ordinary new-ref creation; it cannot update or rewrite an existing ref.
case "$EXECUTION_PHASE" in
  delivery)
    test -z "$REMOTE_STORY_BRANCH_BEFORE_PUSH"
    git -C "$STORY_WORKTREE" push \
      --force-with-lease="refs/heads/$STORY_BRANCH:" \
      "$ORIGIN_PUSH_URL" \
      "$STORY_FEATURE_SHA:refs/heads/$STORY_BRANCH"
    ;;
  publish-recovery)
    case "$REMOTE_STORY_BRANCH_BEFORE_PUSH" in
      '')
        git -C "$STORY_WORKTREE" push \
          --force-with-lease="refs/heads/$STORY_BRANCH:" \
          "$ORIGIN_PUSH_URL" \
          "$STORY_FEATURE_SHA:refs/heads/$STORY_BRANCH"
        ;;
      "$EXPECTED_REMOTE_STORY_BRANCH") ;;
      *) exit 73 ;;
    esac
    ;;
esac
if ! REMOTE_STORY_BRANCH="$(git -C "$STORY_WORKTREE" ls-remote --heads \
  "$ORIGIN_PUSH_URL" "refs/heads/$STORY_BRANCH")"; then
  exit 71
fi
test "$REMOTE_STORY_BRANCH" = "$EXPECTED_REMOTE_STORY_BRANCH"
REPOSITORY_OWNER="${REPOSITORY%%/*}"
assert_story_rest_pull_state_normalization() {
  if ! STORY_NORMALIZED_STATE_SYNTHETICS="$(printf '%s\n' \
    '[{"state":"open","merged_at":null},{"state":"closed","merged_at":"2026-08-26T00:00:00Z"},{"state":"closed","merged_at":null},{"state":"draft","merged_at":null}]' | \
    jq -r '.[] | if .state == "open" then "OPEN" elif .state == "closed" and .merged_at != null then "MERGED" elif .state == "closed" and .merged_at == null then "CLOSED_UNMERGED" else "INVALID" end')"; then
    exit 71
  fi
  test "$STORY_NORMALIZED_STATE_SYNTHETICS" = "$(printf '%s\n' \
    'OPEN' 'MERGED' 'CLOSED_UNMERGED' 'INVALID')"
}
assert_story_rest_pull_state_normalization
list_exact_story_prs() {
  gh api --method GET --paginate "repos/$REPOSITORY/pulls" \
    -f state=all \
    -f head="$REPOSITORY_OWNER:$STORY_BRANCH" \
    -f per_page=100 \
    --jq '.[] | [.number, (if .state == "open" then "OPEN" elif .state == "closed" and .merged_at != null then "MERGED" elif .state == "closed" and .merged_at == null then "CLOSED_UNMERGED" else "INVALID" end), .base.ref, .head.ref, .head.sha, .html_url, (.merged_at // "null")] | @tsv'
}
if ! EXACT_STORY_PR_ROWS="$(list_exact_story_prs)"; then
  exit 71
fi
EXACT_STORY_PR_COUNT="$(printf '%s\n' "$EXACT_STORY_PR_ROWS" | \
  awk 'NF { count++ } END { print count + 0 }')"
case "$EXACT_STORY_PR_COUNT" in
  0)
    PR_CREATE_STATUS=0
    gh pr create --repo "$REPOSITORY" --base main --head "$STORY_BRANCH" \
      --title "Story 169.15: align paid-storage import boundary" \
      --body "Implements the approved shared frontend paid-storage contract boundary against merged Story 169.14 with RED-first tests, local validation, two independent reviews, and mandatory cleanup. No route UI, partial-success feature, deploy, production, direct-main, or force-push authority." \
      >/dev/null || PR_CREATE_STATUS=$?
    if ! EXACT_STORY_PR_ROWS="$(list_exact_story_prs)"; then
      exit 71
    fi
    EXACT_STORY_PR_COUNT="$(printf '%s\n' "$EXACT_STORY_PR_ROWS" | \
      awk 'NF { count++ } END { print count + 0 }')"
    if test "$EXACT_STORY_PR_COUNT" != "1"; then
      test "$PR_CREATE_STATUS" = "0"
      exit 71
    fi
    ;;
  1) ;;
  *) exit 65 ;;
esac
IFS=$'\t' read -r STORY_PR_NUMBER STORY_PR_STATE STORY_PR_BASE_BRANCH \
  STORY_PR_HEAD_BRANCH STORY_PR_HEAD_SHA STORY_PR_URL STORY_PR_MERGED_AT \
  <<< "$EXACT_STORY_PR_ROWS"
case "$STORY_PR_NUMBER" in
  *[!0-9]* | '') exit 64 ;;
esac
case "$STORY_PR_STATE" in
  OPEN) test "$STORY_PR_MERGED_AT" = "null" ;;
  MERGED) test "$STORY_PR_MERGED_AT" != "null" ;;
  CLOSED_UNMERGED | *) exit 73 ;;
esac
test "$STORY_PR_BASE_BRANCH" = "main"
test "$STORY_PR_HEAD_BRANCH" = "$STORY_BRANCH"
test "$STORY_PR_HEAD_SHA" = "$STORY_FEATURE_SHA"
test "$STORY_PR_URL" = \
  "https://github.com/$EXPECTED_FRONTEND_GITHUB_REPOSITORY/pull/$STORY_PR_NUMBER"
test ! -L "$STORY_LIFECYCLE_RECORD"
if test -e "$STORY_LIFECYCLE_RECORD"; then
  test "$STORY_PR_NUMBER" = "$RECOVERY_RECORD_PR_NUMBER"
  test "$STORY_PR_URL" = "$RECOVERY_RECORD_PR_URL"
fi
write_lifecycle_record_candidate() {
  LIFECYCLE_MERGE_SHA="$1"
  LIFECYCLE_OUTPUT_FILE="$2"
  case "$LIFECYCLE_MERGE_SHA" in
    PENDING) ;;
    *)
      test "$(expr "$LIFECYCLE_MERGE_SHA" : '.*')" = "40"
      case "$LIFECYCLE_MERGE_SHA" in
        *[!0-9a-f]* | '') exit 64 ;;
      esac
      ;;
  esac
  if ! printf '%s\n' \
    'STORY_169_15_LIFECYCLE_RECORD_VERSION=1' \
    "STORY_169_15_REPOSITORY=$EXPECTED_FRONTEND_GITHUB_REPOSITORY" \
    "STORY_169_15_PR_NUMBER=$STORY_PR_NUMBER" \
    "STORY_169_15_PR_URL=$STORY_PR_URL" \
    "STORY_169_15_FEATURE_SHA=$STORY_FEATURE_SHA" \
    "STORY_169_15_MERGE_SHA=$LIFECYCLE_MERGE_SHA" \
    "STORY_169_15_REVIEW_PASS_1_ID=$REVIEW_PASS_1_ID" \
    'STORY_169_15_REVIEW_PASS_1_DISPOSITION=PASS' \
    "STORY_169_15_REVIEW_PASS_2_ID=$REVIEW_PASS_2_ID" \
    'STORY_169_15_REVIEW_PASS_2_DISPOSITION=PASS' \
    > "$LIFECYCLE_OUTPUT_FILE"; then
    exit 71
  fi
  chmod 600 "$LIFECYCLE_OUTPUT_FILE"
  test "$(awk 'END { print NR }' "$LIFECYCLE_OUTPUT_FILE")" = "10"
}
create_or_validate_pending_lifecycle_record() {
  umask 077
  if ! PENDING_LIFECYCLE_RECORD_TMP="$(mktemp \
    "$FRONTEND_GIT_COMMON_DIR/story-169-15-lifecycle-record-v1.tmp.XXXXXX")"; then
    exit 71
  fi
  write_lifecycle_record_candidate "PENDING" "$PENDING_LIFECYCLE_RECORD_TMP"
  test ! -L "$STORY_LIFECYCLE_RECORD"
  if test -e "$STORY_LIFECYCLE_RECORD"; then
    assert_regular_mode_600_record "$STORY_LIFECYCLE_RECORD"
    cmp -s "$STORY_LIFECYCLE_RECORD" "$PENDING_LIFECYCLE_RECORD_TMP"
    rm -- "$PENDING_LIFECYCLE_RECORD_TMP"
  else
    assert_absent_non_symlink_residue "$STORY_LIFECYCLE_RECORD"
    mv -- "$PENDING_LIFECYCLE_RECORD_TMP" "$STORY_LIFECYCLE_RECORD"
  fi
  assert_regular_mode_600_record "$STORY_LIFECYCLE_RECORD"
}
finalize_exact_pending_lifecycle_record() {
  FINAL_MERGE_SHA="$1"
  umask 077
  PENDING_LIFECYCLE_RECORD_TMP="$(mktemp \
    "$FRONTEND_GIT_COMMON_DIR/story-169-15-lifecycle-record-v1.pending.XXXXXX")"
  FINAL_LIFECYCLE_RECORD_TMP="$(mktemp \
    "$FRONTEND_GIT_COMMON_DIR/story-169-15-lifecycle-record-v1.final.XXXXXX")"
  write_lifecycle_record_candidate "PENDING" "$PENDING_LIFECYCLE_RECORD_TMP"
  write_lifecycle_record_candidate "$FINAL_MERGE_SHA" "$FINAL_LIFECYCLE_RECORD_TMP"
  assert_regular_mode_600_record "$STORY_LIFECYCLE_RECORD"
  cmp -s "$STORY_LIFECYCLE_RECORD" "$PENDING_LIFECYCLE_RECORD_TMP"
  mv -- "$FINAL_LIFECYCLE_RECORD_TMP" "$STORY_LIFECYCLE_RECORD"
  rm -- "$PENDING_LIFECYCLE_RECORD_TMP"
  test "$(stat -f '%Lp' "$STORY_LIFECYCLE_RECORD")" = "600"
  cmp -s "$STORY_LIFECYCLE_RECORD" \
    "$STORY_TMP/final-lifecycle-record-verification"
}
# Persist the exact reviewed PR/head before the irreversible operation. PENDING is the
# only non-SHA merge sentinel accepted by recovery.
create_or_validate_pending_lifecycle_record
if test "$STORY_PR_STATE" = "OPEN"; then
  gh pr merge "$STORY_PR_NUMBER" --repo "$REPOSITORY" --merge \
    --match-head-commit "$STORY_FEATURE_SHA"
else
  test "$STORY_PR_STATE" = "MERGED"
fi
if ! MERGED_PR_JSON="$(gh pr view "$STORY_PR_NUMBER" --repo "$REPOSITORY" \
  --json number,state,baseRefName,headRefName,headRefOid,url,mergeCommit)"; then
  exit 71
fi
MERGED_PR_NUMBER="$(printf '%s\n' "$MERGED_PR_JSON" | jq -r '.number')"
MERGED_PR_STATE="$(printf '%s\n' "$MERGED_PR_JSON" | jq -r '.state')"
MERGED_PR_BASE="$(printf '%s\n' "$MERGED_PR_JSON" | jq -r '.baseRefName')"
MERGED_PR_HEAD="$(printf '%s\n' "$MERGED_PR_JSON" | jq -r '.headRefName')"
MERGED_PR_HEAD_SHA="$(printf '%s\n' "$MERGED_PR_JSON" | jq -r '.headRefOid')"
MERGED_PR_URL="$(printf '%s\n' "$MERGED_PR_JSON" | jq -r '.url')"
STORY_MERGE_SHA="$(printf '%s\n' "$MERGED_PR_JSON" | jq -r '.mergeCommit.oid')"
test "$MERGED_PR_NUMBER" = "$STORY_PR_NUMBER"
test "$MERGED_PR_STATE" = "MERGED"
test "$MERGED_PR_BASE" = "main"
test "$MERGED_PR_HEAD" = "$STORY_BRANCH"
test "$MERGED_PR_HEAD_SHA" = "$STORY_FEATURE_SHA"
test "$MERGED_PR_URL" = "$STORY_PR_URL"
test "$(expr "$STORY_FEATURE_SHA" : '.*')" = "40"
case "$STORY_FEATURE_SHA" in
  *[!0-9a-f]* | '') exit 64 ;;
esac
# Atomically finalize only the exact matching PENDING record after exact merged-PR proof.
write_lifecycle_record_candidate "$STORY_MERGE_SHA" \
  "$STORY_TMP/final-lifecycle-record-verification"
finalize_exact_pending_lifecycle_record "$STORY_MERGE_SHA"
```

## Mandatory cleanup

```bash
set -euo pipefail

FRONTEND_REPOSITORY="/Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend"
EXPECTED_FRONTEND_GITHUB_REPOSITORY="salacoste/wb-erp-system-daytona-FE"
STORY_BRANCH="cdx/epic-169-story-15-storage-import-boundary"
STORY_WORKTREE="/private/tmp/wb-repricer-fe-169-15-storage-import-boundary"
assert_story_worktree_identity() {
  if ! ASSERTED_TOPLEVEL="$(git -C "$STORY_WORKTREE" rev-parse --show-toplevel)"; then
    exit 71
  fi
  if ! ASSERTED_BRANCH="$(git -C "$STORY_WORKTREE" branch --show-current)"; then
    exit 71
  fi
  test "$ASSERTED_TOPLEVEL" = "$STORY_WORKTREE"
  test "$ASSERTED_BRANCH" = "$STORY_BRANCH"
}

if ! FRONTEND_TOPLEVEL="$(git -C "$FRONTEND_REPOSITORY" rev-parse --show-toplevel)"; then
  exit 71
fi
test "$FRONTEND_TOPLEVEL" = "$FRONTEND_REPOSITORY"
if ! FRONTEND_GIT_COMMON_DIR="$(git -C "$FRONTEND_REPOSITORY" rev-parse --path-format=absolute --git-common-dir)"; then
  exit 71
fi
test -d "$FRONTEND_GIT_COMMON_DIR"
STORY_LIFECYCLE_RECORD="$FRONTEND_GIT_COMMON_DIR/story-169-15-lifecycle-record-v1"
STORY_REVIEWED_HEAD_RECORD="$FRONTEND_GIT_COMMON_DIR/story-169-15-reviewed-head-v1"
STORY_CLEANUP_TRANSACTION_RECORD="$FRONTEND_GIT_COMMON_DIR/story-169-15-cleanup-transaction-v1"
STORY_CLEANUP_TMP="$(mktemp -d "${TMPDIR:-/tmp}/story-169-15-cleanup.XXXXXX")"
trap 'rm -rf "$STORY_CLEANUP_TMP"' EXIT
assert_failed_git_absence_query_blocks_cleanup_retirement() {
  local query_kind="$1"
  local deletion_marker="$2"
  local non_repository="$STORY_CLEANUP_TMP/non-repository-$query_kind"
  mkdir -p "$non_repository"
  if (
    set -euo pipefail
    case "$query_kind" in
      branch)
        if ! QUERY_OUTPUT="$(git -C "$non_repository" \
          branch --list synthetic 2>/dev/null)"; then
          exit 71
        fi
        ;;
      ls-remote)
        if ! QUERY_OUTPUT="$(git -C "$non_repository" ls-remote --heads \
          "$non_repository/missing-remote.git" \
          refs/heads/synthetic 2>/dev/null)"; then
          exit 71
        fi
        ;;
      *) exit 64 ;;
    esac
    test -z "$QUERY_OUTPUT"
    : > "$deletion_marker"
  ); then
    exit 73
  fi
  test ! -e "$deletion_marker"
}
assert_failed_git_absence_query_blocks_cleanup_retirement branch \
  "$STORY_CLEANUP_TMP/source-record-deletion-authorized"
assert_failed_git_absence_query_blocks_cleanup_retirement ls-remote \
  "$STORY_CLEANUP_TMP/cleanup-complete-publication-authorized"
assert_regular_mode_600_record() {
  local record_path="$1"
  test ! -L "$record_path" || return 74
  test -f "$record_path" || return 74
  test "$(stat -f '%Lp' "$record_path")" = "600" || return 74
}
assert_absent_non_symlink_residue() {
  local residue_path="$1"
  test ! -L "$residue_path" || return 74
  test ! -e "$residue_path" || return 74
}
SYNTHETIC_REGULAR_RECORD="$STORY_CLEANUP_TMP/regular-mode-600-record"
SYNTHETIC_LIVE_RECORD_SYMLINK="$STORY_CLEANUP_TMP/live-record-symlink"
printf '%s\n' 'synthetic-authority' > "$SYNTHETIC_REGULAR_RECORD"
chmod 600 "$SYNTHETIC_REGULAR_RECORD"
assert_regular_mode_600_record "$SYNTHETIC_REGULAR_RECORD"
ln -s "$SYNTHETIC_REGULAR_RECORD" "$SYNTHETIC_LIVE_RECORD_SYMLINK"
if assert_regular_mode_600_record "$SYNTHETIC_LIVE_RECORD_SYMLINK"; then
  exit 73
fi
rm -- "$SYNTHETIC_LIVE_RECORD_SYMLINK" "$SYNTHETIC_REGULAR_RECORD"
SYNTHETIC_ABSENT_WORKTREE="$STORY_CLEANUP_TMP/absent-worktree"
assert_absent_non_symlink_residue "$SYNTHETIC_ABSENT_WORKTREE"
ln -s "$STORY_CLEANUP_TMP/missing-worktree-target" "$SYNTHETIC_ABSENT_WORKTREE"
if assert_absent_non_symlink_residue "$SYNTHETIC_ABSENT_WORKTREE"; then
  exit 73
fi
SYNTHETIC_SOURCE_AUTHORITY="$STORY_CLEANUP_TMP/source-authority"
SYNTHETIC_CLEANUP_PROOF="$STORY_CLEANUP_TMP/cleanup-complete-proof"
printf '%s\n' 'retained-authority' > "$SYNTHETIC_SOURCE_AUTHORITY"
if assert_absent_non_symlink_residue "$SYNTHETIC_ABSENT_WORKTREE" &&
  printf '%s\n' 'PASS' > "$SYNTHETIC_CLEANUP_PROOF" &&
  rm -- "$SYNTHETIC_SOURCE_AUTHORITY"; then
  exit 73
fi
test -f "$SYNTHETIC_SOURCE_AUTHORITY"
assert_absent_non_symlink_residue "$SYNTHETIC_CLEANUP_PROOF"
rm -- "$SYNTHETIC_ABSENT_WORKTREE" "$SYNTHETIC_SOURCE_AUTHORITY"
assert_cleanup_transaction_source() {
  TRANSACTION_SOURCE_PATH="$1"
  TRANSACTION_SOURCE_SHA256="$2"
  if test "${#TRANSACTION_SOURCE_SHA256}" != "64"; then
    return 74
  fi
  case "$TRANSACTION_SOURCE_SHA256" in
    *[!0-9a-f]*) return 74 ;;
  esac
  if test -L "$TRANSACTION_SOURCE_PATH"; then
    return 74
  fi
  if test -e "$TRANSACTION_SOURCE_PATH"; then
    test -f "$TRANSACTION_SOURCE_PATH" || return 74
    test "$(stat -f '%Lp' "$TRANSACTION_SOURCE_PATH")" = "600" || return 74
    test "$(shasum -a 256 "$TRANSACTION_SOURCE_PATH" | \
      awk '{ print $1 }')" = "$TRANSACTION_SOURCE_SHA256" || return 74
  fi
  return 0
}
printf '%s\n' 'exact-source' > "$STORY_CLEANUP_TMP/source-state-synthetic"
chmod 600 "$STORY_CLEANUP_TMP/source-state-synthetic"
SYNTHETIC_SOURCE_SHA256="$(shasum -a 256 \
  "$STORY_CLEANUP_TMP/source-state-synthetic" | awk '{ print $1 }')"
SHORT_SYNTHETIC_SOURCE_SHA256="${SYNTHETIC_SOURCE_SHA256%?}"
NONLOWERHEX_SYNTHETIC_SOURCE_SHA256="${SYNTHETIC_SOURCE_SHA256%?}G"
assert_cleanup_transaction_source \
  "$STORY_CLEANUP_TMP/source-state-synthetic" "$SYNTHETIC_SOURCE_SHA256"
for MALFORMED_SYNTHETIC_SOURCE_SHA256 in \
  "$SHORT_SYNTHETIC_SOURCE_SHA256" \
  "$NONLOWERHEX_SYNTHETIC_SOURCE_SHA256"; do
  if assert_cleanup_transaction_source \
    "$STORY_CLEANUP_TMP/source-state-synthetic" \
    "$MALFORMED_SYNTHETIC_SOURCE_SHA256"; then
    exit 73
  fi
done
rm -- "$STORY_CLEANUP_TMP/source-state-synthetic"
assert_cleanup_transaction_source \
  "$STORY_CLEANUP_TMP/source-state-synthetic" "$SYNTHETIC_SOURCE_SHA256"
for MALFORMED_SYNTHETIC_SOURCE_SHA256 in \
  "$SHORT_SYNTHETIC_SOURCE_SHA256" \
  "$NONLOWERHEX_SYNTHETIC_SOURCE_SHA256"; do
  if assert_cleanup_transaction_source \
    "$STORY_CLEANUP_TMP/source-state-synthetic" \
    "$MALFORMED_SYNTHETIC_SOURCE_SHA256"; then
    exit 73
  fi
done
printf '%s\n' 'foreign-source' > "$STORY_CLEANUP_TMP/source-state-synthetic"
chmod 600 "$STORY_CLEANUP_TMP/source-state-synthetic"
if assert_cleanup_transaction_source \
  "$STORY_CLEANUP_TMP/source-state-synthetic" "$SYNTHETIC_SOURCE_SHA256"; then
  exit 73
fi
rm -- "$STORY_CLEANUP_TMP/source-state-synthetic"
printf '%s\n' 'exact-source' > "$STORY_CLEANUP_TMP/source-state-synthetic"
chmod 644 "$STORY_CLEANUP_TMP/source-state-synthetic"
if assert_cleanup_transaction_source \
  "$STORY_CLEANUP_TMP/source-state-synthetic" "$SYNTHETIC_SOURCE_SHA256"; then
  exit 73
fi
rm -- "$STORY_CLEANUP_TMP/source-state-synthetic"
ln -s "$STORY_CLEANUP_TMP/missing-source" \
  "$STORY_CLEANUP_TMP/source-state-synthetic"
if assert_cleanup_transaction_source \
  "$STORY_CLEANUP_TMP/source-state-synthetic" "$SYNTHETIC_SOURCE_SHA256"; then
  exit 73
fi
rm -- "$STORY_CLEANUP_TMP/source-state-synthetic"
printf '%s\n' 'exact-source' > "$STORY_CLEANUP_TMP/source-state-target"
chmod 600 "$STORY_CLEANUP_TMP/source-state-target"
ln -s "$STORY_CLEANUP_TMP/source-state-target" \
  "$STORY_CLEANUP_TMP/source-state-synthetic"
if assert_cleanup_transaction_source \
  "$STORY_CLEANUP_TMP/source-state-synthetic" "$SYNTHETIC_SOURCE_SHA256"; then
  exit 73
fi
rm -- "$STORY_CLEANUP_TMP/source-state-synthetic" \
  "$STORY_CLEANUP_TMP/source-state-target"
if ! ORIGIN_FETCH_URLS="$(git -C "$FRONTEND_REPOSITORY" \
  remote get-url --all origin)"; then
  exit 71
fi
if ! ORIGIN_PUSH_URLS="$(git -C "$FRONTEND_REPOSITORY" \
  remote get-url --push --all origin)"; then
  exit 71
fi
test "$(printf '%s\n' "$ORIGIN_FETCH_URLS" | awk 'END { print NR }')" = "1"
test "$(printf '%s\n' "$ORIGIN_PUSH_URLS" | awk 'END { print NR }')" = "1"
ORIGIN_FETCH_URL="$(printf '%s\n' "$ORIGIN_FETCH_URLS" | sed -n '1p')"
ORIGIN_PUSH_URL="$(printf '%s\n' "$ORIGIN_PUSH_URLS" | sed -n '1p')"
if ! FETCH_REPOSITORY="$(gh repo view "$ORIGIN_FETCH_URL" \
  --json nameWithOwner --jq '.nameWithOwner')"; then
  exit 71
fi
if ! PUSH_REPOSITORY="$(gh repo view "$ORIGIN_PUSH_URL" \
  --json nameWithOwner --jq '.nameWithOwner')"; then
  exit 71
fi
test "$FETCH_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
test "$PUSH_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
REPOSITORY="$FETCH_REPOSITORY"
if test -L "$STORY_CLEANUP_TRANSACTION_RECORD"; then
  exit 74
fi
if test -e "$STORY_CLEANUP_TRANSACTION_RECORD"; then
  assert_regular_mode_600_record "$STORY_CLEANUP_TRANSACTION_RECORD"
  if ! CLEANUP_TRANSACTION_VALUES="$(awk -F= '
    BEGIN {
      order[1] = "STORY_169_15_CLEANUP_TRANSACTION_VERSION"
      order[2] = "STORY_169_15_REPOSITORY"
      order[3] = "STORY_169_15_BASE_SHA"
      order[4] = "STORY_169_15_EVIDENCE_COMMIT_SHA"
      order[5] = "STORY_169_15_FEATURE_SHA"
      order[6] = "STORY_169_15_FEATURE_TREE_SHA"
      order[7] = "STORY_169_15_CUMULATIVE_MANIFEST_SHA256"
      order[8] = "STORY_169_15_REVIEW_PASS_1_ID"
      order[9] = "STORY_169_15_REVIEW_PASS_1_DISPOSITION"
      order[10] = "STORY_169_15_REVIEW_PASS_2_ID"
      order[11] = "STORY_169_15_REVIEW_PASS_2_DISPOSITION"
      order[12] = "STORY_169_15_PR_NUMBER"
      order[13] = "STORY_169_15_PR_URL"
      order[14] = "STORY_169_15_PR_BASE_BRANCH"
      order[15] = "STORY_169_15_PR_HEAD_BRANCH"
      order[16] = "STORY_169_15_MERGE_SHA"
      order[17] = "STORY_169_15_REFRESHED_ORIGIN_MAIN_SHA"
      order[18] = "STORY_169_15_REVIEWED_HEAD_RECORD_PATH"
      order[19] = "STORY_169_15_REVIEWED_HEAD_RECORD_SHA256"
      order[20] = "STORY_169_15_LIFECYCLE_RECORD_PATH"
      order[21] = "STORY_169_15_LIFECYCLE_RECORD_SHA256"
      order[22] = "STORY_169_15_EXPECTED_DELETION_SET"
      order[23] = "STORY_169_15_COMPLETED_TOPOLOGY_PROOF"
      order[24] = "STORY_169_15_CLEANUP_PROOF"
    }
    {
      if (NR > 24 || $1 != order[NR]) exit 2
      value = substr($0, length($1) + 2)
      if (value == "") exit 2
      values[NR] = value
    }
    END {
      if (NR != 24) exit 2
      output = values[1]
      for (line = 2; line <= 24; line++) output = output "|" values[line]
      print output
    }
  ' "$STORY_CLEANUP_TRANSACTION_RECORD")"; then
    exit 71
  fi
  IFS='|' read -r TRANSACTION_VERSION TRANSACTION_REPOSITORY \
    TRANSACTION_BASE_SHA TRANSACTION_EVIDENCE_COMMIT_SHA \
    TRANSACTION_FEATURE_SHA TRANSACTION_FEATURE_TREE_SHA \
    TRANSACTION_CUMULATIVE_MANIFEST_SHA256 TRANSACTION_REVIEW_PASS_1_ID \
    TRANSACTION_REVIEW_PASS_1_DISPOSITION TRANSACTION_REVIEW_PASS_2_ID \
    TRANSACTION_REVIEW_PASS_2_DISPOSITION TRANSACTION_PR_NUMBER \
    TRANSACTION_PR_URL TRANSACTION_PR_BASE_BRANCH \
    TRANSACTION_PR_HEAD_BRANCH TRANSACTION_MERGE_SHA \
    TRANSACTION_ORIGIN_MAIN_SHA \
    TRANSACTION_REVIEWED_PATH TRANSACTION_REVIEWED_SHA256 \
    TRANSACTION_LIFECYCLE_PATH TRANSACTION_LIFECYCLE_SHA256 \
    TRANSACTION_DELETION_SET TRANSACTION_TOPOLOGY_PROOF \
    TRANSACTION_CLEANUP_PROOF <<< "$CLEANUP_TRANSACTION_VALUES"
  test "$TRANSACTION_VERSION" = "1"
  test "$TRANSACTION_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
  for TRANSACTION_SHA_VALUE in \
    "$TRANSACTION_BASE_SHA" \
    "$TRANSACTION_EVIDENCE_COMMIT_SHA" \
    "$TRANSACTION_FEATURE_SHA" \
    "$TRANSACTION_FEATURE_TREE_SHA" \
    "$TRANSACTION_MERGE_SHA" \
    "$TRANSACTION_ORIGIN_MAIN_SHA"; do
    test "${#TRANSACTION_SHA_VALUE}" -eq 40
    case "$TRANSACTION_SHA_VALUE" in
      *[!0-9a-f]* | '') exit 64 ;;
    esac
  done
  test "${#TRANSACTION_CUMULATIVE_MANIFEST_SHA256}" -eq 64
  case "$TRANSACTION_CUMULATIVE_MANIFEST_SHA256" in
    *[!0-9a-f]* | '') exit 64 ;;
  esac
  test "$TRANSACTION_REVIEW_PASS_1_DISPOSITION" = "PASS"
  test "$TRANSACTION_REVIEW_PASS_2_DISPOSITION" = "PASS"
  test "$TRANSACTION_REVIEW_PASS_1_ID" != \
    "$TRANSACTION_REVIEW_PASS_2_ID"
  test "$TRANSACTION_PR_BASE_BRANCH" = "main"
  test "$TRANSACTION_PR_HEAD_BRANCH" = "$STORY_BRANCH"
  test "$TRANSACTION_REVIEWED_PATH" = "$STORY_REVIEWED_HEAD_RECORD"
  test "$TRANSACTION_LIFECYCLE_PATH" = "$STORY_LIFECYCLE_RECORD"
  test "$TRANSACTION_DELETION_SET" = \
    "$STORY_REVIEWED_HEAD_RECORD,$STORY_LIFECYCLE_RECORD"
  test "$TRANSACTION_TOPOLOGY_PROOF" = \
    "MERGED_MAIN_ANCESTRY_AND_BRANCH_WORKTREE_ABSENT"
  test "$TRANSACTION_CLEANUP_PROOF" = "PASS"
  TRANSACTION_PR_JSON="$(gh pr view "$TRANSACTION_PR_NUMBER" \
    --repo "$EXPECTED_FRONTEND_GITHUB_REPOSITORY" \
    --json state,baseRefName,headRefName,headRefOid,url,mergeCommit)"
  test "$(printf '%s\n' "$TRANSACTION_PR_JSON" | jq -r '.state')" = "MERGED"
  test "$(printf '%s\n' "$TRANSACTION_PR_JSON" | jq -r '.baseRefName')" = \
    "$TRANSACTION_PR_BASE_BRANCH"
  test "$(printf '%s\n' "$TRANSACTION_PR_JSON" | jq -r '.headRefName')" = \
    "$TRANSACTION_PR_HEAD_BRANCH"
  test "$(printf '%s\n' "$TRANSACTION_PR_JSON" | jq -r '.headRefOid')" = \
    "$TRANSACTION_FEATURE_SHA"
  test "$(printf '%s\n' "$TRANSACTION_PR_JSON" | jq -r '.url')" = \
    "$TRANSACTION_PR_URL"
  test "$(printf '%s\n' "$TRANSACTION_PR_JSON" | jq -r '.mergeCommit.oid')" = \
    "$TRANSACTION_MERGE_SHA"
  if ! TRANSACTION_ACTUAL_FEATURE_TREE_SHA="$(git -C \
    "$FRONTEND_REPOSITORY" rev-parse \
    "$TRANSACTION_FEATURE_SHA^{tree}")"; then
    exit 71
  fi
  test "$TRANSACTION_ACTUAL_FEATURE_TREE_SHA" = \
    "$TRANSACTION_FEATURE_TREE_SHA"
  if ! TRANSACTION_FEATURE_AND_PARENTS="$(git -C "$FRONTEND_REPOSITORY" \
    rev-list --parents -n 1 "$TRANSACTION_FEATURE_SHA")"; then
    exit 71
  fi
  IFS=' ' read -r TRANSACTION_FEATURE_OID TRANSACTION_FEATURE_PARENT_SHA \
    TRANSACTION_FEATURE_EXTRA_PARENT <<< "$TRANSACTION_FEATURE_AND_PARENTS"
  test "$TRANSACTION_FEATURE_OID" = "$TRANSACTION_FEATURE_SHA"
  test "$TRANSACTION_FEATURE_PARENT_SHA" = \
    "$TRANSACTION_EVIDENCE_COMMIT_SHA"
  test -z "$TRANSACTION_FEATURE_EXTRA_PARENT"
  git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
    "$TRANSACTION_BASE_SHA" "$TRANSACTION_FEATURE_SHA"
  if ! (
    set -o pipefail
    git -C "$FRONTEND_REPOSITORY" diff --no-renames --name-only \
      "$TRANSACTION_BASE_SHA" "$TRANSACTION_FEATURE_SHA" | \
      LC_ALL=C sort -u > "$STORY_CLEANUP_TMP/transaction-cumulative-manifest"
  ); then
    exit 71
  fi
  test -s "$STORY_CLEANUP_TMP/transaction-cumulative-manifest"
  test "$(shasum -a 256 \
    "$STORY_CLEANUP_TMP/transaction-cumulative-manifest" | \
    awk '{ print $1 }')" = "$TRANSACTION_CUMULATIVE_MANIFEST_SHA256"
  git -C "$FRONTEND_REPOSITORY" fetch --no-tags "$ORIGIN_FETCH_URL" \
    'refs/heads/main:refs/remotes/origin/main'
  CURRENT_TRANSACTION_ORIGIN_MAIN_SHA="$(git -C "$FRONTEND_REPOSITORY" \
    rev-parse origin/main)"
  git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
    "$TRANSACTION_ORIGIN_MAIN_SHA" "$CURRENT_TRANSACTION_ORIGIN_MAIN_SHA"
  git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
    "$TRANSACTION_FEATURE_SHA" "$TRANSACTION_MERGE_SHA"
  git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
    "$TRANSACTION_MERGE_SHA" "$TRANSACTION_ORIGIN_MAIN_SHA"
  git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
    "$TRANSACTION_MERGE_SHA" "$CURRENT_TRANSACTION_ORIGIN_MAIN_SHA"
  assert_absent_non_symlink_residue "$STORY_WORKTREE"
  if ! TRANSACTION_LOCAL_STORY_BRANCH="$(git -C "$FRONTEND_REPOSITORY" \
    branch --list "$STORY_BRANCH")"; then
    exit 71
  fi
  if ! TRANSACTION_REMOTE_STORY_BRANCH="$(git -C "$FRONTEND_REPOSITORY" \
    ls-remote --heads "$ORIGIN_PUSH_URL" \
    "refs/heads/$STORY_BRANCH")"; then
    exit 71
  fi
  test -z "$TRANSACTION_LOCAL_STORY_BRANCH"
  test -z "$TRANSACTION_REMOTE_STORY_BRANCH"
  if ! TRANSACTION_WORKTREE_INVENTORY="$(git -C "$FRONTEND_REPOSITORY" \
    worktree list --porcelain)"; then
    exit 71
  fi
  case "$TRANSACTION_WORKTREE_INVENTORY" in
    *"$STORY_WORKTREE"*) exit 70 ;;
  esac
  assert_cleanup_transaction_source \
    "$TRANSACTION_REVIEWED_PATH" "$TRANSACTION_REVIEWED_SHA256"
  assert_cleanup_transaction_source \
    "$TRANSACTION_LIFECYCLE_PATH" "$TRANSACTION_LIFECYCLE_SHA256"
  if test -e "$TRANSACTION_REVIEWED_PATH"; then
    rm -- "$TRANSACTION_REVIEWED_PATH"
  fi
  if test -e "$TRANSACTION_LIFECYCLE_PATH"; then
    rm -- "$TRANSACTION_LIFECYCLE_PATH"
  fi
  assert_absent_non_symlink_residue "$TRANSACTION_REVIEWED_PATH"
  assert_absent_non_symlink_residue "$TRANSACTION_LIFECYCLE_PATH"
  rm -- "$STORY_CLEANUP_TRANSACTION_RECORD"
  assert_absent_non_symlink_residue "$STORY_CLEANUP_TRANSACTION_RECORD"
  exit 0
fi
assert_regular_mode_600_record "$STORY_REVIEWED_HEAD_RECORD"
if ! REVIEWED_HEAD_RECORD_SHA256="$(shasum -a 256 \
  "$STORY_REVIEWED_HEAD_RECORD" | awk '{ print $1 }')"; then
  exit 71
fi
if ! REVIEWED_HEAD_VALUES="$(awk -F= '
  BEGIN {
    required["STORY_169_15_REVIEWED_HEAD_RECORD_VERSION"] = 1
    required["STORY_169_15_REPOSITORY"] = 1
    required["STORY_169_15_BASE_SHA"] = 1
    required["STORY_169_15_EVIDENCE_COMMIT_SHA"] = 1
    required["STORY_169_15_FEATURE_SHA"] = 1
    required["STORY_169_15_FEATURE_TREE_SHA"] = 1
    required["STORY_169_15_CUMULATIVE_MANIFEST_SHA256"] = 1
    required["STORY_169_15_REVIEW_PASS_1_ID"] = 1
    required["STORY_169_15_REVIEW_PASS_1_DISPOSITION"] = 1
    required["STORY_169_15_REVIEW_PASS_2_ID"] = 1
    required["STORY_169_15_REVIEW_PASS_2_DISPOSITION"] = 1
  }
  {
    if (!($1 in required) || seen[$1]++) exit 2
    value = substr($0, length($1) + 2)
    if (value == "") exit 2
    values[$1] = value
    lines++
  }
  END {
    if (lines != 11) exit 2
    for (key in required) if (seen[key] != 1) exit 2
    print values["STORY_169_15_REVIEWED_HEAD_RECORD_VERSION"] "|" \
      values["STORY_169_15_REPOSITORY"] "|" \
      values["STORY_169_15_BASE_SHA"] "|" \
      values["STORY_169_15_EVIDENCE_COMMIT_SHA"] "|" \
      values["STORY_169_15_FEATURE_SHA"] "|" \
      values["STORY_169_15_FEATURE_TREE_SHA"] "|" \
      values["STORY_169_15_CUMULATIVE_MANIFEST_SHA256"] "|" \
      values["STORY_169_15_REVIEW_PASS_1_ID"] "|" \
      values["STORY_169_15_REVIEW_PASS_1_DISPOSITION"] "|" \
      values["STORY_169_15_REVIEW_PASS_2_ID"] "|" \
      values["STORY_169_15_REVIEW_PASS_2_DISPOSITION"]
  }
' "$STORY_REVIEWED_HEAD_RECORD")"; then
  exit 71
fi
IFS='|' read -r REVIEWED_HEAD_VERSION REVIEWED_HEAD_REPOSITORY STORY_BASE_SHA \
  EVIDENCE_COMMIT_SHA REVIEWED_HEAD_FEATURE_SHA REVIEWED_HEAD_FEATURE_TREE_SHA \
  REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256 REVIEWED_HEAD_REVIEW_PASS_1_ID \
  REVIEWED_HEAD_REVIEW_PASS_1_DISPOSITION REVIEWED_HEAD_REVIEW_PASS_2_ID \
  REVIEWED_HEAD_REVIEW_PASS_2_DISPOSITION <<< "$REVIEWED_HEAD_VALUES"
test "$REVIEWED_HEAD_VERSION" = "1"
test "$REVIEWED_HEAD_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
test "$REVIEWED_HEAD_REVIEW_PASS_1_DISPOSITION" = "PASS"
test "$REVIEWED_HEAD_REVIEW_PASS_2_DISPOSITION" = "PASS"
test "$REVIEWED_HEAD_REVIEW_PASS_1_ID" != "$REVIEWED_HEAD_REVIEW_PASS_2_ID"
for SHA_VALUE in \
  "$STORY_BASE_SHA" \
  "$EVIDENCE_COMMIT_SHA" \
  "$REVIEWED_HEAD_FEATURE_SHA" \
  "$REVIEWED_HEAD_FEATURE_TREE_SHA"; do
  test "${#SHA_VALUE}" -eq 40
  case "$SHA_VALUE" in
    *[!0-9a-f]* | '') exit 64 ;;
  esac
done
test "${#REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256}" -eq 64
case "$REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256" in
  *[!0-9a-f]* | '') exit 64 ;;
esac
assert_regular_mode_600_record "$STORY_LIFECYCLE_RECORD"
if ! RECORDED_LIFECYCLE_RECORD_MODE="$(stat -f '%Lp' "$STORY_LIFECYCLE_RECORD")"; then
  exit 71
fi
test "$RECORDED_LIFECYCLE_RECORD_MODE" = "600"
if ! LIFECYCLE_RECORD_SHA256="$(shasum -a 256 \
  "$STORY_LIFECYCLE_RECORD" | awk '{ print $1 }')"; then
  exit 71
fi
if ! RECORDED_LIFECYCLE_VALUES="$(awk -F= '
  BEGIN {
    required["STORY_169_15_LIFECYCLE_RECORD_VERSION"] = 1
    required["STORY_169_15_REPOSITORY"] = 1
    required["STORY_169_15_PR_NUMBER"] = 1
    required["STORY_169_15_PR_URL"] = 1
    required["STORY_169_15_FEATURE_SHA"] = 1
    required["STORY_169_15_MERGE_SHA"] = 1
    required["STORY_169_15_REVIEW_PASS_1_ID"] = 1
    required["STORY_169_15_REVIEW_PASS_1_DISPOSITION"] = 1
    required["STORY_169_15_REVIEW_PASS_2_ID"] = 1
    required["STORY_169_15_REVIEW_PASS_2_DISPOSITION"] = 1
  }
  {
    if (!($1 in required) || seen[$1]++) exit 2
    value = substr($0, length($1) + 2)
    if (value == "") exit 2
    values[$1] = value
    lines++
  }
  END {
    if (lines != 10) exit 2
    for (key in required) if (seen[key] != 1) exit 2
    print values["STORY_169_15_LIFECYCLE_RECORD_VERSION"] "|" \
      values["STORY_169_15_REPOSITORY"] "|" \
      values["STORY_169_15_PR_NUMBER"] "|" \
      values["STORY_169_15_PR_URL"] "|" \
      values["STORY_169_15_FEATURE_SHA"] "|" \
      values["STORY_169_15_MERGE_SHA"] "|" \
      values["STORY_169_15_REVIEW_PASS_1_ID"] "|" \
      values["STORY_169_15_REVIEW_PASS_1_DISPOSITION"] "|" \
      values["STORY_169_15_REVIEW_PASS_2_ID"] "|" \
      values["STORY_169_15_REVIEW_PASS_2_DISPOSITION"]
  }
' "$STORY_LIFECYCLE_RECORD")"; then
  exit 71
fi
IFS='|' read -r RECORD_VERSION RECORDED_REPOSITORY STORY_PR_NUMBER \
  STORY_PR_URL STORY_FEATURE_SHA RECORDED_STORY_MERGE_SHA \
  REVIEW_PASS_1_ID REVIEW_PASS_1_DISPOSITION REVIEW_PASS_2_ID \
  REVIEW_PASS_2_DISPOSITION <<< "$RECORDED_LIFECYCLE_VALUES"
test "$RECORD_VERSION" = "1"
test "$RECORDED_REPOSITORY" = "$EXPECTED_FRONTEND_GITHUB_REPOSITORY"
case "$STORY_PR_NUMBER" in
  '' | *[!0-9]*) exit 64 ;;
esac
test "$(expr "$STORY_FEATURE_SHA" : '.*')" = "40"
case "$STORY_FEATURE_SHA" in
  *[!0-9a-f]* | '') exit 64 ;;
esac
case "$RECORDED_STORY_MERGE_SHA" in
  PENDING) ;;
  *)
    test "$(expr "$RECORDED_STORY_MERGE_SHA" : '.*')" = "40"
    case "$RECORDED_STORY_MERGE_SHA" in
      *[!0-9a-f]* | '') exit 64 ;;
    esac
    ;;
esac
case "$REVIEW_PASS_1_ID" in
  '' | *[!A-Za-z0-9._/@:-]*) exit 64 ;;
esac
case "$REVIEW_PASS_2_ID" in
  '' | *[!A-Za-z0-9._/@:-]*) exit 64 ;;
esac
test "$REVIEW_PASS_1_ID" != "$REVIEW_PASS_2_ID"
test "$REVIEW_PASS_1_DISPOSITION" = "PASS"
test "$REVIEW_PASS_2_DISPOSITION" = "PASS"
test "$STORY_FEATURE_SHA" = "$REVIEWED_HEAD_FEATURE_SHA"
test "$REVIEW_PASS_1_ID" = "$REVIEWED_HEAD_REVIEW_PASS_1_ID"
test "$REVIEW_PASS_1_DISPOSITION" = \
  "$REVIEWED_HEAD_REVIEW_PASS_1_DISPOSITION"
test "$REVIEW_PASS_2_ID" = "$REVIEWED_HEAD_REVIEW_PASS_2_ID"
test "$REVIEW_PASS_2_DISPOSITION" = \
  "$REVIEWED_HEAD_REVIEW_PASS_2_DISPOSITION"
write_cleanup_lifecycle_candidate() {
  CLEANUP_MERGE_SHA="$1"
  CLEANUP_RECORD_OUTPUT="$2"
  printf '%s\n' \
    'STORY_169_15_LIFECYCLE_RECORD_VERSION=1' \
    "STORY_169_15_REPOSITORY=$EXPECTED_FRONTEND_GITHUB_REPOSITORY" \
    "STORY_169_15_PR_NUMBER=$STORY_PR_NUMBER" \
    "STORY_169_15_PR_URL=$STORY_PR_URL" \
    "STORY_169_15_FEATURE_SHA=$STORY_FEATURE_SHA" \
    "STORY_169_15_MERGE_SHA=$CLEANUP_MERGE_SHA" \
    "STORY_169_15_REVIEW_PASS_1_ID=$REVIEW_PASS_1_ID" \
    'STORY_169_15_REVIEW_PASS_1_DISPOSITION=PASS' \
    "STORY_169_15_REVIEW_PASS_2_ID=$REVIEW_PASS_2_ID" \
    'STORY_169_15_REVIEW_PASS_2_DISPOSITION=PASS' \
    > "$CLEANUP_RECORD_OUTPUT"
  chmod 600 "$CLEANUP_RECORD_OUTPUT"
  test "$(awk 'END { print NR }' "$CLEANUP_RECORD_OUTPUT")" = "10"
}
write_cleanup_lifecycle_candidate "$RECORDED_STORY_MERGE_SHA" \
  "$STORY_CLEANUP_TMP/canonical-recorded-lifecycle"
cmp -s "$STORY_LIFECYCLE_RECORD" \
  "$STORY_CLEANUP_TMP/canonical-recorded-lifecycle"
case "$STORY_PR_URL" in
  "https://github.com/$EXPECTED_FRONTEND_GITHUB_REPOSITORY/pull/$STORY_PR_NUMBER") ;;
  *) exit 64 ;;
esac
read_recorded_pr() {
  if ! STORY_PR_JSON="$(gh pr view "$STORY_PR_NUMBER" --repo "$REPOSITORY" \
    --json number,state,baseRefName,headRefName,headRefOid,url,mergeCommit)"; then
    exit 71
  fi
  test "$(printf '%s\n' "$STORY_PR_JSON" | jq -r '.number')" = "$STORY_PR_NUMBER"
  STORY_PR_STATE="$(printf '%s\n' "$STORY_PR_JSON" | jq -r '.state')"
  STORY_PR_BASE_BRANCH="$(printf '%s\n' "$STORY_PR_JSON" | jq -r '.baseRefName')"
  STORY_PR_HEAD_BRANCH="$(printf '%s\n' "$STORY_PR_JSON" | jq -r '.headRefName')"
  STORY_PR_HEAD_SHA="$(printf '%s\n' "$STORY_PR_JSON" | jq -r '.headRefOid')"
  VERIFIED_STORY_PR_URL="$(printf '%s\n' "$STORY_PR_JSON" | jq -r '.url')"
  STORY_MERGE_SHA="$(printf '%s\n' "$STORY_PR_JSON" | jq -r '.mergeCommit.oid')"
  test "$STORY_PR_BASE_BRANCH" = "main"
  test "$STORY_PR_HEAD_BRANCH" = "$STORY_BRANCH"
  test "$STORY_PR_HEAD_SHA" = "$STORY_FEATURE_SHA"
  test "$VERIFIED_STORY_PR_URL" = "$STORY_PR_URL"
}
read_recorded_pr
test "$(shasum -a 256 "$STORY_LIFECYCLE_RECORD" | awk '{ print $1 }')" = \
  "$LIFECYCLE_RECORD_SHA256"
if test "$RECORDED_STORY_MERGE_SHA" = "PENDING"; then
  case "$STORY_PR_STATE" in
    OPEN)
      # Recovery uses only the exact PR/head from the pending record.
      gh pr merge "$STORY_PR_NUMBER" --repo "$REPOSITORY" --merge \
        --match-head-commit "$STORY_FEATURE_SHA"
      read_recorded_pr
      ;;
    MERGED) ;;
    *) exit 65 ;;
  esac
  test "$STORY_PR_STATE" = "MERGED"
  test "$(expr "$STORY_MERGE_SHA" : '.*')" = "40"
  case "$STORY_MERGE_SHA" in
    *[!0-9a-f]* | '') exit 64 ;;
  esac
  umask 077
  FINAL_LIFECYCLE_RECORD_TMP="$(mktemp \
    "$FRONTEND_GIT_COMMON_DIR/story-169-15-lifecycle-record-v1.tmp.XXXXXX")"
  write_cleanup_lifecycle_candidate "$STORY_MERGE_SHA" \
    "$FINAL_LIFECYCLE_RECORD_TMP"
  cmp -s "$STORY_LIFECYCLE_RECORD" \
    "$STORY_CLEANUP_TMP/canonical-recorded-lifecycle"
  mv -- "$FINAL_LIFECYCLE_RECORD_TMP" "$STORY_LIFECYCLE_RECORD"
  test "$(stat -f '%Lp' "$STORY_LIFECYCLE_RECORD")" = "600"
  write_cleanup_lifecycle_candidate "$STORY_MERGE_SHA" \
    "$STORY_CLEANUP_TMP/canonical-final-lifecycle"
  cmp -s "$STORY_LIFECYCLE_RECORD" \
    "$STORY_CLEANUP_TMP/canonical-final-lifecycle"
  RECORDED_STORY_MERGE_SHA="$STORY_MERGE_SHA"
else
  test "$STORY_PR_STATE" = "MERGED"
  test "$STORY_MERGE_SHA" = "$RECORDED_STORY_MERGE_SHA"
fi
if ! FINAL_LIFECYCLE_RECORD_SHA256="$(shasum -a 256 \
  "$STORY_LIFECYCLE_RECORD" | awk '{ print $1 }')"; then
  exit 71
fi
if ! PRIMARY_STATUS="$(git -C "$FRONTEND_REPOSITORY" status --porcelain)"; then
  exit 71
fi
test -z "$PRIMARY_STATUS"
test ! -L "$STORY_WORKTREE"
if test -e "$STORY_WORKTREE"; then
  assert_story_worktree_identity
  if ! WORKTREE_STATUS="$(git -C "$STORY_WORKTREE" status --porcelain)"; then
    exit 71
  fi
  if ! WORKTREE_HEAD_SHA="$(git -C "$STORY_WORKTREE" rev-parse HEAD)"; then
    exit 71
  fi
  test -z "$WORKTREE_STATUS"
  test "$WORKTREE_HEAD_SHA" = "$STORY_FEATURE_SHA"
else
  if ! PRE_CLEANUP_WORKTREE_LIST="$(git -C "$FRONTEND_REPOSITORY" worktree list --porcelain)"; then
    exit 71
  fi
  case "$PRE_CLEANUP_WORKTREE_LIST" in
    *"$STORY_WORKTREE"*) exit 70 ;;
  esac
fi
if ! WORKTREE_TREE_SHA="$(git -C "$FRONTEND_REPOSITORY" rev-parse \
  "$STORY_FEATURE_SHA^{tree}")"; then
  exit 71
fi
test "$WORKTREE_TREE_SHA" = "$REVIEWED_HEAD_FEATURE_TREE_SHA"
if ! STORY_FEATURE_AND_PARENTS="$(git -C "$FRONTEND_REPOSITORY" rev-list \
  --parents -n 1 "$REVIEWED_HEAD_FEATURE_SHA")"; then
  exit 71
fi
IFS=' ' read -r STORY_FEATURE_OID STORY_FEATURE_PARENT_SHA \
  STORY_FEATURE_EXTRA_PARENT <<< "$STORY_FEATURE_AND_PARENTS"
test "$STORY_FEATURE_OID" = "$REVIEWED_HEAD_FEATURE_SHA"
test "$STORY_FEATURE_PARENT_SHA" = "$EVIDENCE_COMMIT_SHA"
test -z "$STORY_FEATURE_EXTRA_PARENT"
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
  "$STORY_BASE_SHA" "$STORY_FEATURE_SHA"
if ! (
  set -o pipefail
  git -C "$FRONTEND_REPOSITORY" diff --no-renames --name-only \
    "$STORY_BASE_SHA" "$STORY_FEATURE_SHA" | LC_ALL=C sort -u > \
    "$STORY_CLEANUP_TMP/cumulative-story-manifest"
); then
  exit 71
fi
test -s "$STORY_CLEANUP_TMP/cumulative-story-manifest"
if ! CLEANUP_CUMULATIVE_MANIFEST_SHA256="$(shasum -a 256 \
  "$STORY_CLEANUP_TMP/cumulative-story-manifest" | awk '{ print $1 }')"; then
  exit 71
fi
test "$CLEANUP_CUMULATIVE_MANIFEST_SHA256" = \
  "$REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256"

git -C "$FRONTEND_REPOSITORY" switch main
git -C "$FRONTEND_REPOSITORY" fetch "$ORIGIN_FETCH_URL" \
  'refs/heads/main:refs/remotes/origin/main'
git -C "$FRONTEND_REPOSITORY" merge --ff-only origin/main
if ! REFRESHED_FRONTEND_LOCAL_MAIN_SHA="$(git -C "$FRONTEND_REPOSITORY" rev-parse main)"; then
  exit 71
fi
if ! REFRESHED_FRONTEND_ORIGIN_MAIN_SHA="$(git -C "$FRONTEND_REPOSITORY" rev-parse origin/main)"; then
  exit 71
fi
test "$REFRESHED_FRONTEND_LOCAL_MAIN_SHA" = "$REFRESHED_FRONTEND_ORIGIN_MAIN_SHA"
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
  "$STORY_FEATURE_SHA" "$STORY_MERGE_SHA"
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
  "$STORY_MERGE_SHA" "$REFRESHED_FRONTEND_ORIGIN_MAIN_SHA"
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor \
  "$STORY_FEATURE_SHA" "$REFRESHED_FRONTEND_ORIGIN_MAIN_SHA"
if ! REMOTE_STORY_BRANCH="$(git -C "$FRONTEND_REPOSITORY" \
  ls-remote --heads "$ORIGIN_PUSH_URL" "refs/heads/$STORY_BRANCH")"; then
  exit 71
fi
if test -n "$REMOTE_STORY_BRANCH"; then
  EXPECTED_REMOTE_STORY_BRANCH="$(printf '%s\trefs/heads/%s' \
    "$STORY_FEATURE_SHA" "$STORY_BRANCH")"
  test "$REMOTE_STORY_BRANCH" = "$EXPECTED_REMOTE_STORY_BRANCH"
  # The server checks the recorded old SHA atomically with deletion. A concurrently
  # advanced ref makes the push fail instead of deleting the new tip.
  git -C "$FRONTEND_REPOSITORY" push \
    --force-with-lease="refs/heads/$STORY_BRANCH:$STORY_FEATURE_SHA" \
    "$ORIGIN_PUSH_URL" ":refs/heads/$STORY_BRANCH"
fi
test ! -L "$STORY_WORKTREE"
if test -e "$STORY_WORKTREE"; then
  git -C "$FRONTEND_REPOSITORY" worktree remove "$STORY_WORKTREE"
fi
if ! PRE_DELETE_LOCAL_BRANCH_SHA="$(git -C "$FRONTEND_REPOSITORY" rev-parse \
  --verify --quiet "refs/heads/$STORY_BRANCH")"; then
  PRE_DELETE_LOCAL_BRANCH_SHA=""
fi
case "$PRE_DELETE_LOCAL_BRANCH_SHA" in
  '') ;;
  "$STORY_FEATURE_SHA")
    git -C "$FRONTEND_REPOSITORY" update-ref -d \
      "refs/heads/$STORY_BRANCH" "$STORY_FEATURE_SHA"
    ;;
  *) exit 73 ;;
esac
git -C "$FRONTEND_REPOSITORY" worktree prune
assert_absent_non_symlink_residue "$STORY_WORKTREE"
if ! FINAL_WORKTREE_LIST="$(git -C "$FRONTEND_REPOSITORY" worktree list --porcelain)"; then
  exit 71
fi
if ! FINAL_LOCAL_STORY_BRANCH="$(git -C "$FRONTEND_REPOSITORY" branch --list "$STORY_BRANCH")"; then
  exit 71
fi
if ! FINAL_REMOTE_STORY_BRANCH="$(git -C "$FRONTEND_REPOSITORY" ls-remote --heads "$ORIGIN_PUSH_URL" "$STORY_BRANCH")"; then
  exit 71
fi
if ! FINAL_PR_STATE="$(gh pr view "$STORY_PR_NUMBER" --repo "$REPOSITORY" --json state --jq '.state')"; then
  exit 71
fi
case "$FINAL_WORKTREE_LIST" in
  *"$STORY_WORKTREE"*) exit 70 ;;
esac
test -z "$FINAL_LOCAL_STORY_BRANCH"
test -z "$FINAL_REMOTE_STORY_BRANCH"
test "$FINAL_PR_STATE" = "MERGED"
test "$(shasum -a 256 "$STORY_REVIEWED_HEAD_RECORD" | awk '{ print $1 }')" = \
  "$REVIEWED_HEAD_RECORD_SHA256"
test "$(shasum -a 256 "$STORY_LIFECYCLE_RECORD" | awk '{ print $1 }')" = \
  "$FINAL_LIFECYCLE_RECORD_SHA256"
umask 077
CLEANUP_TRANSACTION_TMP="$(mktemp \
  "$FRONTEND_GIT_COMMON_DIR/story-169-15-cleanup-transaction-v1.tmp.XXXXXX")"
printf '%s\n' \
  'STORY_169_15_CLEANUP_TRANSACTION_VERSION=1' \
  "STORY_169_15_REPOSITORY=$EXPECTED_FRONTEND_GITHUB_REPOSITORY" \
  "STORY_169_15_BASE_SHA=$STORY_BASE_SHA" \
  "STORY_169_15_EVIDENCE_COMMIT_SHA=$EVIDENCE_COMMIT_SHA" \
  "STORY_169_15_FEATURE_SHA=$STORY_FEATURE_SHA" \
  "STORY_169_15_FEATURE_TREE_SHA=$REVIEWED_HEAD_FEATURE_TREE_SHA" \
  "STORY_169_15_CUMULATIVE_MANIFEST_SHA256=$REVIEWED_HEAD_CUMULATIVE_MANIFEST_SHA256" \
  "STORY_169_15_REVIEW_PASS_1_ID=$REVIEW_PASS_1_ID" \
  'STORY_169_15_REVIEW_PASS_1_DISPOSITION=PASS' \
  "STORY_169_15_REVIEW_PASS_2_ID=$REVIEW_PASS_2_ID" \
  'STORY_169_15_REVIEW_PASS_2_DISPOSITION=PASS' \
  "STORY_169_15_PR_NUMBER=$STORY_PR_NUMBER" \
  "STORY_169_15_PR_URL=$STORY_PR_URL" \
  'STORY_169_15_PR_BASE_BRANCH=main' \
  "STORY_169_15_PR_HEAD_BRANCH=$STORY_BRANCH" \
  "STORY_169_15_MERGE_SHA=$STORY_MERGE_SHA" \
  "STORY_169_15_REFRESHED_ORIGIN_MAIN_SHA=$REFRESHED_FRONTEND_ORIGIN_MAIN_SHA" \
  "STORY_169_15_REVIEWED_HEAD_RECORD_PATH=$STORY_REVIEWED_HEAD_RECORD" \
  "STORY_169_15_REVIEWED_HEAD_RECORD_SHA256=$REVIEWED_HEAD_RECORD_SHA256" \
  "STORY_169_15_LIFECYCLE_RECORD_PATH=$STORY_LIFECYCLE_RECORD" \
  "STORY_169_15_LIFECYCLE_RECORD_SHA256=$FINAL_LIFECYCLE_RECORD_SHA256" \
  "STORY_169_15_EXPECTED_DELETION_SET=$STORY_REVIEWED_HEAD_RECORD,$STORY_LIFECYCLE_RECORD" \
  'STORY_169_15_COMPLETED_TOPOLOGY_PROOF=MERGED_MAIN_ANCESTRY_AND_BRANCH_WORKTREE_ABSENT' \
  'STORY_169_15_CLEANUP_PROOF=PASS' \
  > "$CLEANUP_TRANSACTION_TMP"
chmod 600 "$CLEANUP_TRANSACTION_TMP"
test "$(awk 'END { print NR }' "$CLEANUP_TRANSACTION_TMP")" = "24"
assert_absent_non_symlink_residue "$STORY_CLEANUP_TRANSACTION_RECORD"
mv -- "$CLEANUP_TRANSACTION_TMP" "$STORY_CLEANUP_TRANSACTION_RECORD"
assert_regular_mode_600_record "$STORY_CLEANUP_TRANSACTION_RECORD"
for CLEANUP_SOURCE_SPEC in \
  "$STORY_LIFECYCLE_RECORD|$FINAL_LIFECYCLE_RECORD_SHA256" \
  "$STORY_REVIEWED_HEAD_RECORD|$REVIEWED_HEAD_RECORD_SHA256"; do
  TRANSACTION_SOURCE_PATH="${CLEANUP_SOURCE_SPEC%%|*}"
  TRANSACTION_SOURCE_SHA256="${CLEANUP_SOURCE_SPEC#*|}"
  assert_cleanup_transaction_source \
    "$TRANSACTION_SOURCE_PATH" "$TRANSACTION_SOURCE_SHA256"
  if test -e "$TRANSACTION_SOURCE_PATH"; then
    rm -- "$TRANSACTION_SOURCE_PATH"
  fi
  assert_absent_non_symlink_residue "$TRANSACTION_SOURCE_PATH"
done
rm -- "$STORY_CLEANUP_TRANSACTION_RECORD"
assert_absent_non_symlink_residue "$STORY_CLEANUP_TRANSACTION_RECORD"
git -C "$FRONTEND_REPOSITORY" status --short --branch
```

Before the first push, the fence has consumed the exact review-bootstrap record and persists the mode-600 reviewed-head record binding base, evidence commit, feature commit/direct-parent/tree, cumulative-manifest hash, and both final reviewer identities/PASS dispositions. Separately, PENDING lifecycle publication is create-or-byte-identical and may recover either the exact OPEN PR or an already MERGED exact PR without issuing a second merge. Cleanup atomically publishes mode-600 `story-169-15-cleanup-transaction-v1` before deleting either source record; the transaction binds exact source paths/hashes, merged-PR and refreshed-main topology, branch/worktree cleanup proof, and the deletion set. Reruns reauthenticate the exact single fetch/push remote identity, PR base/head topology, local/remote branch absence, and worktree-inventory absence. Before source existence is considered, every transaction-bound source SHA-256 must be exactly 64 lowercase hexadecimal characters; malformed values fail closed whether the source is present or already absent, and executable synthetics prove both states alongside the two valid-hash states. Only then may cleanup accept each source as exact-present or already absent under that transaction, delete idempotently, prove both absent, then delete and prove absence of the transaction record.

## Acceptance and stop conditions

- Request serialization exactly matches the merged Story 169.14 DTO.
- The wire contract accepts exactly `pending | processing | completed | failed`; the frontend-only `unknown` sentinel remains distinguishable from `failed`, emits defensive diagnostics, and cannot produce false terminal evidence.
- Polling continues through pending/processing, stops at completed/failed, and preserves missing-ID/disabled behavior.
- Authoritative imported-row results and actionable failure details reach consumers without generic/zero loss.
- Before worktree creation, the exact nine-line Story 169.14 cleanup authority is independently reconstructed from committed/static authoritative fields and its complete-byte SHA-256 equals the committed handoff hash; valid reconstruction passes, every changed field/hash fails, clean mode accepts only `NONE\n`, deferral rejects empty/whitespace-blank/unsorted/duplicate payloads while accepting the exact canonical payload, and all expected-absent record/bootstrap/transaction/worktree paths reject dangling symlinks.
- No route-owned presentation, route tests, E2E spec, generic primitive, unrelated shared boundary, dependency, backend source, or partial-success feature changes.
- Targeted tests, frontend gates, diff/scope audit, two reviews, PR/merge, and mandatory cleanup pass.

Stop on missing/ambiguous Story 169.14 merge evidence, unrecorded or overlapping backend foreign WIP, inability to prove merge ancestry on refreshed backend `origin/main`, need for route presentation changes, shared ownership conflict, unresolved error-data/privacy findings, required production access, or scope expansion. No deploy, production operation, direct push to `main`, force-push, or destructive cleanup outside the exact documented post-merge removal of this Story's merged branch and temporary worktree is authorized.
