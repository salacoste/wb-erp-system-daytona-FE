---
storyId: '169.15'
storyTitle: 'Align the Shared Frontend Paid-Storage Import Boundary'
epic: '169-FE'
status: backlog
masterPlan: .omx/plans/shadcn-full-ui-migration-master.md
bmadArtifact: _bmad-output/planning-artifacts/epics-166-174-fe-shadcn-migration.md
repository: /Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend
branch: cdx/epic-169-story-15-storage-import-boundary
temporaryWorktree: /private/tmp/wb-repricer-fe-169-15-storage-import-boundary
productionScope: forbidden
---

# OMX Plan — Story 169.15: Align the Shared Frontend Paid-Storage Import Boundary

## Outcome

Align the shared frontend paid-storage import request, lifecycle, result, error, and polling boundary to the real merged Story 169.14 backend contract. The backend-authoritative wire lifecycle remains `pending | processing | completed | failed`; the frontend may retain an internal `unknown` sentinel for malformed or future wire values, but it must stay distinguishable from `failed`, emit defensive diagnostics, and never become false terminal evidence. The route must receive imported-row results and actionable failure detail without route-local contract duplication or a fabricated partial-success state.

This is a non-route shared frontend prerequisite. It changes no storage-route presentation, route-ledger row, generic design-system primitive, product composition, or E2E route spec. Story 169.12 remains the sole owner of `src/app/(dashboard)/analytics/storage/**` migration and browser/visual evidence.

## Authority and exact scope

- **Repository:** `/Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend`.
- **Owned/allowed production files:**
  - `src/lib/api/storage-analytics.ts`;
  - `src/lib/api/storage-import-normalizer.ts`;
  - `src/types/storage-analytics-trends.ts`;
  - `src/types/storage-analytics/index.ts` only if the public export surface must change;
  - `src/hooks/useImportStatus.ts`.
- **Owned/allowed direct tests:**
  - `src/lib/api/__tests__/storage-analytics.test.ts`;
  - `src/lib/api/__tests__/storage-import-normalizer.test.ts`;
  - paid-storage boundary hunks in `src/hooks/__tests__/useStorageAnalytics.test.ts`;
  - Story-specific evidence.
- **Forbidden:** `src/app/(dashboard)/analytics/storage/**`; every route-owned tree; `e2e/storage-analytics.spec.ts`; unrelated API/hook/type/store files; `src/components/ui/**`; product/analytics-shared presentation; tokens/styles/AppShell; package/dependency files; backend repository; deploy/production operations; direct push to `main`; force-push.
- Preserve query keys, mutation invalidation, cabinet/auth headers, date semantics, consumer-visible Russian presentation ownership, and all unrelated storage analytics query behavior.
- A required file outside the exact list is a stop condition and must return to the orchestrator for ownership correction.

**Merged frontend preface baseline:** PR #226 merged as `2c7a3c5931dbc9890ed585eaf71f5717c04453b2`. Inside this Story's allowed surface it already changed `src/types/storage-analytics-trends.ts`, `src/lib/api/storage-import-normalizer.ts`, and `src/lib/api/__tests__/storage-import-normalizer.test.ts` to preserve an internal `unknown` sentinel rather than coerce it to `failed`. It also changed route-owned `src/app/(dashboard)/analytics/storage/components/useStorageImport.ts` and seven other storage-query/fixture/consumer files that remain forbidden here. Story 169.15 locks that preface behavior and owns only the remaining post-169.14 reconciliation; it must not force-edit a path merely to claim work already merged by PR #226.

## Prerequisite DAG and merge evidence

```text
Story 169.14 backend PR merged and cleaned
  -> exact backend merge SHA is an ancestor of backend main
  -> read final request/status/result/error fixtures and API documentation
  -> refresh frontend main
  -> Story 169.15 RED shared-boundary proof
  -> GREEN implementation and local validation
  -> independent review pass 1 -> fixes/reruns
  -> independent review pass 2 -> fixes/reruns
  -> PR merge and mandatory cleanup
  -> Story 169.12 contract closeout may start from refreshed frontend main containing PR #227
```

Before worktree creation, consume the exact Story 169.14 backend merge SHA from its approved delivery record. Do not rediscover it from commit-message text. Refresh backend `origin/main`, prove the recorded SHA is an ancestor of both refreshed backend `origin/main` and local backend `main`, and prove the exact backend Story branch and worktree were cleaned. Refresh frontend `main`, prove PR #226 merge `2c7a3c5931dbc9890ed585eaf71f5717c04453b2` is an ancestor, and inventory any later changes to the allowed surface. Do not simulate a missing or unmerged backend contract and call the frontend boundary GREEN. Inventory current frontend worktrees/writers and stop on overlap with any allowed production or test hunk.

## Checkout-independent Git lifecycle

### Create the isolated frontend lane

```bash
FRONTEND_REPOSITORY="/Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend"
BACKEND_REPOSITORY="/Users/r2d2/Documents/Code_Projects/wb-repricer-system-new"
STORY_BRANCH="cdx/epic-169-story-15-storage-import-boundary"
STORY_WORKTREE="/private/tmp/wb-repricer-fe-169-15-storage-import-boundary"
STORY_169_14_BRANCH="cdx/epic-169-story-14-paid-storage-import-contract"
STORY_169_14_WORKTREE="/private/tmp/wb-repricer-be-169-14-paid-storage-import-contract"
RECORDED_STORY_169_14_MERGE_SHA="${RECORDED_STORY_169_14_MERGE_SHA:?export the exact SHA from the approved Story 169.14 delivery record}"
PR_226_PREFACE_MERGE_SHA="2c7a3c5931dbc9890ed585eaf71f5717c04453b2"

git -C "$BACKEND_REPOSITORY" fetch origin
git -C "$BACKEND_REPOSITORY" switch main
git -C "$BACKEND_REPOSITORY" pull --ff-only origin main
test "$(git -C "$BACKEND_REPOSITORY" rev-parse main)" = "$(git -C "$BACKEND_REPOSITORY" rev-parse origin/main)"
git -C "$BACKEND_REPOSITORY" merge-base --is-ancestor "$RECORDED_STORY_169_14_MERGE_SHA" origin/main
git -C "$BACKEND_REPOSITORY" merge-base --is-ancestor "$RECORDED_STORY_169_14_MERGE_SHA" main
test -z "$(git -C "$BACKEND_REPOSITORY" branch --list "$STORY_169_14_BRANCH")"
test -z "$(git -C "$BACKEND_REPOSITORY" ls-remote --heads origin "$STORY_169_14_BRANCH")"
test ! -e "$STORY_169_14_WORKTREE"
test -z "$(git -C "$BACKEND_REPOSITORY" worktree list --porcelain | rg -F "$STORY_169_14_WORKTREE")"

test "$(git -C "$FRONTEND_REPOSITORY" rev-parse --show-toplevel)" = "$FRONTEND_REPOSITORY"
test ! -e "$STORY_WORKTREE"
test -z "$(git -C "$FRONTEND_REPOSITORY" branch --list "$STORY_BRANCH")"
test -z "$(git -C "$FRONTEND_REPOSITORY" worktree list --porcelain | rg -F "$STORY_WORKTREE")"
git -C "$FRONTEND_REPOSITORY" fetch origin
git -C "$FRONTEND_REPOSITORY" switch main
git -C "$FRONTEND_REPOSITORY" pull --ff-only origin main
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor "$PR_226_PREFACE_MERGE_SHA" origin/main
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor "$PR_226_PREFACE_MERGE_SHA" main
STORY_BASE_SHA="$(git -C "$FRONTEND_REPOSITORY" rev-parse HEAD)"
git -C "$FRONTEND_REPOSITORY" worktree add -b "$STORY_BRANCH" "$STORY_WORKTREE" "$STORY_BASE_SHA"
git -C "$STORY_WORKTREE" status --short --branch
```

The exact recorded SHA is mandatory input; merge-message grep or another fuzzy lookup is forbidden. Never reuse a stale branch/worktree or clean/revert another owner's work.

## RED-first implementation sequence

1. Read the merged Story 169.14 DTO/controller/status fixtures and authoritative API documentation; record exact request keys and response fields.
2. Record PR #226 as the frontend preface baseline, inventory every later consumer/change to the allowed API/type/normalizer/hook paths, and lock its `unknown`-sentinel regression tests before writing new RED cases. Do not relabel already-green PR #226 behavior as Story 169.15 RED.
3. Add honest RED tests only for gaps that remain against the merged Story 169.14 contract: exact request serialization, start/status field aliases, imported-row result propagation, actionable failure detail, shared polling termination/recovery, and defensive diagnostics.
4. Cover pending, processing, completed with imported rows, failed with actionable detail, snake/camel aliases only where the merged contract explicitly requires them, and unknown/malformed values. Assert that `unknown` remains distinguishable from `failed` and cannot create false terminal evidence.
5. After RED identifies the acceptance-necessary changes and before the first implementation edit, freeze the exact newline-delimited reviewed changed-file manifest in the Story delivery record and export it as `STORY_169_15_FROZEN_REVIEWED_MANIFEST`. Do not derive it from the later Git diff and do not include an unchanged preface path merely to satisfy a static list.
6. Prove RED is behavioral, not broken imports/fixtures/types, and retain exact failing output.
7. Implement the smallest type/API/normalizer/polling alignment. Keep the authoritative mapping in the shared boundary; do not duplicate it in Story 169.12's route.
8. Preserve an explicit defensive `unknown` sentinel and diagnostics for unrecognized lifecycle values; do not coerce it to `failed`, treat it as success, or synthesize `partial`, counts, or retry subsets.
9. Run targeted and universal frontend validation, exact scope audit, review pass 1, fixes/reruns, review pass 2, and final readiness verification.

## Validation

Run in this order and record commands, exit codes, counts, and complete failures:

```bash
npx vitest run src/lib/api/__tests__/storage-analytics.test.ts src/lib/api/__tests__/storage-import-normalizer.test.ts src/hooks/__tests__/useStorageAnalytics.test.ts
npm run format:check
npm run lint
npm run type-check
npm run check:max-lines
npm run build
git diff --check
```

No browser/E2E or route-presentation proof is claimed here. `e2e/storage-analytics.spec.ts` is read-only and belongs to Story 169.12's exact-path validation. An unavailable backend integration check is a named gap, never a pass.

Record Node/npm versions, frontend base SHA, Story 169.14 merge SHA/ancestry, request/response fixtures, exact changed-file manifest, targeted/full gate output, defensive-unknown diagnostics evidence, and reviewer dispositions.

## Independent review

1. **Pass 1 — adversarial code review:** a fresh reviewer who did not author the change checks merged-contract fidelity, request serialization, type/normalizer strictness, polling termination, rows/error preservation, query/invalidation stability, tests, diagnostics, and exact scope.
2. Resolve every accepted finding and rerun the smallest affected checks plus all universal frontend gates.
3. **Pass 2 — fresh verifier review:** a different fresh context verifies final diff, recorded backend merge proof, no route/E2E/forbidden files, no fabricated partial state, test evidence, and PR readiness.
4. Do not commit with unresolved accepted findings or self-approval as the only evidence.

## Commit, push, PR, and merge

Run only after the backend prerequisite, RED→GREEN proof, all local gates, both reviews, and exact scope audit:

```bash
FRONTEND_REPOSITORY="/Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend"
STORY_BRANCH="cdx/epic-169-story-15-storage-import-boundary"
STORY_WORKTREE="/private/tmp/wb-repricer-fe-169-15-storage-import-boundary"
ORIGIN_URL="$(git -C "$FRONTEND_REPOSITORY" remote get-url origin)"
REPOSITORY="$(gh repo view "$ORIGIN_URL" --json nameWithOwner --jq '.nameWithOwner')"

test "$(git -C "$STORY_WORKTREE" branch --show-current)" = "$STORY_BRANCH"
git -C "$STORY_WORKTREE" diff --check
test -z "$(git -C "$STORY_WORKTREE" diff --cached --name-only)"
STORY_ALLOWED_MANIFEST="$(printf '%s\n' \
  'src/hooks/__tests__/useStorageAnalytics.test.ts' \
  'src/hooks/useImportStatus.ts' \
  'src/lib/api/__tests__/storage-analytics.test.ts' \
  'src/lib/api/__tests__/storage-import-normalizer.test.ts' \
  'src/lib/api/storage-analytics.ts' \
  'src/lib/api/storage-import-normalizer.ts' \
  'src/types/storage-analytics-trends.ts' \
  'src/types/storage-analytics/index.ts' | sort)"
STORY_REQUIRED_MANIFEST="$(printf '%s\n' \
  'src/hooks/__tests__/useStorageAnalytics.test.ts' \
  'src/lib/api/__tests__/storage-analytics.test.ts' \
  'src/lib/api/__tests__/storage-import-normalizer.test.ts' \
  'src/lib/api/storage-analytics.ts' \
  'src/lib/api/storage-import-normalizer.ts' | sort)"
REVIEWED_MANIFEST="$(printf '%s\n' "${STORY_169_15_FROZEN_REVIEWED_MANIFEST:?export the exact manifest frozen after RED and before implementation edits}" | sed '/^$/d' | sort -u)"
ACTUAL_CHANGED_MANIFEST="$({
  git -C "$STORY_WORKTREE" diff --name-only
  git -C "$STORY_WORKTREE" ls-files --others --exclude-standard
} | sed '/^$/d' | sort -u)"
test -n "$REVIEWED_MANIFEST"
test -n "$ACTUAL_CHANGED_MANIFEST"
test -z "$(comm -23 \
  <(printf '%s\n' "$REVIEWED_MANIFEST") \
  <(printf '%s\n' "$STORY_ALLOWED_MANIFEST"))"
test -z "$(comm -23 \
  <(printf '%s\n' "$STORY_REQUIRED_MANIFEST") \
  <(printf '%s\n' "$REVIEWED_MANIFEST"))"
test "$ACTUAL_CHANGED_MANIFEST" = "$REVIEWED_MANIFEST"
printf '%s\n' "$REVIEWED_MANIFEST"
# The exact allowlist excludes the storage route tree and e2e/storage-analytics.spec.ts.
# PR #226 preface paths are not forced edits. An optional allowed path is valid only when
# post-169.14 RED evidence added it to the frozen manifest before its first Story edit.
# Any later addition requires a separate ownership/scope review,
# affected RED rerun, manifest re-freeze, and inclusion in both independent reviews.
while IFS= read -r STORY_PATH; do
  git -C "$STORY_WORKTREE" add -- "$STORY_PATH"
done <<< "$REVIEWED_MANIFEST"
git -C "$STORY_WORKTREE" diff --cached --check
CACHED_MANIFEST="$(git -C "$STORY_WORKTREE" diff --cached --name-only | sort)"
test "$CACHED_MANIFEST" = "$REVIEWED_MANIFEST"
git -C "$STORY_WORKTREE" commit -m "fix(analytics): align paid-storage import boundary" -m "Implement Story 169.15 against the merged Story 169.14 request/status/result contract with polling, normalization, failure-detail, contract-test, and cleanup evidence."
git -C "$STORY_WORKTREE" push -u origin "$STORY_BRANCH"
STORY_PR_URL="$(gh pr create --repo "$REPOSITORY" --base main --head "$STORY_BRANCH" --title "Story 169.15: align paid-storage import boundary" --body "Implements the approved shared frontend paid-storage contract boundary against merged Story 169.14 with RED-first tests, local validation, two independent reviews, and mandatory cleanup. No route UI, partial-success feature, deploy, production, direct-main, or force-push authority.")"
STORY_PR_NUMBER="$(gh pr view "$STORY_PR_URL" --repo "$REPOSITORY" --json number --jq '.number')"
gh pr merge "$STORY_PR_NUMBER" --repo "$REPOSITORY" --merge
```

## Mandatory cleanup

```bash
FRONTEND_REPOSITORY="/Users/r2d2/Documents/Code_Projects/wb-repricer-system-new/frontend"
STORY_BRANCH="cdx/epic-169-story-15-storage-import-boundary"
STORY_WORKTREE="/private/tmp/wb-repricer-fe-169-15-storage-import-boundary"
ORIGIN_URL="$(git -C "$FRONTEND_REPOSITORY" remote get-url origin)"
REPOSITORY="$(gh repo view "$ORIGIN_URL" --json nameWithOwner --jq '.nameWithOwner')"
STORY_PR_NUMBER="$(gh pr list --repo "$REPOSITORY" --head "$STORY_BRANCH" --state merged --json number --jq '.[0].number')"
STORY_MERGE_SHA="$(gh pr view "$STORY_PR_NUMBER" --repo "$REPOSITORY" --json mergeCommit --jq '.mergeCommit.oid')"

git -C "$FRONTEND_REPOSITORY" switch main
git -C "$FRONTEND_REPOSITORY" pull --ff-only origin main
git -C "$FRONTEND_REPOSITORY" merge-base --is-ancestor "$STORY_MERGE_SHA" main
git -C "$FRONTEND_REPOSITORY" push origin --delete "$STORY_BRANCH"
git -C "$FRONTEND_REPOSITORY" worktree remove "$STORY_WORKTREE"
git -C "$FRONTEND_REPOSITORY" branch -d "$STORY_BRANCH"
git -C "$FRONTEND_REPOSITORY" worktree prune
test ! -e "$STORY_WORKTREE"
test -z "$(git -C "$FRONTEND_REPOSITORY" worktree list --porcelain | rg -F "$STORY_WORKTREE")"
test -z "$(git -C "$FRONTEND_REPOSITORY" branch --list "$STORY_BRANCH")"
test -z "$(git -C "$FRONTEND_REPOSITORY" ls-remote --heads origin "$STORY_BRANCH")"
git -C "$FRONTEND_REPOSITORY" status --short --branch
```

Record PR URL/number, feature commit SHA, merge SHA, ancestry proof, both reviews, final validation summary, remote/local branch absence, and worktree removal/prune before the Story 169.12 contract closeout starts.

## Acceptance and stop conditions

- Request serialization exactly matches the merged Story 169.14 DTO.
- The wire contract accepts exactly `pending | processing | completed | failed`; the frontend-only `unknown` sentinel remains distinguishable from `failed`, emits defensive diagnostics, and cannot produce false terminal evidence.
- Polling continues through pending/processing, stops at completed/failed, and preserves missing-ID/disabled behavior.
- Authoritative imported-row results and actionable failure details reach consumers without generic/zero loss.
- No route-owned presentation, route tests, E2E spec, generic primitive, unrelated shared boundary, dependency, backend source, or partial-success feature changes.
- Targeted tests, frontend gates, diff/scope audit, two reviews, PR/merge, and mandatory cleanup pass.

Stop on missing/ambiguous Story 169.14 merge evidence, need for route presentation changes, shared ownership conflict, unresolved error-data/privacy findings, required production access, or scope expansion. No deploy, production operation, direct push to `main`, force-push, or destructive cleanup outside the exact documented post-merge removal of this Story's merged branch and temporary worktree is authorized.
