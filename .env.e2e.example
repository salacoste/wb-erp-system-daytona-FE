# E2E Test Environment Configuration
# Copy this file to .env.e2e and fill in your test credentials
# IMPORTANT: .env.e2e is excluded from version control via .gitignore

# =============================================================================
# E2E TEST CONFIGURATION
# =============================================================================

# Frontend URL (default: Next.js dev server)
E2E_BASE_URL=http://localhost:3100

# Backend API URL (default: NestJS backend)
E2E_API_URL=http://localhost:3000

# =============================================================================
# TEST USER CREDENTIALS
# =============================================================================
# These credentials MUST match the seeded test user in prisma/seed.ts
# Default seeded credentials: test@test.com / Russia23!
#
# SECURITY NOTES:
# - These are DEVELOPMENT/TEST credentials only
# - NEVER use production credentials here
# - For CI/CD: Use secret management (GitHub Secrets, GitLab Variables, etc.)

E2E_TEST_EMAIL=test@test.com
E2E_TEST_PASSWORD=Russia23!

# =============================================================================
# TEST CONFIGURATION
# =============================================================================

# Request timeout in milliseconds (default: 30000)
E2E_REQUEST_TIMEOUT=30000

# Screenshot directory for test failures
E2E_SCREENSHOT_DIR=test-results/screenshots

# Debug mode (set to 'true' to enable Playwright Inspector)
E2E_DEBUG=false

# Headless mode (set to 'false' to watch tests run in browser)
E2E_HEADED=false

# =============================================================================
# OPTIONAL: TEST WB API TOKEN
# =============================================================================
# Optional: Use a real WB API token for integration tests
# Leave empty to use mock data
E2E_WB_TOKEN=

# =============================================================================
# INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file to .env.e2e:
#    cp .env.e2e.example .env.e2e
#
# 2. Ensure the database is seeded with test user:
#    npm run seed
#
# 3. Run E2E tests:
#    npm run test:e2e
#
# 4. For CI/CD environments:
#    - Set E2E_TEST_EMAIL and E2E_TEST_PASSWORD as CI secrets
#    - Do NOT commit .env.e2e file
#    - Use encrypted secrets for production deployments
#
# =============================================================================
